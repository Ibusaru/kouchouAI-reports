1:"$Sreact.fragment"
2:I[16501,["771","static/chunks/771-6df7383af9cb22cb.js","732","static/chunks/732-1930f1f7bd6746a5.js","13","static/chunks/13-f12e2db1f59fda05.js","355","static/chunks/355-f98f115f8c6f56c6.js","177","static/chunks/app/layout-be7f58cf6f9b9f21.js"],"Provider"]
3:I[9766,[],""]
4:I[98924,[],""]
5:I[58923,["771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","335","static/chunks/app/%5Bslug%5D/error-b2784470a4722ef4.js"],"default"]
6:I[52619,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],""]
7:I[54921,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Button"]
9:I[24431,[],"OutletBoundary"]
b:I[15278,[],"AsyncMetadataOutlet"]
d:I[24431,[],"ViewportBoundary"]
f:I[24431,[],"MetadataBoundary"]
10:"$Sreact.suspense"
12:I[57150,[],""]
:HL["/kouchouAI-reports/_next/static/css/a63443551c7d7d9f.css","style"]
0:{"P":null,"b":"-Ec3yNVR6tcE1Pi-oBI6I","p":"/kouchouAI-reports","c":["","2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77",""],"i":false,"f":[[["",{"children":[["slug","2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77","d"],{"children":["__PAGE__",{}]}]},"$undefined","$undefined",true],["",["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/kouchouAI-reports/_next/static/css/a63443551c7d7d9f.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]],["$","html",null,{"suppressHydrationWarning":true,"lang":"ja","children":[["$","head",null,{"children":[["$","link",null,{"rel":"preconnect","href":"https://fonts.googleapis.com"}],["$","link",null,{"rel":"preconnect","href":"https://fonts.gstatic.com","crossOrigin":"anonymous"}],["$","link",null,{"href":"https://fonts.googleapis.com/css2?family=BIZ+UDPGothic&display=swap","rel":"stylesheet"}],["$","link",null,{"rel":"icon","href":"/kouchouAI-reports/meta/icon.png","sizes":"any"}],false]}],["$","body",null,{"children":["$","$L2",null,{"children":["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","title",null,{"children":"404: This page could not be found."}],["$","div",null,{"style":{"fontFamily":"system-ui,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif,\"Apple Color Emoji\",\"Segoe UI Emoji\"","height":"100vh","textAlign":"center","display":"flex","flexDirection":"column","alignItems":"center","justifyContent":"center"},"children":["$","div",null,{"children":[["$","style",null,{"dangerouslySetInnerHTML":{"__html":"body{color:#000;background:#fff;margin:0}.next-error-h1{border-right:1px solid rgba(0,0,0,.3)}@media (prefers-color-scheme:dark){body{color:#fff;background:#000}.next-error-h1{border-right:1px solid rgba(255,255,255,.3)}}"}}],["$","h1",null,{"className":"next-error-h1","style":{"display":"inline-block","margin":"0 20px 0 0","padding":"0 23px 0 0","fontSize":24,"fontWeight":500,"verticalAlign":"top","lineHeight":"49px"},"children":404}],["$","div",null,{"style":{"display":"inline-block"},"children":["$","h2",null,{"style":{"fontSize":14,"fontWeight":400,"lineHeight":"49px","margin":0},"children":"This page could not be found."}]}]]}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]}]}]]}]]}],{"children":[["slug","2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77","d"],["$","$1","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$5","errorStyles":[],"errorScripts":[],"template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","p",null,{"children":"ページが見つかりませんでした"}],["$","$L6",null,{"href":"/","children":["$","$L7",null,{"children":"トップに戻る"}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":["__PAGE__",["$","$1","c",{"children":["$L8",null,["$","$L9",null,{"children":["$La",["$","$Lb",null,{"promise":"$@c"}]]}]]}],{},null,false]},null,false]},null,false],["$","$1","h",{"children":[null,[["$","$Ld",null,{"children":"$Le"}],null],["$","$Lf",null,{"children":["$","div",null,{"hidden":true,"children":["$","$10",null,{"fallback":null,"children":"$L11"}]}]}]]}],false]],"m":"$undefined","G":["$12",[]],"s":false,"S":true}
e:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
a:null
13:I[67733,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Header"]
14:I[99347,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Box"]
15:I[55756,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Heading"]
16:I[48409,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Text"]
17:I[92091,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Icon"]
18:I[6026,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"ClientContainer"]
19:T194c,import concurrent.futures
import json
import logging
import os
import re

import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai
from services.parse_json_list import parse_extraction_response
from utils import update_progress

COMMA_AND_SPACE_AND_RIGHT_BRACKET = re.compile(r",\s*(\])")


class ExtractionResponse(BaseModel):
    extractedOpinionList: list[str] = Field(..., description="抽出した意見のリスト")


def _validate_property_columns(property_columns: list[str], comments: pd.DataFrame) -> None:
    if not all(property in comments.columns for property in property_columns):
        raise ValueError(f"Properties {property_columns} not found in comments. Columns are {comments.columns}")


def extraction(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/args.csv"
    model = config["extraction"]["model"]
    prompt = config["extraction"]["prompt"]
    workers = config["extraction"]["workers"]
    limit = config["extraction"]["limit"]
    property_columns = config["extraction"]["properties"]

    if "provider" not in config:
        raise RuntimeError("provider is not set")
    provider = config["provider"]

    # カラム名だけを読み込み、必要なカラムが含まれているか確認する
    comments = pd.read_csv(f"inputs/{config['input']}.csv", nrows=0)
    _validate_property_columns(property_columns, comments)
    # エラーが出なかった場合、すべての行を読み込む
    comments = pd.read_csv(
        f"inputs/{config['input']}.csv", usecols=["comment-id", "comment-body"] + config["extraction"]["properties"]
    )
    comment_ids = (comments["comment-id"].values)[:limit]
    comments.set_index("comment-id", inplace=True)
    results = pd.DataFrame()
    update_progress(config, total=len(comment_ids))

    argument_map = {}
    relation_rows = []

    for i in tqdm(range(0, len(comment_ids), workers)):
        batch = comment_ids[i : i + workers]
        batch_inputs = [comments.loc[id]["comment-body"] for id in batch]
        batch_results = extract_batch(
            batch_inputs, prompt, model, workers, provider, config.get("local_llm_address"), config
        )

        for comment_id, extracted_args in zip(batch, batch_results, strict=False):
            for j, arg in enumerate(extracted_args):
                if arg not in argument_map:
                    # argumentテーブルに追加
                    arg_id = f"A{comment_id}_{j}"
                    argument = arg
                    argument_map[arg] = {
                        "arg-id": arg_id,
                        "argument": argument,
                    }
                else:
                    arg_id = argument_map[arg]["arg-id"]

                # relationテーブルにcommentとargの関係を追加
                relation_row = {
                    "arg-id": arg_id,
                    "comment-id": comment_id,
                }
                relation_rows.append(relation_row)

        update_progress(config, incr=len(batch))

    # DataFrame化
    results = pd.DataFrame(argument_map.values())
    relation_df = pd.DataFrame(relation_rows)

    if results.empty:
        raise RuntimeError("result is empty, maybe bad prompt")

    results.to_csv(path, index=False)
    # comment-idとarg-idの関係を保存
    relation_df.to_csv(f"outputs/{dataset}/relations.csv", index=False)


logging.basicConfig(level=logging.DEBUG)


def extract_batch(batch, prompt, model, workers, provider="openai", local_llm_address=None, config=None):
    with concurrent.futures.ThreadPoolExecutor(max_workers=workers) as executor:
        futures_with_index = [
            (i, executor.submit(extract_arguments, input, prompt, model, provider, local_llm_address))
            for i, input in enumerate(batch)
        ]

        done, not_done = concurrent.futures.wait([f for _, f in futures_with_index], timeout=30)
        results = [[] for _ in range(len(batch))]
        total_token_input = 0
        total_token_output = 0
        total_token_usage = 0

        for _, future in futures_with_index:
            if future in not_done and not future.cancelled():
                future.cancel()

        for i, future in futures_with_index:
            if future in done:
                try:
                    result = future.result()
                    if isinstance(result, tuple) and len(result) == 4:
                        items, token_input, token_output, token_total = result
                        results[i] = items
                        total_token_input += token_input
                        total_token_output += token_output
                        total_token_usage += token_total
                    else:
                        results[i] = result
                except Exception as e:
                    logging.error(f"Task {future} failed with error: {e}")
                    results[i] = []

        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + total_token_usage
            config["token_usage_input"] = config.get("token_usage_input", 0) + total_token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + total_token_output
            print(
                f"Extraction batch: input={total_token_input}, output={total_token_output}, total={total_token_usage} tokens"
            )

        return results


def extract_arguments(input, prompt, model, provider="openai", local_llm_address=None):
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            is_json=False,
            json_schema=ExtractionResponse,
            provider=provider,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )
        items = parse_extraction_response(response)
        items = list(filter(None, items))  # omit empty strings
        return items, token_input, token_output, token_total
    except json.decoder.JSONDecodeError as e:
        print("JSON error:", e)
        print("Input was:", input)
        print("Response was:", response)
        print("Silently giving up on trying to generate valid list.")
        return []
1a:T1149,"""Cluster the arguments using UMAP + HDBSCAN and GPT-4."""

from importlib import import_module

import numpy as np
import pandas as pd
import scipy.cluster.hierarchy as sch
from sklearn.cluster import KMeans


def hierarchical_clustering(config):
    UMAP = import_module("umap").UMAP

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_clusters.csv"
    arguments_df = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings_df = pd.read_pickle(f"outputs/{dataset}/embeddings.pkl")
    embeddings_array = np.asarray(embeddings_df["embedding"].values.tolist())
    cluster_nums = config["hierarchical_clustering"]["cluster_nums"]

    n_samples = embeddings_array.shape[0]
    # デフォルト設定は15
    default_n_neighbors = 15

    # テスト等サンプルが少なすぎる場合、n_neighborsの設定値を下げる
    if n_samples <= default_n_neighbors:
        n_neighbors = max(2, n_samples - 1)  # 最低2以上
    else:
        n_neighbors = default_n_neighbors

    umap_model = UMAP(random_state=42, n_components=2, n_neighbors=n_neighbors)
    # TODO 詳細エラーメッセージを加える
    # 以下のエラーの場合、おそらく元の意見件数が少なすぎることが原因
    # TypeError: Cannot use scipy.linalg.eigh for sparse A with k >= N. Use scipy.linalg.eigh(A.toarray()) or reduce k.
    umap_embeds = umap_model.fit_transform(embeddings_array)

    cluster_results = hierarchical_clustering_embeddings(
        umap_embeds=umap_embeds,
        cluster_nums=cluster_nums,
    )
    result_df = pd.DataFrame(
        {
            "arg-id": arguments_df["arg-id"],
            "argument": arguments_df["argument"],
            "x": umap_embeds[:, 0],
            "y": umap_embeds[:, 1],
        }
    )

    for cluster_level, final_labels in enumerate(cluster_results.values(), start=1):
        result_df[f"cluster-level-{cluster_level}-id"] = [f"{cluster_level}_{label}" for label in final_labels]

    result_df.to_csv(path, index=False)


def generate_cluster_count_list(min_clusters: int, max_clusters: int):
    cluster_counts = []
    current = min_clusters
    cluster_counts.append(current)

    if min_clusters == max_clusters:
        return cluster_counts

    while True:
        next_double = current * 2
        next_triple = current * 3

        if next_double >= max_clusters:
            if cluster_counts[-1] != max_clusters:
                cluster_counts.append(max_clusters)
            break

        # 次の倍はまだ max_clusters に収まるが、3倍だと超える
        # -> (次の倍は細かすぎるので)スキップして max_clusters に飛ぶ
        if next_triple > max_clusters:
            cluster_counts.append(max_clusters)
            break

        cluster_counts.append(next_double)
        current = next_double

    return cluster_counts


def merge_clusters_with_hierarchy(
    cluster_centers: np.ndarray,
    kmeans_labels: np.ndarray,
    umap_array: np.ndarray,
    n_cluster_cut: int,
):
    Z = sch.linkage(cluster_centers, method="ward")
    cluster_labels_merged = sch.fcluster(Z, t=n_cluster_cut, criterion="maxclust")

    n_samples = umap_array.shape[0]
    final_labels = np.zeros(n_samples, dtype=int)

    for i in range(n_samples):
        original_label = kmeans_labels[i]
        final_labels[i] = cluster_labels_merged[original_label]

    return final_labels


def hierarchical_clustering_embeddings(
    umap_embeds,
    cluster_nums,
):
    # 最大分割数でクラスタリングを実施
    print("start initial clustering")
    initial_cluster_num = cluster_nums[-1]
    kmeans_model = KMeans(n_clusters=initial_cluster_num, random_state=42)
    kmeans_model.fit(umap_embeds)
    print("end initial clustering")

    results = {}
    print("start hierarchical clustering")
    cluster_nums.sort()
    print(cluster_nums)
    for n_cluster_cut in cluster_nums[:-1]:
        print("n_cluster_cut: ", n_cluster_cut)
        final_labels = merge_clusters_with_hierarchy(
            cluster_centers=kmeans_model.cluster_centers_,
            kmeans_labels=kmeans_model.labels_,
            umap_array=umap_embeds,
            n_cluster_cut=n_cluster_cut,
        )
        results[n_cluster_cut] = final_labels

    results[initial_cluster_num] = kmeans_model.labels_
    print("end hierarchical clustering")

    return results
1b:T1c36,import json
import os
from concurrent.futures import ThreadPoolExecutor
from functools import partial
from typing import TypedDict

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class LabellingResult(TypedDict):
    """各クラスタのラベリング結果を表す型"""

    cluster_id: str  # クラスタのID
    label: str  # クラスタのラベル名
    description: str  # クラスタの説明文


def hierarchical_initial_labelling(config: dict) -> None:
    """階層的クラスタリングの初期ラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_initial_labelling: 初期ラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_initial_labels.csv"
    clusters_argument_df = pd.read_csv(f"outputs/{dataset}/hierarchical_clusters.csv")

    cluster_id_columns = [col for col in clusters_argument_df.columns if col.startswith("cluster-level-")]
    initial_cluster_id_column = cluster_id_columns[-1]
    sampling_num = config["hierarchical_initial_labelling"]["sampling_num"]
    initial_labelling_prompt = config["hierarchical_initial_labelling"]["prompt"]
    model = config["hierarchical_initial_labelling"]["model"]
    workers = config["hierarchical_initial_labelling"]["workers"]

    # トークン使用量を追跡するための変数を初期化
    config["total_token_usage"] = config.get("total_token_usage", 0)

    initial_label_df = initial_labelling(
        initial_labelling_prompt,
        clusters_argument_df,
        sampling_num,
        model,
        workers,
        config["provider"],
        config.get("local_llm_address"),
        config,  # configを渡して、トークン使用量を累積できるようにする
    )
    print("start initial labelling")
    initial_clusters_argument_df = clusters_argument_df.merge(
        initial_label_df,
        left_on=initial_cluster_id_column,
        right_on="cluster_id",
        how="left",
    ).rename(
        columns={
            "label": f"{initial_cluster_id_column.replace('-id', '')}-label",
            "description": f"{initial_cluster_id_column.replace('-id', '')}-description",
        }
    )
    print("end initial labelling")
    initial_clusters_argument_df.to_csv(path, index=False)


def initial_labelling(
    prompt: str,
    clusters_df: pd.DataFrame,
    sampling_num: int,
    model: str,
    workers: int,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> pd.DataFrame:
    """各クラスタに対して初期ラベリングを実行する

    Args:
        prompt: LLMへのプロンプト
        clusters_df: クラスタリング結果のDataFrame
        sampling_num: 各クラスタからサンプリングする意見の数
        model: 使用するLLMモデル名
        workers: 並列処理のワーカー数
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        各クラスタのラベリング結果を含むDataFrame
    """
    cluster_columns = [col for col in clusters_df.columns if col.startswith("cluster-level-")]
    initial_cluster_column = cluster_columns[-1]
    cluster_ids = clusters_df[initial_cluster_column].unique()
    process_func = partial(
        process_initial_labelling,
        df=clusters_df,
        prompt=prompt,
        sampling_num=sampling_num,
        target_column=initial_cluster_column,
        model=model,
        provider=provider,
        local_llm_address=local_llm_address,
        config=config,  # configを渡す
    )
    with ThreadPoolExecutor(max_workers=workers) as executor:
        results = list(executor.map(process_func, cluster_ids))
    return pd.DataFrame(results)


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_initial_labelling(
    cluster_id: str,
    df: pd.DataFrame,
    prompt: str,
    sampling_num: int,
    target_column: str,
    model: str,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> LabellingResult:
    """個別のクラスタに対してラベリングを実行する

    Args:
        cluster_id: 処理対象のクラスタID
        df: クラスタリング結果のDataFrame
        prompt: LLMへのプロンプト
        sampling_num: サンプリングする意見の数
        target_column: クラスタIDが格納されている列名
        model: 使用するLLMモデル名
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        クラスタのラベリング結果
    """
    cluster_data = df[df[target_column] == cluster_id]
    sampling_num = min(sampling_num, len(cluster_data))
    cluster = cluster_data.sample(sampling_num)
    input = "\n".join(cluster["argument"].values)
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            provider=provider,
            json_schema=LabellingFromat,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )

        # トークン使用量を累積（configが渡されている場合）
        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
            config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + token_output

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return LabellingResult(
            cluster_id=cluster_id,
            label=response_json.get("label", "エラーでラベル名が取得できませんでした"),
            description=response_json.get("description", "エラーで解説が取得できませんでした"),
        )
    except Exception as e:
        print(e)
        return LabellingResult(
            cluster_id=cluster_id,
            label="エラーでラベル名が取得できませんでした",
            description="エラーで解説が取得できませんでした",
        )
1c:T337b,import json
import os
from concurrent.futures import ThreadPoolExecutor
from dataclasses import dataclass
from functools import partial

import numpy as np
import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai


@dataclass
class ClusterColumns:
    """同一階層のクラスター関連のカラム名を管理するクラス"""

    id: str
    label: str
    description: str

    @classmethod
    def from_id_column(cls, id_column: str) -> "ClusterColumns":
        """ID列名から関連するカラム名を生成"""
        return cls(
            id=id_column,
            label=id_column.replace("-id", "-label"),
            description=id_column.replace("-id", "-description"),
        )


@dataclass
class ClusterValues:
    """対象クラスタのlabel/descriptionを管理するクラス"""

    label: str
    description: str

    def to_prompt_text(self) -> str:
        return f"- {self.label}: {self.description}"


def hierarchical_merge_labelling(config: dict) -> None:
    """階層的クラスタリングの結果に対してマージラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_merge_labelling: マージラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    merge_path = f"outputs/{dataset}/hierarchical_merge_labels.csv"
    clusters_df = pd.read_csv(f"outputs/{dataset}/hierarchical_initial_labels.csv")

    cluster_id_columns: list[str] = _filter_id_columns(clusters_df.columns)
    # ボトムクラスタのラベル・説明とクラスタid付きの各argumentを入力し、各階層のクラスタラベル・説明を生成し、argumentに付けたdfを作成
    merge_result_df = merge_labelling(
        clusters_df=clusters_df,
        cluster_id_columns=sorted(cluster_id_columns, reverse=True),
        config=config,
    )
    # 上記のdfから各クラスタのlevel, id, label, description, valueを取得してdfを作成
    melted_df = melt_cluster_data(merge_result_df)
    # 上記のdfに親子関係を追加
    parent_child_df = _build_parent_child_mapping(merge_result_df, cluster_id_columns)
    melted_df = melted_df.merge(parent_child_df, on=["level", "id"], how="left")
    density_df = calculate_cluster_density(melted_df, config)
    density_df.to_csv(merge_path, index=False)


def _build_parent_child_mapping(df: pd.DataFrame, cluster_id_columns: list[str]):
    """クラスタ間の親子関係をマッピングする

    Args:
        df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト

    Returns:
        親子関係のマッピング情報を含むDataFrame
    """
    results = []
    top_cluster_column = cluster_id_columns[0]
    top_cluster_values = df[top_cluster_column].unique()
    for c in top_cluster_values:
        results.append(
            {
                "level": 1,
                "id": c,
                "parent": "0",  # aggregationで追加する全体クラスタのid
            }
        )

    for idx in range(len(cluster_id_columns) - 1):
        current_column = cluster_id_columns[idx]
        children_column = cluster_id_columns[idx + 1]
        current_level = current_column.replace("-id", "").replace("cluster-level-", "")
        # 現在のレベルのクラスタid
        current_cluster_values = df[current_column].unique()
        for current_id in current_cluster_values:
            children_ids = df.loc[df[current_column] == current_id, children_column].unique()
            for child_id in children_ids:
                results.append(
                    {
                        "level": int(current_level) + 1,
                        "id": child_id,
                        "parent": current_id,
                    }
                )
    return pd.DataFrame(results)


def _filter_id_columns(columns: list[str]) -> list[str]:
    """クラスタIDのカラム名をフィルタリングする

    Args:
        columns: 全カラム名のリスト

    Returns:
        クラスタIDのカラム名のリスト
    """
    return [col for col in columns if col.startswith("cluster-level-") and col.endswith("-id")]


def melt_cluster_data(df: pd.DataFrame) -> pd.DataFrame:
    """クラスタデータを行形式に変換する

    cluster-level-n-(id|label|description) を行形式 (level, id, label, description, value) にまとめる。
    [cluster-level-n-id, cluster-level-n-label, cluster-level-n-description] を [level, id, label, description, value(件数)] に変換する。

    Args:
        df: クラスタリング結果のDataFrame

    Returns:
        行形式に変換されたDataFrame
    """
    id_columns: list[str] = _filter_id_columns(df.columns)
    levels: set[int] = {int(col.replace("cluster-level-", "").replace("-id", "")) for col in id_columns}
    all_rows: list[dict] = []

    # levelごとに各クラスタの出現件数を集計・縦持ちにする
    for level in levels:
        cluster_columns = ClusterColumns.from_id_column(f"cluster-level-{level}-id")
        # クラスタidごとの件数集計
        level_count_df = df.groupby(cluster_columns.id).size().reset_index(name="value")

        level_unique_val_df = df[
            [cluster_columns.id, cluster_columns.label, cluster_columns.description]
        ].drop_duplicates()
        level_unique_val_df = level_unique_val_df.merge(level_count_df, on=cluster_columns.id, how="left")
        level_unique_vals = [
            {
                "level": level,
                "id": row[cluster_columns.id],
                "label": row[cluster_columns.label],
                "description": row[cluster_columns.description],
                "value": row["value"],
            }
            for _, row in level_unique_val_df.iterrows()
        ]
        all_rows.extend(level_unique_vals)
    return pd.DataFrame(all_rows)


def merge_labelling(clusters_df: pd.DataFrame, cluster_id_columns: list[str], config) -> pd.DataFrame:
    """階層的なクラスタのマージラベリングを実行する

    Args:
        clusters_df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含むDataFrame
    """
    for idx in tqdm(range(len(cluster_id_columns) - 1)):
        previous_columns = ClusterColumns.from_id_column(cluster_id_columns[idx])
        current_columns = ClusterColumns.from_id_column(cluster_id_columns[idx + 1])

        process_fn = partial(
            process_merge_labelling,
            result_df=clusters_df,
            current_columns=current_columns,
            previous_columns=previous_columns,
            config=config,
        )

        current_cluster_ids = sorted(clusters_df[current_columns.id].unique())
        with ThreadPoolExecutor(max_workers=config["hierarchical_merge_labelling"]["workers"]) as executor:
            responses = list(
                tqdm(
                    executor.map(process_fn, current_cluster_ids),
                    total=len(current_cluster_ids),
                )
            )

        current_result_df = pd.DataFrame(responses)
        clusters_df = clusters_df.merge(current_result_df, on=[current_columns.id])
    return clusters_df


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_merge_labelling(
    target_cluster_id: str,
    result_df: pd.DataFrame,
    current_columns: ClusterColumns,
    previous_columns: ClusterColumns,
    config,
):
    """個別のクラスタに対してマージラベリングを実行する

    Args:
        target_cluster_id: 処理対象のクラスタID
        result_df: クラスタリング結果のDataFrame
        current_columns: 現在のレベルのカラム情報
        previous_columns: 前のレベルのカラム情報
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含む辞書
    """

    def filter_previous_values(df: pd.DataFrame, previous_columns: ClusterColumns) -> list[ClusterValues]:
        """前のレベルのクラスタ情報を取得する"""
        previous_records = df[df[current_columns.id] == target_cluster_id][
            [previous_columns.label, previous_columns.description]
        ].drop_duplicates()
        previous_values = [
            ClusterValues(
                label=row[previous_columns.label],
                description=row[previous_columns.description],
            )
            for _, row in previous_records.iterrows()
        ]
        return previous_values

    previous_values = filter_previous_values(result_df, previous_columns)
    if len(previous_values) == 1:
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: previous_values[0].label,
            current_columns.description: previous_values[0].description,
        }
    elif len(previous_values) == 0:
        raise ValueError(f"クラスタ {target_cluster_id} には前のレベルのクラスタが存在しません。")

    current_cluster_data = result_df[result_df[current_columns.id] == target_cluster_id]
    sampling_num = min(
        config["hierarchical_merge_labelling"]["sampling_num"],
        len(current_cluster_data),
    )
    sampled_data = current_cluster_data.sample(sampling_num)
    sampled_argument_text = "\n".join(sampled_data["argument"].values)
    cluster_text = "\n".join([value.to_prompt_text() for value in previous_values])
    messages = [
        {"role": "system", "content": config["hierarchical_merge_labelling"]["prompt"]},
        {
            "role": "user",
            "content": "クラスタラベル\n" + cluster_text + "\n" + "クラスタの意見\n" + sampled_argument_text,
        },
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=config["hierarchical_merge_labelling"]["model"],
            json_schema=LabellingFromat,
            provider=config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )

        config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
        config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
        config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
        print(f"Merge labelling: input={token_input}, output={token_output}, total={token_total} tokens")

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: response_json.get("label", "エラーでラベル名が取得できませんでした"),
            current_columns.description: response_json.get("description", "エラーで解説が取得できませんでした"),
        }
    except Exception as e:
        print(f"エラーが発生しました: {e}")
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: "エラーでラベル名が取得できませんでした",
            current_columns.description: "エラーで解説が取得できませんでした",
        }


def calculate_cluster_density(melted_df: pd.DataFrame, config: dict):
    """クラスタ内の密度計算"""
    hierarchical_cluster_df = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_clusters.csv")

    densities = []
    for level, c_id in zip(melted_df["level"], melted_df["id"], strict=False):
        cluster_embeds = hierarchical_cluster_df[hierarchical_cluster_df[f"cluster-level-{level}-id"] == c_id][
            ["x", "y"]
        ].values
        density = calculate_density(cluster_embeds)
        densities.append(density)

    # 密度のランクを計算
    melted_df["density"] = densities
    melted_df["density_rank"] = melted_df.groupby("level")["density"].rank(ascending=False, method="first")
    melted_df["density_rank_percentile"] = melted_df.groupby("level")["density_rank"].transform(lambda x: x / len(x))
    return melted_df


def calculate_density(embeds: np.ndarray):
    """平均距離に基づいて密度を計算"""
    center = np.mean(embeds, axis=0)
    distances = np.linalg.norm(embeds - center, axis=1)
    avg_distance = np.mean(distances)
    density = 1 / (avg_distance + 1e-10)
    return density
1d:Ta82,"""Create summaries for the clusters."""

import json
import os
import re

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class OverviewResponse(BaseModel):
    summary: str = Field(..., description="クラスターの全体的な要約")


def hierarchical_overview(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_overview.txt"

    hierarchical_label_df = pd.read_csv(f"outputs/{dataset}/hierarchical_merge_labels.csv")

    prompt = config["hierarchical_overview"]["prompt"]
    model = config["hierarchical_overview"]["model"]

    # TODO: level1で固定にしているが、設定で変えられるようにする
    target_level = 1
    target_records = hierarchical_label_df[hierarchical_label_df["level"] == target_level]
    ids = target_records["id"].to_list()
    labels = target_records["label"].to_list()
    descriptions = target_records["description"].to_list()
    target_records.set_index("id", inplace=True)

    input_text = ""
    for i, _ in enumerate(ids):
        input_text += f"# Cluster {i}/{len(ids)}: {labels[i]}\n\n"
        input_text += descriptions[i] + "\n\n"

    messages = [{"role": "system", "content": prompt}, {"role": "user", "content": input_text}]
    response_text, token_input, token_output, token_total = request_to_chat_ai(
        messages=messages,
        model=model,
        provider=config["provider"],
        local_llm_address=config.get("local_llm_address"),
        user_api_key=os.getenv("USER_API_KEY"),
        json_schema=OverviewResponse,
    )

    # トークン使用量を累積
    config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
    config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
    config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
    print(f"Hierarchical overview: input={token_input}, output={token_output}, total={token_total} tokens")

    try:
        # structured outputとしてパースできるなら処理する
        if isinstance(response_text, dict):
            parsed_response = response_text
        else:
            parsed_response = json.loads(response_text)

        with open(path, "w") as file:
            file.write(parsed_response["summary"])

    except Exception:
        # thinkタグが出力されるReasoningモデル用に、thinkタグを除去する
        thinking_removed = re.sub(
            r"<think\b[^>]*>.*?</think>",
            "",
            response_text,
            flags=re.DOTALL,
        )

        with open(path, "w") as file:
            file.write(thinking_removed)
1e:T3f38,"""Generate a convenient JSON output file."""

import json
from collections import defaultdict
from pathlib import Path
from typing import Any, TypedDict

import numpy as np
import pandas as pd

ROOT_DIR = Path(__file__).parent.parent.parent.parent
CONFIG_DIR = ROOT_DIR / "scatter" / "pipeline" / "configs"
PIPELINE_DIR = ROOT_DIR / "broadlistening" / "pipeline"


def json_serialize_numpy(obj: Any) -> Any:
    """
    Recursively convert NumPy data types to native Python types for JSON serialization.

    Args:
        obj: Any Python object which might contain NumPy data types

    Returns:
        The same object structure with NumPy types converted to Python native types
    """
    if isinstance(obj, np.integer):
        return int(obj)
    elif isinstance(obj, np.floating):
        return float(obj)
    elif isinstance(obj, np.ndarray):
        return obj.tolist()
    elif isinstance(obj, dict):
        return {k: json_serialize_numpy(v) for k, v in obj.items()}
    elif isinstance(obj, list):
        return [json_serialize_numpy(item) for item in obj]
    elif isinstance(obj, tuple):
        return tuple(json_serialize_numpy(item) for item in obj)
    else:
        return obj


class Argument(TypedDict):
    arg_id: str
    argument: str
    comment_id: str
    x: float
    y: float
    p: float
    cluster_ids: list[str]
    attributes: dict[str, str] | None
    url: str | None


class Cluster(TypedDict):
    level: int
    id: str
    label: str
    takeaway: str
    value: int
    parent: str
    density_rank_percentile: float | None


def hierarchical_aggregation(config) -> bool:
    try:
        path = f"outputs/{config['output_dir']}/hierarchical_result.json"
        results = {
            "arguments": [],
            "clusters": [],
            "comments": {},
            "propertyMap": {},
            "translations": {},
            "overview": "",
            "config": config,
        }

        arguments = pd.read_csv(f"outputs/{config['output_dir']}/args.csv")
        arguments.set_index("arg-id", inplace=True)
        arg_num = len(arguments)
        relation_df = pd.read_csv(f"outputs/{config['output_dir']}/relations.csv")
        comments = pd.read_csv(f"inputs/{config['input']}.csv")
        clusters = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_clusters.csv")
        labels = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_merge_labels.csv")

        hidden_properties_map: dict[str, list[str]] = config["hierarchical_aggregation"]["hidden_properties"]

        results["arguments"] = _build_arguments(clusters, comments, relation_df, config)
        results["clusters"] = _build_cluster_value(labels, arg_num)

        # results["comments"] = _build_comments_value(
        #     comments, arguments, hidden_properties_map
        # )
        results["comment_num"] = len(comments)
        results["translations"] = _build_translations(config)
        # 属性情報のカラムは、元データに対して指定したカラムとclassificationするカテゴリを合わせたもの
        results["propertyMap"] = _build_property_map(arguments, comments, hidden_properties_map, config)

        with open(f"outputs/{config['output_dir']}/hierarchical_overview.txt") as f:
            overview = f.read()
        print("overview")
        print(overview)
        results["overview"] = overview

        # Convert non-serializable NumPy types to native Python types
        results = json_serialize_numpy(results)

        with open(path, "w") as file:
            json.dump(results, file, indent=2, ensure_ascii=False)
        # TODO: サンプリングロジックを実装したいが、現状は全件抽出
        create_custom_intro(config)
        if config["is_pubcom"]:
            add_original_comments(labels, arguments, relation_df, clusters, config)
        return True
    except Exception as e:
        print("error")
        print(e)
        return False


def create_custom_intro(config):
    dataset = config["output_dir"]
    args_path = PIPELINE_DIR / f"outputs/{dataset}/args.csv"
    comments = pd.read_csv(PIPELINE_DIR / f"inputs/{config['input']}.csv")
    result_path = PIPELINE_DIR / f"outputs/{dataset}/hierarchical_result.json"

    input_count = len(comments)
    args_count = len(pd.read_csv(args_path))
    processed_num = min(input_count, config["extraction"]["limit"])

    print(f"Input count: {input_count}")
    print(f"Args count: {args_count}")

    # LLMプロバイダーとモデル名の判定
    def get_llm_provider_display():
        # configからプロバイダー情報を取得（優先）
        provider = config.get("provider", "openai")
        model = config.get("model", "unknown")

        # プロバイダー名をマッピング
        provider_names = {
            "openai": "OpenAI API",
            "azure": "Azure OpenAI API",
            "openrouter": "OpenRouter API",
            "local": "Local LLM",
        }

        provider_name = provider_names.get(provider, f"{provider} API")
        return f"{provider_name} ({model})"

    llm_provider = get_llm_provider_display()

    base_custom_intro = """{intro}
分析対象となったデータの件数は{processed_num}件で、これらのデータに対して{llm_provider}を用いて{args_count}件の意見（議論）を抽出し、クラスタリングを行った。
"""

    intro = config["intro"]
    custom_intro = base_custom_intro.format(
        intro=intro, processed_num=processed_num, args_count=args_count, llm_provider=llm_provider
    )

    with open(result_path) as f:
        result = json.load(f)
    result["config"]["intro"] = custom_intro
    with open(result_path, "w") as f:
        json.dump(result, f, indent=2, ensure_ascii=False)


def add_original_comments(labels, arguments, relation_df, clusters, config):
    # 大カテゴリ（cluster-level-1）に該当するラベルだけ抽出
    labels_lv1 = labels[labels["level"] == 1][["id", "label"]].rename(
        columns={"id": "cluster-level-1-id", "label": "category_label"}
    )

    # arguments と clusters をマージ（カテゴリ情報付与）
    merged = arguments.merge(clusters[["arg-id", "cluster-level-1-id"]], on="arg-id").merge(
        labels_lv1, on="cluster-level-1-id", how="left"
    )

    # relation_df と結合
    merged = merged.merge(relation_df, on="arg-id", how="left")

    # 元コメント取得
    comments = pd.read_csv(PIPELINE_DIR / f"inputs/{config['input']}.csv")
    comments["comment-id"] = comments["comment-id"].astype(str)
    merged["comment-id"] = merged["comment-id"].astype(str)

    # 元コメント本文などとマージ
    final_df = merged.merge(comments, on="comment-id", how="left")

    # 必要カラムのみ整形
    final_cols = ["comment-id", "comment-body", "arg-id", "argument", "cluster-level-1-id", "category_label"]

    # 基本カラム
    for col in ["x", "y", "source", "url"]:
        if col in comments.columns:
            final_cols.append(col)

    # 属性カラムを追加
    attribute_columns = []
    for col in comments.columns:
        # attributeプレフィックスが付いたカラムを探す
        if col.startswith("attribute_"):
            attribute_columns.append(col)
            final_cols.append(col)

    print(f"属性カラム検出: {attribute_columns}")

    # 必要なカラムだけ選択
    final_df = final_df[final_cols]
    final_df = final_df.rename(
        columns={
            "cluster-level-1-id": "category_id",
            "category_label": "category",
            "arg-id": "arg_id",
            "argument": "argument",
            "comment-body": "original-comment",
        }
    )

    # 保存
    final_df.to_csv(PIPELINE_DIR / f"outputs/{config['output_dir']}/final_result_with_comments.csv", index=False)


def _build_arguments(
    clusters: pd.DataFrame, comments: pd.DataFrame, relation_df: pd.DataFrame, config: dict
) -> list[Argument]:
    """
    Build the arguments list including attribute information from original comments

    Args:
        clusters: DataFrame containing cluster information for each argument
        comments: DataFrame containing original comments with attribute columns
        relation_df: DataFrame relating arguments to original comments
        config: Configuration dictionary containing enable_source_link setting
    """
    cluster_columns = [col for col in clusters.columns if col.startswith("cluster-level-") and "id" in col]

    # Prepare for merging with original comments to get attributes
    comments_copy = comments.copy()
    comments_copy["comment-id"] = comments_copy["comment-id"].astype(str)

    # Get argument to comment mapping
    arg_comment_map = {}
    if "comment-id" in relation_df.columns:
        relation_df["comment-id"] = relation_df["comment-id"].astype(str)
        arg_comment_map = dict(zip(relation_df["arg-id"], relation_df["comment-id"], strict=False))

    # Find attribute columns in comments dataframe
    attribute_columns = [col for col in comments.columns if col.startswith("attribute_")]
    print(f"属性カラム検出: {attribute_columns}")

    arguments: list[Argument] = []
    for _, row in clusters.iterrows():
        cluster_ids = ["0"]
        for cluster_column in cluster_columns:
            cluster_ids.append(str(row[cluster_column]))  # Convert to string to ensure serializable

        # Create base argument
        argument: Argument = {
            "arg_id": str(row["arg-id"]),  # Convert to string to ensure serializable
            "argument": str(row["argument"]),
            "x": float(row["x"]),  # Convert to native float
            "y": float(row["y"]),  # Convert to native float
            "p": 0,  # NOTE: 一旦全部0でいれる
            "cluster_ids": cluster_ids,
            "attributes": None,
            "url": None,
        }

        # Add attributes and URL if available
        if row["arg-id"] in arg_comment_map:
            comment_id = arg_comment_map[row["arg-id"]]
            comment_rows = comments_copy[comments_copy["comment-id"] == comment_id]

            if not comment_rows.empty:
                comment_row = comment_rows.iloc[0]

                # Add URL if available and enabled
                if config.get("enable_source_link", False) and "url" in comment_row and comment_row["url"] is not None:
                    argument["url"] = str(comment_row["url"])

                # Add attributes if available
                if attribute_columns:
                    attributes = {}
                    for attr_col in attribute_columns:
                        # Remove "attribute_" prefix for cleaner attribute names
                        attr_name = attr_col[len("attribute_") :]
                        # Convert potential numpy types to Python native types
                        attr_value = comment_row.get(attr_col, None)
                        if attr_value is not None:
                            if isinstance(attr_value, np.integer):
                                attr_value = int(attr_value)
                            elif isinstance(attr_value, np.floating):
                                attr_value = float(attr_value)
                            elif isinstance(attr_value, np.ndarray):
                                attr_value = attr_value.tolist()
                        attributes[attr_name] = attr_value

                    # Only add non-empty attributes
                    if any(v is not None for v in attributes.values()):
                        argument["attributes"] = attributes

        arguments.append(argument)
    return arguments


def _build_cluster_value(melted_labels: pd.DataFrame, total_num: int) -> list[Cluster]:
    results: list[Cluster] = [
        Cluster(
            level=0,
            id="0",
            label="全体",
            takeaway="",
            value=int(total_num),  # Convert to native int
            parent="",
            density_rank_percentile=0,
        )
    ]

    for _, melted_label in melted_labels.iterrows():
        # Convert potential NumPy types to native Python types
        level = (
            int(melted_label["level"]) if isinstance(melted_label["level"], int | np.integer) else melted_label["level"]
        )
        cluster_id = str(melted_label["id"])
        label = str(melted_label["label"])
        takeaway = str(melted_label["description"])
        value = (
            int(melted_label["value"]) if isinstance(melted_label["value"], int | np.integer) else melted_label["value"]
        )
        parent = str(melted_label.get("parent", "全体"))

        # Handle density_rank_percentile which might be None or a numeric value
        density_rank = melted_label.get("density_rank_percentile")
        if density_rank is not None:
            if isinstance(density_rank, float | np.floating):
                density_rank = float(density_rank)
            elif isinstance(density_rank, int | np.integer):
                density_rank = int(density_rank)

        cluster_value = Cluster(
            level=level,
            id=cluster_id,
            label=label,
            takeaway=takeaway,
            value=value,
            parent=parent,
            density_rank_percentile=density_rank,
        )
        results.append(cluster_value)
    return results


def _build_comments_value(
    comments: pd.DataFrame,
    arguments: pd.DataFrame,
    hidden_properties_map: dict[str, list[str]],
):
    comment_dict: dict[str, dict[str, str]] = {}
    useful_comment_ids = set(arguments["comment-id"].values)
    for _, row in comments.iterrows():
        id = row["comment-id"]
        if id in useful_comment_ids:
            res = {"comment": row["comment-body"]}
            should_skip = any(row[prop] in hidden_values for prop, hidden_values in hidden_properties_map.items())
            if should_skip:
                continue
            comment_dict[str(id)] = res

    return comment_dict


def _build_translations(config):
    languages = list(config.get("translation", {}).get("languages", []))
    if len(languages) > 0:
        with open(PIPELINE_DIR / f"outputs/{config['output_dir']}/translations.json") as f:
            translations = f.read()
        return json.loads(translations)
    return {}


def _build_property_map(
    arguments: pd.DataFrame, comments: pd.DataFrame, hidden_properties_map: dict[str, list[str]], config: dict
) -> dict[str, dict[str, str]]:
    property_columns = list(hidden_properties_map.keys()) + list(config["extraction"]["categories"].keys())
    property_map = defaultdict(dict)

    # 指定された property_columns が arguments に存在するかチェック
    missing_cols = [col for col in property_columns if col not in arguments.columns]
    if missing_cols:
        raise ValueError(
            f"指定されたカラム {missing_cols} が args.csv に存在しません。"
            "設定ファイルaggregation / hidden_propertiesから該当カラムを取り除いてください。"
        )

    for prop in property_columns:
        for arg_id, row in arguments.iterrows():
            # LLMによるcategory classificationがうまく行かず、NaNの場合はNoneにする
            value = row[prop] if not pd.isna(row[prop]) else None

            # Convert NumPy types to Python native types
            if value is not None:
                if isinstance(value, np.integer):
                    value = int(value)
                elif isinstance(value, np.floating):
                    value = float(value)
                elif isinstance(value, np.ndarray):
                    value = value.tolist()
                else:
                    # Convert any other types to string to ensure serialization
                    try:
                        value = str(value)
                    except Exception as e:
                        print(f"Error converting value to string: {e}")
                        value = None

            # Make sure arg_id is string
            str_arg_id = str(arg_id)
            property_map[prop][str_arg_id] = value

    return property_map
1f:T458,import os

import pandas as pd
from tqdm import tqdm

from services.llm import request_to_embed


def embedding(config):
    model = config["embedding"]["model"]
    is_embedded_at_local = config["is_embedded_at_local"]
    # print("start embedding")
    # print(f"embedding model: {model}, is_embedded_at_local: {is_embedded_at_local}")

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/embeddings.pkl"
    arguments = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings = []
    batch_size = 1000
    for i in tqdm(range(0, len(arguments), batch_size)):
        args = arguments["argument"].tolist()[i : i + batch_size]
        embeds = request_to_embed(
            args,
            model,
            is_embedded_at_local,
            config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )
        embeddings.extend(embeds)
    df = pd.DataFrame([{"arg-id": arguments.iloc[i]["arg-id"], "embedding": e} for i, e in enumerate(embeddings)])
    df.to_pickle(path)
20:T194c,import concurrent.futures
import json
import logging
import os
import re

import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai
from services.parse_json_list import parse_extraction_response
from utils import update_progress

COMMA_AND_SPACE_AND_RIGHT_BRACKET = re.compile(r",\s*(\])")


class ExtractionResponse(BaseModel):
    extractedOpinionList: list[str] = Field(..., description="抽出した意見のリスト")


def _validate_property_columns(property_columns: list[str], comments: pd.DataFrame) -> None:
    if not all(property in comments.columns for property in property_columns):
        raise ValueError(f"Properties {property_columns} not found in comments. Columns are {comments.columns}")


def extraction(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/args.csv"
    model = config["extraction"]["model"]
    prompt = config["extraction"]["prompt"]
    workers = config["extraction"]["workers"]
    limit = config["extraction"]["limit"]
    property_columns = config["extraction"]["properties"]

    if "provider" not in config:
        raise RuntimeError("provider is not set")
    provider = config["provider"]

    # カラム名だけを読み込み、必要なカラムが含まれているか確認する
    comments = pd.read_csv(f"inputs/{config['input']}.csv", nrows=0)
    _validate_property_columns(property_columns, comments)
    # エラーが出なかった場合、すべての行を読み込む
    comments = pd.read_csv(
        f"inputs/{config['input']}.csv", usecols=["comment-id", "comment-body"] + config["extraction"]["properties"]
    )
    comment_ids = (comments["comment-id"].values)[:limit]
    comments.set_index("comment-id", inplace=True)
    results = pd.DataFrame()
    update_progress(config, total=len(comment_ids))

    argument_map = {}
    relation_rows = []

    for i in tqdm(range(0, len(comment_ids), workers)):
        batch = comment_ids[i : i + workers]
        batch_inputs = [comments.loc[id]["comment-body"] for id in batch]
        batch_results = extract_batch(
            batch_inputs, prompt, model, workers, provider, config.get("local_llm_address"), config
        )

        for comment_id, extracted_args in zip(batch, batch_results, strict=False):
            for j, arg in enumerate(extracted_args):
                if arg not in argument_map:
                    # argumentテーブルに追加
                    arg_id = f"A{comment_id}_{j}"
                    argument = arg
                    argument_map[arg] = {
                        "arg-id": arg_id,
                        "argument": argument,
                    }
                else:
                    arg_id = argument_map[arg]["arg-id"]

                # relationテーブルにcommentとargの関係を追加
                relation_row = {
                    "arg-id": arg_id,
                    "comment-id": comment_id,
                }
                relation_rows.append(relation_row)

        update_progress(config, incr=len(batch))

    # DataFrame化
    results = pd.DataFrame(argument_map.values())
    relation_df = pd.DataFrame(relation_rows)

    if results.empty:
        raise RuntimeError("result is empty, maybe bad prompt")

    results.to_csv(path, index=False)
    # comment-idとarg-idの関係を保存
    relation_df.to_csv(f"outputs/{dataset}/relations.csv", index=False)


logging.basicConfig(level=logging.DEBUG)


def extract_batch(batch, prompt, model, workers, provider="openai", local_llm_address=None, config=None):
    with concurrent.futures.ThreadPoolExecutor(max_workers=workers) as executor:
        futures_with_index = [
            (i, executor.submit(extract_arguments, input, prompt, model, provider, local_llm_address))
            for i, input in enumerate(batch)
        ]

        done, not_done = concurrent.futures.wait([f for _, f in futures_with_index], timeout=30)
        results = [[] for _ in range(len(batch))]
        total_token_input = 0
        total_token_output = 0
        total_token_usage = 0

        for _, future in futures_with_index:
            if future in not_done and not future.cancelled():
                future.cancel()

        for i, future in futures_with_index:
            if future in done:
                try:
                    result = future.result()
                    if isinstance(result, tuple) and len(result) == 4:
                        items, token_input, token_output, token_total = result
                        results[i] = items
                        total_token_input += token_input
                        total_token_output += token_output
                        total_token_usage += token_total
                    else:
                        results[i] = result
                except Exception as e:
                    logging.error(f"Task {future} failed with error: {e}")
                    results[i] = []

        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + total_token_usage
            config["token_usage_input"] = config.get("token_usage_input", 0) + total_token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + total_token_output
            print(
                f"Extraction batch: input={total_token_input}, output={total_token_output}, total={total_token_usage} tokens"
            )

        return results


def extract_arguments(input, prompt, model, provider="openai", local_llm_address=None):
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            is_json=False,
            json_schema=ExtractionResponse,
            provider=provider,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )
        items = parse_extraction_response(response)
        items = list(filter(None, items))  # omit empty strings
        return items, token_input, token_output, token_total
    except json.decoder.JSONDecodeError as e:
        print("JSON error:", e)
        print("Input was:", input)
        print("Response was:", response)
        print("Silently giving up on trying to generate valid list.")
        return []
21:T458,import os

import pandas as pd
from tqdm import tqdm

from services.llm import request_to_embed


def embedding(config):
    model = config["embedding"]["model"]
    is_embedded_at_local = config["is_embedded_at_local"]
    # print("start embedding")
    # print(f"embedding model: {model}, is_embedded_at_local: {is_embedded_at_local}")

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/embeddings.pkl"
    arguments = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings = []
    batch_size = 1000
    for i in tqdm(range(0, len(arguments), batch_size)):
        args = arguments["argument"].tolist()[i : i + batch_size]
        embeds = request_to_embed(
            args,
            model,
            is_embedded_at_local,
            config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )
        embeddings.extend(embeds)
    df = pd.DataFrame([{"arg-id": arguments.iloc[i]["arg-id"], "embedding": e} for i, e in enumerate(embeddings)])
    df.to_pickle(path)
22:T1149,"""Cluster the arguments using UMAP + HDBSCAN and GPT-4."""

from importlib import import_module

import numpy as np
import pandas as pd
import scipy.cluster.hierarchy as sch
from sklearn.cluster import KMeans


def hierarchical_clustering(config):
    UMAP = import_module("umap").UMAP

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_clusters.csv"
    arguments_df = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings_df = pd.read_pickle(f"outputs/{dataset}/embeddings.pkl")
    embeddings_array = np.asarray(embeddings_df["embedding"].values.tolist())
    cluster_nums = config["hierarchical_clustering"]["cluster_nums"]

    n_samples = embeddings_array.shape[0]
    # デフォルト設定は15
    default_n_neighbors = 15

    # テスト等サンプルが少なすぎる場合、n_neighborsの設定値を下げる
    if n_samples <= default_n_neighbors:
        n_neighbors = max(2, n_samples - 1)  # 最低2以上
    else:
        n_neighbors = default_n_neighbors

    umap_model = UMAP(random_state=42, n_components=2, n_neighbors=n_neighbors)
    # TODO 詳細エラーメッセージを加える
    # 以下のエラーの場合、おそらく元の意見件数が少なすぎることが原因
    # TypeError: Cannot use scipy.linalg.eigh for sparse A with k >= N. Use scipy.linalg.eigh(A.toarray()) or reduce k.
    umap_embeds = umap_model.fit_transform(embeddings_array)

    cluster_results = hierarchical_clustering_embeddings(
        umap_embeds=umap_embeds,
        cluster_nums=cluster_nums,
    )
    result_df = pd.DataFrame(
        {
            "arg-id": arguments_df["arg-id"],
            "argument": arguments_df["argument"],
            "x": umap_embeds[:, 0],
            "y": umap_embeds[:, 1],
        }
    )

    for cluster_level, final_labels in enumerate(cluster_results.values(), start=1):
        result_df[f"cluster-level-{cluster_level}-id"] = [f"{cluster_level}_{label}" for label in final_labels]

    result_df.to_csv(path, index=False)


def generate_cluster_count_list(min_clusters: int, max_clusters: int):
    cluster_counts = []
    current = min_clusters
    cluster_counts.append(current)

    if min_clusters == max_clusters:
        return cluster_counts

    while True:
        next_double = current * 2
        next_triple = current * 3

        if next_double >= max_clusters:
            if cluster_counts[-1] != max_clusters:
                cluster_counts.append(max_clusters)
            break

        # 次の倍はまだ max_clusters に収まるが、3倍だと超える
        # -> (次の倍は細かすぎるので)スキップして max_clusters に飛ぶ
        if next_triple > max_clusters:
            cluster_counts.append(max_clusters)
            break

        cluster_counts.append(next_double)
        current = next_double

    return cluster_counts


def merge_clusters_with_hierarchy(
    cluster_centers: np.ndarray,
    kmeans_labels: np.ndarray,
    umap_array: np.ndarray,
    n_cluster_cut: int,
):
    Z = sch.linkage(cluster_centers, method="ward")
    cluster_labels_merged = sch.fcluster(Z, t=n_cluster_cut, criterion="maxclust")

    n_samples = umap_array.shape[0]
    final_labels = np.zeros(n_samples, dtype=int)

    for i in range(n_samples):
        original_label = kmeans_labels[i]
        final_labels[i] = cluster_labels_merged[original_label]

    return final_labels


def hierarchical_clustering_embeddings(
    umap_embeds,
    cluster_nums,
):
    # 最大分割数でクラスタリングを実施
    print("start initial clustering")
    initial_cluster_num = cluster_nums[-1]
    kmeans_model = KMeans(n_clusters=initial_cluster_num, random_state=42)
    kmeans_model.fit(umap_embeds)
    print("end initial clustering")

    results = {}
    print("start hierarchical clustering")
    cluster_nums.sort()
    print(cluster_nums)
    for n_cluster_cut in cluster_nums[:-1]:
        print("n_cluster_cut: ", n_cluster_cut)
        final_labels = merge_clusters_with_hierarchy(
            cluster_centers=kmeans_model.cluster_centers_,
            kmeans_labels=kmeans_model.labels_,
            umap_array=umap_embeds,
            n_cluster_cut=n_cluster_cut,
        )
        results[n_cluster_cut] = final_labels

    results[initial_cluster_num] = kmeans_model.labels_
    print("end hierarchical clustering")

    return results
23:T1c36,import json
import os
from concurrent.futures import ThreadPoolExecutor
from functools import partial
from typing import TypedDict

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class LabellingResult(TypedDict):
    """各クラスタのラベリング結果を表す型"""

    cluster_id: str  # クラスタのID
    label: str  # クラスタのラベル名
    description: str  # クラスタの説明文


def hierarchical_initial_labelling(config: dict) -> None:
    """階層的クラスタリングの初期ラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_initial_labelling: 初期ラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_initial_labels.csv"
    clusters_argument_df = pd.read_csv(f"outputs/{dataset}/hierarchical_clusters.csv")

    cluster_id_columns = [col for col in clusters_argument_df.columns if col.startswith("cluster-level-")]
    initial_cluster_id_column = cluster_id_columns[-1]
    sampling_num = config["hierarchical_initial_labelling"]["sampling_num"]
    initial_labelling_prompt = config["hierarchical_initial_labelling"]["prompt"]
    model = config["hierarchical_initial_labelling"]["model"]
    workers = config["hierarchical_initial_labelling"]["workers"]

    # トークン使用量を追跡するための変数を初期化
    config["total_token_usage"] = config.get("total_token_usage", 0)

    initial_label_df = initial_labelling(
        initial_labelling_prompt,
        clusters_argument_df,
        sampling_num,
        model,
        workers,
        config["provider"],
        config.get("local_llm_address"),
        config,  # configを渡して、トークン使用量を累積できるようにする
    )
    print("start initial labelling")
    initial_clusters_argument_df = clusters_argument_df.merge(
        initial_label_df,
        left_on=initial_cluster_id_column,
        right_on="cluster_id",
        how="left",
    ).rename(
        columns={
            "label": f"{initial_cluster_id_column.replace('-id', '')}-label",
            "description": f"{initial_cluster_id_column.replace('-id', '')}-description",
        }
    )
    print("end initial labelling")
    initial_clusters_argument_df.to_csv(path, index=False)


def initial_labelling(
    prompt: str,
    clusters_df: pd.DataFrame,
    sampling_num: int,
    model: str,
    workers: int,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> pd.DataFrame:
    """各クラスタに対して初期ラベリングを実行する

    Args:
        prompt: LLMへのプロンプト
        clusters_df: クラスタリング結果のDataFrame
        sampling_num: 各クラスタからサンプリングする意見の数
        model: 使用するLLMモデル名
        workers: 並列処理のワーカー数
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        各クラスタのラベリング結果を含むDataFrame
    """
    cluster_columns = [col for col in clusters_df.columns if col.startswith("cluster-level-")]
    initial_cluster_column = cluster_columns[-1]
    cluster_ids = clusters_df[initial_cluster_column].unique()
    process_func = partial(
        process_initial_labelling,
        df=clusters_df,
        prompt=prompt,
        sampling_num=sampling_num,
        target_column=initial_cluster_column,
        model=model,
        provider=provider,
        local_llm_address=local_llm_address,
        config=config,  # configを渡す
    )
    with ThreadPoolExecutor(max_workers=workers) as executor:
        results = list(executor.map(process_func, cluster_ids))
    return pd.DataFrame(results)


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_initial_labelling(
    cluster_id: str,
    df: pd.DataFrame,
    prompt: str,
    sampling_num: int,
    target_column: str,
    model: str,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> LabellingResult:
    """個別のクラスタに対してラベリングを実行する

    Args:
        cluster_id: 処理対象のクラスタID
        df: クラスタリング結果のDataFrame
        prompt: LLMへのプロンプト
        sampling_num: サンプリングする意見の数
        target_column: クラスタIDが格納されている列名
        model: 使用するLLMモデル名
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        クラスタのラベリング結果
    """
    cluster_data = df[df[target_column] == cluster_id]
    sampling_num = min(sampling_num, len(cluster_data))
    cluster = cluster_data.sample(sampling_num)
    input = "\n".join(cluster["argument"].values)
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            provider=provider,
            json_schema=LabellingFromat,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )

        # トークン使用量を累積（configが渡されている場合）
        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
            config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + token_output

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return LabellingResult(
            cluster_id=cluster_id,
            label=response_json.get("label", "エラーでラベル名が取得できませんでした"),
            description=response_json.get("description", "エラーで解説が取得できませんでした"),
        )
    except Exception as e:
        print(e)
        return LabellingResult(
            cluster_id=cluster_id,
            label="エラーでラベル名が取得できませんでした",
            description="エラーで解説が取得できませんでした",
        )
24:T337b,import json
import os
from concurrent.futures import ThreadPoolExecutor
from dataclasses import dataclass
from functools import partial

import numpy as np
import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai


@dataclass
class ClusterColumns:
    """同一階層のクラスター関連のカラム名を管理するクラス"""

    id: str
    label: str
    description: str

    @classmethod
    def from_id_column(cls, id_column: str) -> "ClusterColumns":
        """ID列名から関連するカラム名を生成"""
        return cls(
            id=id_column,
            label=id_column.replace("-id", "-label"),
            description=id_column.replace("-id", "-description"),
        )


@dataclass
class ClusterValues:
    """対象クラスタのlabel/descriptionを管理するクラス"""

    label: str
    description: str

    def to_prompt_text(self) -> str:
        return f"- {self.label}: {self.description}"


def hierarchical_merge_labelling(config: dict) -> None:
    """階層的クラスタリングの結果に対してマージラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_merge_labelling: マージラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    merge_path = f"outputs/{dataset}/hierarchical_merge_labels.csv"
    clusters_df = pd.read_csv(f"outputs/{dataset}/hierarchical_initial_labels.csv")

    cluster_id_columns: list[str] = _filter_id_columns(clusters_df.columns)
    # ボトムクラスタのラベル・説明とクラスタid付きの各argumentを入力し、各階層のクラスタラベル・説明を生成し、argumentに付けたdfを作成
    merge_result_df = merge_labelling(
        clusters_df=clusters_df,
        cluster_id_columns=sorted(cluster_id_columns, reverse=True),
        config=config,
    )
    # 上記のdfから各クラスタのlevel, id, label, description, valueを取得してdfを作成
    melted_df = melt_cluster_data(merge_result_df)
    # 上記のdfに親子関係を追加
    parent_child_df = _build_parent_child_mapping(merge_result_df, cluster_id_columns)
    melted_df = melted_df.merge(parent_child_df, on=["level", "id"], how="left")
    density_df = calculate_cluster_density(melted_df, config)
    density_df.to_csv(merge_path, index=False)


def _build_parent_child_mapping(df: pd.DataFrame, cluster_id_columns: list[str]):
    """クラスタ間の親子関係をマッピングする

    Args:
        df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト

    Returns:
        親子関係のマッピング情報を含むDataFrame
    """
    results = []
    top_cluster_column = cluster_id_columns[0]
    top_cluster_values = df[top_cluster_column].unique()
    for c in top_cluster_values:
        results.append(
            {
                "level": 1,
                "id": c,
                "parent": "0",  # aggregationで追加する全体クラスタのid
            }
        )

    for idx in range(len(cluster_id_columns) - 1):
        current_column = cluster_id_columns[idx]
        children_column = cluster_id_columns[idx + 1]
        current_level = current_column.replace("-id", "").replace("cluster-level-", "")
        # 現在のレベルのクラスタid
        current_cluster_values = df[current_column].unique()
        for current_id in current_cluster_values:
            children_ids = df.loc[df[current_column] == current_id, children_column].unique()
            for child_id in children_ids:
                results.append(
                    {
                        "level": int(current_level) + 1,
                        "id": child_id,
                        "parent": current_id,
                    }
                )
    return pd.DataFrame(results)


def _filter_id_columns(columns: list[str]) -> list[str]:
    """クラスタIDのカラム名をフィルタリングする

    Args:
        columns: 全カラム名のリスト

    Returns:
        クラスタIDのカラム名のリスト
    """
    return [col for col in columns if col.startswith("cluster-level-") and col.endswith("-id")]


def melt_cluster_data(df: pd.DataFrame) -> pd.DataFrame:
    """クラスタデータを行形式に変換する

    cluster-level-n-(id|label|description) を行形式 (level, id, label, description, value) にまとめる。
    [cluster-level-n-id, cluster-level-n-label, cluster-level-n-description] を [level, id, label, description, value(件数)] に変換する。

    Args:
        df: クラスタリング結果のDataFrame

    Returns:
        行形式に変換されたDataFrame
    """
    id_columns: list[str] = _filter_id_columns(df.columns)
    levels: set[int] = {int(col.replace("cluster-level-", "").replace("-id", "")) for col in id_columns}
    all_rows: list[dict] = []

    # levelごとに各クラスタの出現件数を集計・縦持ちにする
    for level in levels:
        cluster_columns = ClusterColumns.from_id_column(f"cluster-level-{level}-id")
        # クラスタidごとの件数集計
        level_count_df = df.groupby(cluster_columns.id).size().reset_index(name="value")

        level_unique_val_df = df[
            [cluster_columns.id, cluster_columns.label, cluster_columns.description]
        ].drop_duplicates()
        level_unique_val_df = level_unique_val_df.merge(level_count_df, on=cluster_columns.id, how="left")
        level_unique_vals = [
            {
                "level": level,
                "id": row[cluster_columns.id],
                "label": row[cluster_columns.label],
                "description": row[cluster_columns.description],
                "value": row["value"],
            }
            for _, row in level_unique_val_df.iterrows()
        ]
        all_rows.extend(level_unique_vals)
    return pd.DataFrame(all_rows)


def merge_labelling(clusters_df: pd.DataFrame, cluster_id_columns: list[str], config) -> pd.DataFrame:
    """階層的なクラスタのマージラベリングを実行する

    Args:
        clusters_df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含むDataFrame
    """
    for idx in tqdm(range(len(cluster_id_columns) - 1)):
        previous_columns = ClusterColumns.from_id_column(cluster_id_columns[idx])
        current_columns = ClusterColumns.from_id_column(cluster_id_columns[idx + 1])

        process_fn = partial(
            process_merge_labelling,
            result_df=clusters_df,
            current_columns=current_columns,
            previous_columns=previous_columns,
            config=config,
        )

        current_cluster_ids = sorted(clusters_df[current_columns.id].unique())
        with ThreadPoolExecutor(max_workers=config["hierarchical_merge_labelling"]["workers"]) as executor:
            responses = list(
                tqdm(
                    executor.map(process_fn, current_cluster_ids),
                    total=len(current_cluster_ids),
                )
            )

        current_result_df = pd.DataFrame(responses)
        clusters_df = clusters_df.merge(current_result_df, on=[current_columns.id])
    return clusters_df


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_merge_labelling(
    target_cluster_id: str,
    result_df: pd.DataFrame,
    current_columns: ClusterColumns,
    previous_columns: ClusterColumns,
    config,
):
    """個別のクラスタに対してマージラベリングを実行する

    Args:
        target_cluster_id: 処理対象のクラスタID
        result_df: クラスタリング結果のDataFrame
        current_columns: 現在のレベルのカラム情報
        previous_columns: 前のレベルのカラム情報
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含む辞書
    """

    def filter_previous_values(df: pd.DataFrame, previous_columns: ClusterColumns) -> list[ClusterValues]:
        """前のレベルのクラスタ情報を取得する"""
        previous_records = df[df[current_columns.id] == target_cluster_id][
            [previous_columns.label, previous_columns.description]
        ].drop_duplicates()
        previous_values = [
            ClusterValues(
                label=row[previous_columns.label],
                description=row[previous_columns.description],
            )
            for _, row in previous_records.iterrows()
        ]
        return previous_values

    previous_values = filter_previous_values(result_df, previous_columns)
    if len(previous_values) == 1:
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: previous_values[0].label,
            current_columns.description: previous_values[0].description,
        }
    elif len(previous_values) == 0:
        raise ValueError(f"クラスタ {target_cluster_id} には前のレベルのクラスタが存在しません。")

    current_cluster_data = result_df[result_df[current_columns.id] == target_cluster_id]
    sampling_num = min(
        config["hierarchical_merge_labelling"]["sampling_num"],
        len(current_cluster_data),
    )
    sampled_data = current_cluster_data.sample(sampling_num)
    sampled_argument_text = "\n".join(sampled_data["argument"].values)
    cluster_text = "\n".join([value.to_prompt_text() for value in previous_values])
    messages = [
        {"role": "system", "content": config["hierarchical_merge_labelling"]["prompt"]},
        {
            "role": "user",
            "content": "クラスタラベル\n" + cluster_text + "\n" + "クラスタの意見\n" + sampled_argument_text,
        },
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=config["hierarchical_merge_labelling"]["model"],
            json_schema=LabellingFromat,
            provider=config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )

        config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
        config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
        config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
        print(f"Merge labelling: input={token_input}, output={token_output}, total={token_total} tokens")

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: response_json.get("label", "エラーでラベル名が取得できませんでした"),
            current_columns.description: response_json.get("description", "エラーで解説が取得できませんでした"),
        }
    except Exception as e:
        print(f"エラーが発生しました: {e}")
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: "エラーでラベル名が取得できませんでした",
            current_columns.description: "エラーで解説が取得できませんでした",
        }


def calculate_cluster_density(melted_df: pd.DataFrame, config: dict):
    """クラスタ内の密度計算"""
    hierarchical_cluster_df = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_clusters.csv")

    densities = []
    for level, c_id in zip(melted_df["level"], melted_df["id"], strict=False):
        cluster_embeds = hierarchical_cluster_df[hierarchical_cluster_df[f"cluster-level-{level}-id"] == c_id][
            ["x", "y"]
        ].values
        density = calculate_density(cluster_embeds)
        densities.append(density)

    # 密度のランクを計算
    melted_df["density"] = densities
    melted_df["density_rank"] = melted_df.groupby("level")["density"].rank(ascending=False, method="first")
    melted_df["density_rank_percentile"] = melted_df.groupby("level")["density_rank"].transform(lambda x: x / len(x))
    return melted_df


def calculate_density(embeds: np.ndarray):
    """平均距離に基づいて密度を計算"""
    center = np.mean(embeds, axis=0)
    distances = np.linalg.norm(embeds - center, axis=1)
    avg_distance = np.mean(distances)
    density = 1 / (avg_distance + 1e-10)
    return density
25:Ta82,"""Create summaries for the clusters."""

import json
import os
import re

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class OverviewResponse(BaseModel):
    summary: str = Field(..., description="クラスターの全体的な要約")


def hierarchical_overview(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_overview.txt"

    hierarchical_label_df = pd.read_csv(f"outputs/{dataset}/hierarchical_merge_labels.csv")

    prompt = config["hierarchical_overview"]["prompt"]
    model = config["hierarchical_overview"]["model"]

    # TODO: level1で固定にしているが、設定で変えられるようにする
    target_level = 1
    target_records = hierarchical_label_df[hierarchical_label_df["level"] == target_level]
    ids = target_records["id"].to_list()
    labels = target_records["label"].to_list()
    descriptions = target_records["description"].to_list()
    target_records.set_index("id", inplace=True)

    input_text = ""
    for i, _ in enumerate(ids):
        input_text += f"# Cluster {i}/{len(ids)}: {labels[i]}\n\n"
        input_text += descriptions[i] + "\n\n"

    messages = [{"role": "system", "content": prompt}, {"role": "user", "content": input_text}]
    response_text, token_input, token_output, token_total = request_to_chat_ai(
        messages=messages,
        model=model,
        provider=config["provider"],
        local_llm_address=config.get("local_llm_address"),
        user_api_key=os.getenv("USER_API_KEY"),
        json_schema=OverviewResponse,
    )

    # トークン使用量を累積
    config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
    config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
    config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
    print(f"Hierarchical overview: input={token_input}, output={token_output}, total={token_total} tokens")

    try:
        # structured outputとしてパースできるなら処理する
        if isinstance(response_text, dict):
            parsed_response = response_text
        else:
            parsed_response = json.loads(response_text)

        with open(path, "w") as file:
            file.write(parsed_response["summary"])

    except Exception:
        # thinkタグが出力されるReasoningモデル用に、thinkタグを除去する
        thinking_removed = re.sub(
            r"<think\b[^>]*>.*?</think>",
            "",
            response_text,
            flags=re.DOTALL,
        )

        with open(path, "w") as file:
            file.write(thinking_removed)
8:[["$","$L13",null,{}],["$","$L14",null,{"className":"container","mt":"8","children":[["$","$L14",null,{"mx":"auto","maxW":"750px","mb":8,"children":[["$","$L15",null,{"textAlign":"left","fontSize":"xl","mb":5,"children":"レポート"}],["$","$L15",null,{"as":"h2","size":"4xl","mb":2,"className":"headingColor","children":"③市民として、2040年のありたい姿とは【1/31 15:00更新】"}],["$","$L16",null,{"fontWeight":"bold","fontSize":"xl","mb":2,"children":[["$","$L17",null,{"mr":1,"children":["$","svg",null,{"ref":"$undefined","xmlns":"http://www.w3.org/2000/svg","width":20,"height":20,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-messages-square","children":[["$","path","p1xzt8",{"d":"M14 9a2 2 0 0 1-2 2H6l-4 4V4a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2z"}],["$","path","1cx29u",{"d":"M18 9h2a2 2 0 0 1 2 2v11l-4-4h-6a2 2 0 0 1-2-2v-1"}],"$undefined"]}]}],"154","件"]}],["$","p",null,{"children":"舞鶴市の地域振興に関する意見は、住民の幸福や地域の魅力向上を重視し、世代を超えた共生や持続可能な社会の構築が求められています。相互支援やポジティブな人間関係の形成、快適な生活環境の整備が強調され、特に子どもや高齢者に配慮した施策が重要視されています。また、共感や楽しさを重視し、自立した多様な生き方を支える社会の実現が期待されています。"}]]}],["$","$L18",null,{"result":{"arguments":[{"arg_id":"Acsv-1_0","argument":"病気になっても舞鶴で暮らせてよかったと思いたい。","x":9.902403,"y":9.250183,"p":0,"cluster_ids":["0","1_1","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-2_0","argument":"子どもが成人してもずっと舞鶴で働くことができる環境が必要である。","x":9.819486,"y":9.616017,"p":0,"cluster_ids":["0","1_1","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-2_1","argument":"子どもを育てやすい環境が整っていることが重要である。","x":8.693138,"y":11.696057,"p":0,"cluster_ids":["0","1_8","2_6"],"attributes":null,"url":null},{"arg_id":"Acsv-3_0","argument":"舞鶴で育った子どもたちが大人になっても住みたいと思える街にするべき","x":9.868599,"y":9.50089,"p":0,"cluster_ids":["0","1_1","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-3_1","argument":"舞鶴で育った子どもたちが戻ってきたいと思える街にするべき","x":9.661818,"y":9.347162,"p":0,"cluster_ids":["0","1_1","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-4_0","argument":"「和をもって貴しとなす」を実現できる町を目指すべき","x":7.401255,"y":9.51268,"p":0,"cluster_ids":["0","1_2","2_60"],"attributes":null,"url":null},{"arg_id":"Acsv-6_0","argument":"健康第一。","x":7.1047025,"y":12.095437,"p":0,"cluster_ids":["0","1_8","2_56"],"attributes":null,"url":null},{"arg_id":"Acsv-9_0","argument":"自己肯定感の高い人が多い","x":5.2107553,"y":10.81102,"p":0,"cluster_ids":["0","1_6","2_25"],"attributes":null,"url":null},{"arg_id":"Acsv-11_0","argument":"各施設に洋式トイレの設置を希望する","x":8.498279,"y":10.122576,"p":0,"cluster_ids":["0","1_3","2_39"],"attributes":null,"url":null},{"arg_id":"Acsv-11_1","argument":"高齢者と障害者に優しい街であって欲しい","x":8.28017,"y":10.61829,"p":0,"cluster_ids":["0","1_3","2_10"],"attributes":null,"url":null},{"arg_id":"Acsv-12_0","argument":"お互いを尊重して縦と横のつながりを大切にしたい。","x":4.747739,"y":10.571033,"p":0,"cluster_ids":["0","1_6","2_13"],"attributes":null,"url":null},{"arg_id":"Acsv-13_0","argument":"都会に出ても戻りたいと思える過ごしやすい便利な田舎が必要である。","x":7.653494,"y":9.580119,"p":0,"cluster_ids":["0","1_2","2_2"],"attributes":null,"url":null},{"arg_id":"Acsv-13_1","argument":"過疎化の地域を置き去りにせず、今の状態より廃れず継続または発展していくまちを目指すべきである。","x":7.4751563,"y":9.532014,"p":0,"cluster_ids":["0","1_2","2_60"],"attributes":null,"url":null},{"arg_id":"Acsv-14_0","argument":"地域活動の場に多様な人材が関わり合うべきである。","x":5.29116,"y":11.174469,"p":0,"cluster_ids":["0","1_6","2_59"],"attributes":null,"url":null},{"arg_id":"Acsv-15_0","argument":"古き良き時代を生かすべき","x":7.774972,"y":12.561359,"p":0,"cluster_ids":["0","1_8","2_12"],"attributes":null,"url":null},{"arg_id":"Acsv-15_1","argument":"新しいことを学べる素直さを持ちたい","x":5.840003,"y":10.725986,"p":0,"cluster_ids":["0","1_4","2_28"],"attributes":null,"url":null},{"arg_id":"Acsv-16_0","argument":"美しい自然と共存しながら安心安全で自立した暮らしを実現すべき","x":6.967886,"y":12.356611,"p":0,"cluster_ids":["0","1_8","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-16_1","argument":"なりたい自分やスキルアップをあきらめなくて済む社会を目指すべき","x":6.059482,"y":10.60372,"p":0,"cluster_ids":["0","1_4","2_28"],"attributes":null,"url":null},{"arg_id":"Acsv-18_0","argument":"技術が進歩してもその進歩についていけない人がいない世の中が望ましい。","x":6.269613,"y":10.850852,"p":0,"cluster_ids":["0","1_4","2_9"],"attributes":null,"url":null},{"arg_id":"Acsv-19_0","argument":"想像できない","x":5.7665377,"y":12.970581,"p":0,"cluster_ids":["0","1_7","2_14"],"attributes":null,"url":null},{"arg_id":"Acsv-20_0","argument":"争いの少ない世の中を目指すべき","x":6.055803,"y":9.875848,"p":0,"cluster_ids":["0","1_4","2_22"],"attributes":null,"url":null},{"arg_id":"Acsv-20_1","argument":"犯罪率の少ない社会を実現する必要がある","x":5.677333,"y":10.037713,"p":0,"cluster_ids":["0","1_4","2_17"],"attributes":null,"url":null},{"arg_id":"Acsv-23_0","argument":"お金を理由に子どもの進路を制限しない町であるべき","x":8.9168825,"y":10.711284,"p":0,"cluster_ids":["0","1_3","2_21"],"attributes":null,"url":null},{"arg_id":"Acsv-24_0","argument":"高齢化社会では、何歳になっても仕事があるのが理想であり、仕事をしていなくても誰かの役に立つことをしたい。","x":8.609455,"y":12.6552305,"p":0,"cluster_ids":["0","1_8","2_1"],"attributes":null,"url":null},{"arg_id":"Acsv-24_1","argument":"企業のトップは、長く働く人を求めるのではなく、いろんな人が適材適所で働ける環境を作ることを考えるべき。","x":8.962116,"y":12.548105,"p":0,"cluster_ids":["0","1_8","2_62"],"attributes":null,"url":null},{"arg_id":"Acsv-24_2","argument":"育休についても、イクボスの考え方を拡大することで、誰もが働きやすい環境が整うと良い。","x":8.859932,"y":12.176031,"p":0,"cluster_ids":["0","1_8","2_31"],"attributes":null,"url":null},{"arg_id":"Acsv-25_0","argument":"子供から高齢者までが触れ合いできる環境を整えるべき","x":8.728789,"y":11.75789,"p":0,"cluster_ids":["0","1_8","2_6"],"attributes":null,"url":null},{"arg_id":"Acsv-25_1","argument":"接点が多くなるように工夫する必要がある","x":5.2571096,"y":11.792261,"p":0,"cluster_ids":["0","1_6","2_8"],"attributes":null,"url":null},{"arg_id":"Acsv-26_0","argument":"健康かつ自由でのびのびと生活できることが重要である","x":7.1243715,"y":11.673479,"p":0,"cluster_ids":["0","1_5","2_16"],"attributes":null,"url":null},{"arg_id":"Acsv-27_0","argument":"娯楽が溢れる世の中である","x":6.6159863,"y":13.345639,"p":0,"cluster_ids":["0","1_7","2_19"],"attributes":null,"url":null},{"arg_id":"Acsv-28_0","argument":"一度失敗しても、許してくれる人になりたい。","x":5.1314316,"y":12.759922,"p":0,"cluster_ids":["0","1_7","2_30"],"attributes":null,"url":null},{"arg_id":"Acsv-29_0","argument":"新しくできたスタバで働きたい","x":9.143771,"y":10.028287,"p":0,"cluster_ids":["0","1_3","2_20"],"attributes":null,"url":null},{"arg_id":"Acsv-30_0","argument":"多様な人がお互いを尊重し気持ちよく生きられる社会が必要である。","x":5.231983,"y":10.235026,"p":0,"cluster_ids":["0","1_4","2_41"],"attributes":null,"url":null},{"arg_id":"Acsv-62_0","argument":"子供の遊び場を増やすべきである。","x":8.745605,"y":11.199449,"p":0,"cluster_ids":["0","1_3","2_43"],"attributes":null,"url":null},{"arg_id":"Acsv-63_0","argument":"若者が楽しめる場所や観光地を増やすべき","x":8.215872,"y":11.301103,"p":0,"cluster_ids":["0","1_8","2_34"],"attributes":null,"url":null},{"arg_id":"Acsv-66_0","argument":"子供の「こうしたい」を多く認めるべきである","x":9.032056,"y":11.152437,"p":0,"cluster_ids":["0","1_3","2_43"],"attributes":null,"url":null},{"arg_id":"Acsv-66_1","argument":"部活や勉学にのびのびと励める環境が必要である","x":8.269174,"y":11.82054,"p":0,"cluster_ids":["0","1_8","2_47"],"attributes":null,"url":null},{"arg_id":"Acsv-67_0","argument":"五条の立体駐車場を復活してほしい","x":8.520725,"y":10.336206,"p":0,"cluster_ids":["0","1_3","2_39"],"attributes":null,"url":null},{"arg_id":"Acsv-68_0","argument":"みんなが笑顔で過ごせることが重要である。","x":6.08832,"y":13.72331,"p":0,"cluster_ids":["0","1_7","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-69_0","argument":"気持ちに余裕をもって人にやさしくしたい。","x":4.9986115,"y":12.174826,"p":0,"cluster_ids":["0","1_6","2_8"],"attributes":null,"url":null},{"arg_id":"Acsv-322_0","argument":"不安のない日々を求めるべきである。","x":6.7590036,"y":12.458078,"p":0,"cluster_ids":["0","1_8","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-323_0","argument":"多文化共生を終わらせるべきである。","x":5.3093386,"y":9.9677925,"p":0,"cluster_ids":["0","1_4","2_0"],"attributes":null,"url":null},{"arg_id":"Acsv-323_1","argument":"日本人にばかり我慢を強いることは問題であり、子どもたちに迷惑をかけないようにすべきである。","x":9.147759,"y":11.228075,"p":0,"cluster_ids":["0","1_3","2_43"],"attributes":null,"url":null},{"arg_id":"Acsv-324_0","argument":"それぞれが自分の特性に応じた生き方ができるべきである。","x":6.728064,"y":11.622831,"p":0,"cluster_ids":["0","1_5","2_26"],"attributes":null,"url":null},{"arg_id":"Acsv-324_1","argument":"人を馬鹿にしないことが重要であり、馬鹿にすることで相手に依存しない自立した生き方が求められる。","x":6.607884,"y":11.275176,"p":0,"cluster_ids":["0","1_5","2_53"],"attributes":null,"url":null},{"arg_id":"Acsv-336_0","argument":"高齢者が車の運転ができなくても、自由にまちを行き来できる環境が必要である。","x":7.8206806,"y":11.394318,"p":0,"cluster_ids":["0","1_8","2_54"],"attributes":null,"url":null},{"arg_id":"Acsv-336_1","argument":"安心して医療を受けられるまちであるべきである。","x":7.173423,"y":12.15748,"p":0,"cluster_ids":["0","1_8","2_56"],"attributes":null,"url":null},{"arg_id":"Acsv-338_0","argument":"舞鶴海洋少年団での活動を通じて、舞鶴で育って良かったと思ってもらえるようにしたい。","x":9.942599,"y":9.329974,"p":0,"cluster_ids":["0","1_1","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-358_0","argument":"市民が住みやすいと思えるまちを作るべき","x":7.173812,"y":9.525878,"p":0,"cluster_ids":["0","1_2","2_11"],"attributes":null,"url":null},{"arg_id":"Acsv-358_1","argument":"市民が出て行こうと思わないまちを目指すべき","x":7.312801,"y":9.7209835,"p":0,"cluster_ids":["0","1_2","2_60"],"attributes":null,"url":null},{"arg_id":"Acsv-358_2","argument":"持続可能なまちを実現する必要がある","x":7.417872,"y":9.316113,"p":0,"cluster_ids":["0","1_2","2_60"],"attributes":null,"url":null},{"arg_id":"Acsv-359_0","argument":"若者が将来に夢を持てる環境を整えるべき","x":8.060748,"y":11.779638,"p":0,"cluster_ids":["0","1_8","2_15"],"attributes":null,"url":null},{"arg_id":"Acsv-359_1","argument":"舞鶴市内に就職先を増やすべき","x":9.850376,"y":9.202954,"p":0,"cluster_ids":["0","1_1","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-359_2","argument":"年配の方が安心して暮らせる社会を実現すべき","x":7.3967876,"y":12.551438,"p":0,"cluster_ids":["0","1_8","2_29"],"attributes":null,"url":null},{"arg_id":"Acsv-360_0","argument":"市民全員が助け合って過ごせるようにすべき","x":6.731341,"y":9.487942,"p":0,"cluster_ids":["0","1_4","2_42"],"attributes":null,"url":null},{"arg_id":"Acsv-360_1","argument":"市全体が優しいコミュニティになるように努めるべき","x":6.696842,"y":9.710662,"p":0,"cluster_ids":["0","1_4","2_42"],"attributes":null,"url":null},{"arg_id":"Acsv-360_2","argument":"孤独にならないように配慮すべき","x":6.62597,"y":12.016887,"p":0,"cluster_ids":["0","1_5","2_33"],"attributes":null,"url":null},{"arg_id":"Acsv-361_0","argument":"多様な人がその人らしく暮らせることは守られるべき尊厳である。","x":5.2229433,"y":9.919813,"p":0,"cluster_ids":["0","1_4","2_0"],"attributes":null,"url":null},{"arg_id":"Acsv-361_1","argument":"障害や病気、高齢に関わらず「自分で選べる、自分でできる」ことが守られるべきである。","x":7.324122,"y":11.475619,"p":0,"cluster_ids":["0","1_5","2_16"],"attributes":null,"url":null},{"arg_id":"Acsv-361_2","argument":"舞鶴が「人を大事にできる」人が大勢になることを願う。","x":9.657847,"y":9.398143,"p":0,"cluster_ids":["0","1_1","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-361_3","argument":"人を大事にできる町になれば争いも生まれず、大きな派閥に苦しむこともない。","x":6.2472043,"y":9.66811,"p":0,"cluster_ids":["0","1_4","2_22"],"attributes":null,"url":null},{"arg_id":"Acsv-361_4","argument":"みんなが自分の得意を活かす町になれると思う。","x":6.873296,"y":9.972563,"p":0,"cluster_ids":["0","1_4","2_23"],"attributes":null,"url":null},{"arg_id":"Acsv-363_0","argument":"光熱水を自給自足することが重要である","x":6.8869,"y":11.477098,"p":0,"cluster_ids":["0","1_5","2_16"],"attributes":null,"url":null},{"arg_id":"Acsv-363_1","argument":"投資で得たお金を使って通販や外食、映画鑑賞を楽しむべきである","x":7.483627,"y":11.971758,"p":0,"cluster_ids":["0","1_8","2_38"],"attributes":null,"url":null},{"arg_id":"Acsv-363_2","argument":"車や公共交通機関を積極的に利用することが推奨される","x":7.741761,"y":10.997355,"p":0,"cluster_ids":["0","1_3","2_32"],"attributes":null,"url":null},{"arg_id":"Acsv-363_3","argument":"赤字になりがちな稲作や土地の整備を行うことが必要である","x":7.3627725,"y":8.969213,"p":0,"cluster_ids":["0","1_2","2_24"],"attributes":null,"url":null},{"arg_id":"Acsv-365_0","argument":"障害のある人に対する偏見を持たないように、小さい頃から共に学習すべき","x":8.1981945,"y":10.890319,"p":0,"cluster_ids":["0","1_3","2_10"],"attributes":null,"url":null},{"arg_id":"Acsv-365_1","argument":"障害のある人が身近にいることが当たり前であるという認識をすべての市民が持つべき","x":8.076575,"y":10.629282,"p":0,"cluster_ids":["0","1_3","2_10"],"attributes":null,"url":null},{"arg_id":"Acsv-367_0","argument":"障害児も含め、どんな子どもでも挨拶ができる街であるべき","x":8.860062,"y":10.853272,"p":0,"cluster_ids":["0","1_3","2_21"],"attributes":null,"url":null},{"arg_id":"Acsv-367_1","argument":"偏見なく健やかに育つ環境が重要である","x":8.276671,"y":11.591494,"p":0,"cluster_ids":["0","1_8","2_15"],"attributes":null,"url":null},{"arg_id":"Acsv-368_0","argument":"多様な人が互いに尊重するためには、個々人が豊かな感性と教養を持つ必要がある。","x":5.062848,"y":10.413847,"p":0,"cluster_ids":["0","1_4","2_41"],"attributes":null,"url":null},{"arg_id":"Acsv-368_1","argument":"ほんとうの意味で賢い市民になりたい。","x":6.8636236,"y":9.597706,"p":0,"cluster_ids":["0","1_4","2_42"],"attributes":null,"url":null},{"arg_id":"Acsv-368_2","argument":"そういう人が増えていくまちでありたい。","x":7.3735476,"y":10.5751705,"p":0,"cluster_ids":["0","1_3","2_18"],"attributes":null,"url":null},{"arg_id":"Acsv-369_0","argument":"多様な人が年齢関係なくお互いを尊重し、繋がり、助け合える社会が必要である。","x":5.2706466,"y":10.401358,"p":0,"cluster_ids":["0","1_4","2_41"],"attributes":null,"url":null},{"arg_id":"Acsv-370_0","argument":"若い世代が増えないと衰退してしまう","x":7.842402,"y":12.279388,"p":0,"cluster_ids":["0","1_8","2_52"],"attributes":null,"url":null},{"arg_id":"Acsv-370_1","argument":"若い世代が生き生きと自分らしく生きていける場所が必要である","x":7.9229484,"y":12.094496,"p":0,"cluster_ids":["0","1_8","2_52"],"attributes":null,"url":null},{"arg_id":"Acsv-374_0","argument":"老若男女が役割を持ち、活気をもって暮らす姿が望ましい。","x":8.3067255,"y":12.695989,"p":0,"cluster_ids":["0","1_8","2_58"],"attributes":null,"url":null},{"arg_id":"Acsv-415_0","argument":"自分のふるさとを大切にするべきである。","x":6.479995,"y":11.411535,"p":0,"cluster_ids":["0","1_5","2_53"],"attributes":null,"url":null},{"arg_id":"Acsv-416_0","argument":"みんなの居場所を作るべきである","x":7.512971,"y":10.416938,"p":0,"cluster_ids":["0","1_3","2_18"],"attributes":null,"url":null},{"arg_id":"Acsv-416_1","argument":"いろんな人を笑顔にできることが重要である","x":5.9084854,"y":13.817487,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-417_0","argument":"自分のやりたいことを楽しくできている","x":6.400157,"y":12.420202,"p":0,"cluster_ids":["0","1_7","2_37"],"attributes":null,"url":null},{"arg_id":"Acsv-418_0","argument":"舞鶴の良さを教えられる人でありたい","x":9.840231,"y":9.152316,"p":0,"cluster_ids":["0","1_1","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-419_0","argument":"たくさんの人が観光スポットに行きたいと思えるようにするべきである。","x":8.042564,"y":10.164588,"p":0,"cluster_ids":["0","1_3","2_61"],"attributes":null,"url":null},{"arg_id":"Acsv-420_0","argument":"人に優しくするべき","x":5.019461,"y":11.975775,"p":0,"cluster_ids":["0","1_6","2_8"],"attributes":null,"url":null},{"arg_id":"Acsv-420_1","argument":"街が好きであるべき","x":7.838077,"y":9.375039,"p":0,"cluster_ids":["0","1_2","2_44"],"attributes":null,"url":null},{"arg_id":"Acsv-420_2","argument":"幸せでありたい","x":6.128554,"y":13.320129,"p":0,"cluster_ids":["0","1_7","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-432_0","argument":"他人のためになることをたまにやりたい","x":5.320558,"y":12.38848,"p":0,"cluster_ids":["0","1_6","2_51"],"attributes":null,"url":null},{"arg_id":"Acsv-434_0","argument":"自然を大切にする世の中が必要である。","x":6.527686,"y":10.913307,"p":0,"cluster_ids":["0","1_4","2_9"],"attributes":null,"url":null},{"arg_id":"Acsv-435_0","argument":"子どもの人権尊重を起点として、あらゆる人々の尊厳が守られる社会が望ましい。","x":5.559224,"y":9.956448,"p":0,"cluster_ids":["0","1_4","2_17"],"attributes":null,"url":null},{"arg_id":"Acsv-435_1","argument":"自分の頭で考え、対話しつつ試行錯誤できる市民が増えることが重要である。","x":6.733096,"y":10.033103,"p":0,"cluster_ids":["0","1_4","2_23"],"attributes":null,"url":null},{"arg_id":"Acsv-435_2","argument":"頼もしい若者たちが新しい実験を繰り返し、それを見守る大人たちも共に成長する社会が理想である。","x":5.8708563,"y":10.472336,"p":0,"cluster_ids":["0","1_4","2_28"],"attributes":null,"url":null},{"arg_id":"Acsv-435_3","argument":"生命力に満ちた社会であることが望ましい。","x":5.678205,"y":10.30314,"p":0,"cluster_ids":["0","1_4","2_17"],"attributes":null,"url":null},{"arg_id":"Acsv-441_0","argument":"失敗しても大丈夫な世の中であるべき","x":5.2483673,"y":13.043538,"p":0,"cluster_ids":["0","1_7","2_30"],"attributes":null,"url":null},{"arg_id":"Acsv-442_0","argument":"ヤンキーを減らしたい","x":6.0070615,"y":12.009488,"p":0,"cluster_ids":["0","1_5","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-442_1","argument":"誰もが優しく接することができるようになりたい","x":4.910435,"y":12.08104,"p":0,"cluster_ids":["0","1_6","2_8"],"attributes":null,"url":null},{"arg_id":"Acsv-443_0","argument":"便利で不満の少ない街を目指すべきである。","x":7.7060213,"y":9.656126,"p":0,"cluster_ids":["0","1_2","2_2"],"attributes":null,"url":null},{"arg_id":"Acsv-446_0","argument":"町は互いに尊重し、尊敬できる環境を作るべきである。","x":5.778217,"y":9.479208,"p":0,"cluster_ids":["0","1_4","2_48"],"attributes":null,"url":null},{"arg_id":"Acsv-447_0","argument":"自由に生きることが重要である","x":6.9314632,"y":11.447845,"p":0,"cluster_ids":["0","1_5","2_16"],"attributes":null,"url":null},{"arg_id":"Acsv-449_0","argument":"かねを出すべきである","x":5.7552214,"y":12.084989,"p":0,"cluster_ids":["0","1_5","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-450_0","argument":"暮らしやすい街を作ることが重要である。","x":7.6543326,"y":9.1006155,"p":0,"cluster_ids":["0","1_2","2_44"],"attributes":null,"url":null},{"arg_id":"Acsv-451_0","argument":"多様な人がお互いを尊重し自由に生きられる街を目指すべき","x":5.835404,"y":9.630666,"p":0,"cluster_ids":["0","1_4","2_48"],"attributes":null,"url":null},{"arg_id":"Acsv-452_0","argument":"ゴミをできるだけ減らして、SDGsに協力すべき","x":7.0232663,"y":9.196518,"p":0,"cluster_ids":["0","1_2","2_11"],"attributes":null,"url":null},{"arg_id":"Acsv-453_0","argument":"一人一人がみんなを思い合う世の中が望ましい。","x":5.669193,"y":12.797802,"p":0,"cluster_ids":["0","1_7","2_14"],"attributes":null,"url":null},{"arg_id":"Acsv-454_0","argument":"全員が正しい行動ができるべきである。","x":6.427613,"y":11.924753,"p":0,"cluster_ids":["0","1_5","2_33"],"attributes":null,"url":null},{"arg_id":"Acsv-455_0","argument":"厳しいことがない","x":6.1562896,"y":12.758587,"p":0,"cluster_ids":["0","1_7","2_37"],"attributes":null,"url":null},{"arg_id":"Acsv-456_0","argument":"まわりの人たちと協力できる優しい姿が重要である。","x":5.225649,"y":11.489744,"p":0,"cluster_ids":["0","1_6","2_27"],"attributes":null,"url":null},{"arg_id":"Acsv-459_0","argument":"自由に生きられるべきである","x":6.9939613,"y":11.40207,"p":0,"cluster_ids":["0","1_5","2_16"],"attributes":null,"url":null},{"arg_id":"Acsv-460_0","argument":"お互いを気遣うことが重要である","x":4.772554,"y":10.869142,"p":0,"cluster_ids":["0","1_6","2_13"],"attributes":null,"url":null},{"arg_id":"Acsv-460_1","argument":"自由に生きていける街を目指すべきである","x":7.6203103,"y":9.892812,"p":0,"cluster_ids":["0","1_3","2_40"],"attributes":null,"url":null},{"arg_id":"Acsv-461_0","argument":"助け合う地域が重要である","x":5.544105,"y":11.304484,"p":0,"cluster_ids":["0","1_6","2_59"],"attributes":null,"url":null},{"arg_id":"Acsv-463_0","argument":"悲しんだり嫌な思いをする人がいない世の中が望ましい。","x":5.861901,"y":12.837964,"p":0,"cluster_ids":["0","1_7","2_14"],"attributes":null,"url":null},{"arg_id":"Acsv-465_0","argument":"みんなが楽しく過ごすことが重要であり、困ることがない状況を目指すべきである。","x":6.553746,"y":13.074224,"p":0,"cluster_ids":["0","1_7","2_35"],"attributes":null,"url":null},{"arg_id":"Acsv-467_0","argument":"楽しい生活ができる","x":6.685876,"y":13.086447,"p":0,"cluster_ids":["0","1_7","2_35"],"attributes":null,"url":null},{"arg_id":"Acsv-468_0","argument":"みんなが笑っていられる姿が大切である。","x":5.628384,"y":13.743215,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-469_0","argument":"どの年代でも楽しく生きられる","x":7.5091987,"y":12.891568,"p":0,"cluster_ids":["0","1_8","2_29"],"attributes":null,"url":null},{"arg_id":"Acsv-470_0","argument":"若い人からお年寄りまで、いろんな人が楽しく過ごすべきである。","x":7.750925,"y":12.870421,"p":0,"cluster_ids":["0","1_8","2_46"],"attributes":null,"url":null},{"arg_id":"Acsv-472_0","argument":"赤ちゃんからお年寄りまで全員が元気なまちを目指すべきである。","x":8.870934,"y":11.874538,"p":0,"cluster_ids":["0","1_8","2_6"],"attributes":null,"url":null},{"arg_id":"Acsv-473_0","argument":"笑顔で過ごしていたい。","x":6.0577703,"y":13.658253,"p":0,"cluster_ids":["0","1_7","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-473_1","argument":"過ごしやすい舞鶴に住みたい。","x":10.129043,"y":9.116822,"p":0,"cluster_ids":["0","1_1","2_57"],"attributes":null,"url":null},{"arg_id":"Acsv-473_2","argument":"人と人との繋がりが沢山あることが重要。","x":4.968842,"y":11.440251,"p":0,"cluster_ids":["0","1_6","2_27"],"attributes":null,"url":null},{"arg_id":"Acsv-474_0","argument":"家族全員で安心して過ごしている姿が重要である","x":6.966522,"y":12.552784,"p":0,"cluster_ids":["0","1_8","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-475_0","argument":"差別のない誰もが尊重し合える社会を目指すべきである。","x":5.45704,"y":10.089363,"p":0,"cluster_ids":["0","1_4","2_17"],"attributes":null,"url":null},{"arg_id":"Acsv-477_0","argument":"誰もが失敗を笑わずに仲良くしている姿が望ましい。","x":5.4305,"y":13.398927,"p":0,"cluster_ids":["0","1_7","2_45"],"attributes":null,"url":null},{"arg_id":"Acsv-478_0","argument":"みんなが協力している姿は重要である。","x":5.525818,"y":11.6371765,"p":0,"cluster_ids":["0","1_6","2_55"],"attributes":null,"url":null},{"arg_id":"Acsv-479_0","argument":"みんなに好かれる姿が重要である。","x":5.8366065,"y":13.901526,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-480_0","argument":"多様な人がお互いを尊重し自由に生きられる社会が必要である。","x":5.41302,"y":9.92126,"p":0,"cluster_ids":["0","1_4","2_0"],"attributes":null,"url":null},{"arg_id":"Acsv-481_0","argument":"差別がないお互いが大切にしあえる関係を持ちたい","x":4.7316566,"y":10.8651285,"p":0,"cluster_ids":["0","1_6","2_13"],"attributes":null,"url":null},{"arg_id":"Acsv-481_1","argument":"辛くても支え合い、笑い合える深い関係を人と持ちたい","x":4.629763,"y":11.63368,"p":0,"cluster_ids":["0","1_6","2_50"],"attributes":null,"url":null},{"arg_id":"Acsv-482_0","argument":"子育て世代がのびのびと生活できる姿が重要である","x":8.311699,"y":11.998833,"p":0,"cluster_ids":["0","1_8","2_47"],"attributes":null,"url":null},{"arg_id":"Acsv-482_1","argument":"小中高校生がのびのびと学べる姿が重要である","x":6.1421256,"y":11.736143,"p":0,"cluster_ids":["0","1_5","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-483_0","argument":"お互い尊重し合える。","x":4.7745895,"y":10.401576,"p":0,"cluster_ids":["0","1_4","2_41"],"attributes":null,"url":null},{"arg_id":"Acsv-485_0","argument":"お年寄りや小さい子供を助ける姿が重要である。","x":8.714725,"y":11.955952,"p":0,"cluster_ids":["0","1_8","2_31"],"attributes":null,"url":null},{"arg_id":"Acsv-487_0","argument":"いろんな年齢の人が繋がれるべきである","x":7.878756,"y":13.071999,"p":0,"cluster_ids":["0","1_8","2_46"],"attributes":null,"url":null},{"arg_id":"Acsv-488_0","argument":"舞鶴市には大きい施設ができるべきである。","x":9.882319,"y":9.08479,"p":0,"cluster_ids":["0","1_1","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-488_1","argument":"舞鶴市はポイ捨てが少ない安全安心な生活ができるようにすべきである。","x":10.042064,"y":9.092301,"p":0,"cluster_ids":["0","1_1","2_57"],"attributes":null,"url":null},{"arg_id":"Acsv-489_0","argument":"平和な世界が必要である。","x":6.2584424,"y":10.310955,"p":0,"cluster_ids":["0","1_4","2_63"],"attributes":null,"url":null},{"arg_id":"Acsv-491_0","argument":"みんなが楽しく生活している姿が望ましい","x":6.395938,"y":13.483314,"p":0,"cluster_ids":["0","1_7","2_19"],"attributes":null,"url":null},{"arg_id":"Acsv-492_0","argument":"道であいさつや笑顔が溢れる姿が望ましい。","x":5.7878165,"y":13.756769,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-494_0","argument":"安心して過ごせる環境が必要である。","x":7.1754837,"y":12.419519,"p":0,"cluster_ids":["0","1_8","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-495_0","argument":"活気あふれる感じの街であるべき","x":7.992749,"y":9.225932,"p":0,"cluster_ids":["0","1_2","2_44"],"attributes":null,"url":null},{"arg_id":"Acsv-496_0","argument":"いろんな店がある","x":7.6787,"y":13.332872,"p":0,"cluster_ids":["0","1_8","2_46"],"attributes":null,"url":null},{"arg_id":"Acsv-497_0","argument":"今よりも人口が増えていてほしい。","x":7.4926333,"y":10.9148,"p":0,"cluster_ids":["0","1_3","2_32"],"attributes":null,"url":null},{"arg_id":"Acsv-499_0","argument":"みんなが笑っている姿は良いものである。","x":5.649821,"y":13.929399,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-500_0","argument":"心が広い人は、他人に対して寛容で理解を示すことができる。","x":4.838007,"y":11.22616,"p":0,"cluster_ids":["0","1_6","2_13"],"attributes":null,"url":null},{"arg_id":"Acsv-501_0","argument":"老若男女問わず明るく仲良く振る舞える場所が必要である","x":8.154864,"y":12.707625,"p":0,"cluster_ids":["0","1_8","2_58"],"attributes":null,"url":null},{"arg_id":"Acsv-501_1","argument":"子供達が活気に溢れている環境が望ましい","x":8.868066,"y":11.631282,"p":0,"cluster_ids":["0","1_8","2_6"],"attributes":null,"url":null},{"arg_id":"Acsv-501_2","argument":"誰もが住みたい場所を作るべきである","x":7.706988,"y":10.058074,"p":0,"cluster_ids":["0","1_3","2_40"],"attributes":null,"url":null},{"arg_id":"Acsv-502_0","argument":"みんなが楽しく自由に生きられるべきである。","x":6.827914,"y":12.874103,"p":0,"cluster_ids":["0","1_7","2_35"],"attributes":null,"url":null},{"arg_id":"Acsv-503_0","argument":"清潔なまちを維持することが重要である。","x":7.321292,"y":8.913634,"p":0,"cluster_ids":["0","1_2","2_24"],"attributes":null,"url":null},{"arg_id":"Acsv-504_0","argument":"多様な人がお互いを尊重して自由に生きられる社会が必要である。","x":5.1248064,"y":10.042497,"p":0,"cluster_ids":["0","1_4","2_0"],"attributes":null,"url":null},{"arg_id":"Acsv-506_0","argument":"自分から進んで行動できる姿が重要である。","x":6.472229,"y":11.477902,"p":0,"cluster_ids":["0","1_5","2_53"],"attributes":null,"url":null},{"arg_id":"Acsv-507_0","argument":"みんなの笑顔が見える姿が大切である。","x":5.8912177,"y":14.064849,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-508_0","argument":"少子高齢化に伴い、大人が楽しく暮らせるようにボランティアに積極的に参加し交流を増やしたい。","x":8.648574,"y":12.328612,"p":0,"cluster_ids":["0","1_8","2_31"],"attributes":null,"url":null},{"arg_id":"Acsv-508_1","argument":"労働者が減っている会社に就いて、少しでも活性化に貢献したい。","x":8.9892235,"y":12.598624,"p":0,"cluster_ids":["0","1_8","2_62"],"attributes":null,"url":null}],"clusters":[{"level":0,"id":"0","label":"全体","takeaway":"","value":154,"parent":"","density_rank_percentile":0},{"level":1,"id":"1_1","label":"舞鶴市の魅力を高める地域振興と住民幸福の実現","takeaway":"舞鶴市の発展に向けた具体的な施策として、地域の魅力を向上させるための大規模な施設の設立や就職機会の増加が求められています。また、地域活動を通じてコミュニティの絆を深め、住民が病気になっても舞鶴での生活を肯定的に捉えられる環境を整えることが重要視されています。さらに、舞鶴で育った子どもたちが成人後も戻りたくなるような街づくりや、安全で快適な生活環境の促進が強調されており、ポイ捨ての少ない清潔な街を目指す声も多く寄せられています。","value":11,"parent":"0","density_rank_percentile":0.125},{"level":1,"id":"1_8","label":"世代を超えた共生と活力ある地域社会の実現","takeaway":"子どもから高齢者までが共に過ごし、触れ合うことができる環境を整えることが、地域社会の活性化に寄与します。安心して医療を受けられる環境や、育児休業を支援する制度の拡充、さらには多様な楽しみの場を提供することで、全世代が元気に過ごせるまちづくりが求められています。また、伝統や文化を尊重しつつ、若者が夢を持てる環境を整えることが、持続可能な社会の構築にもつながります。","value":33,"parent":"0","density_rank_percentile":1},{"level":1,"id":"1_2","label":"持続可能で魅力的な地域社会の構築と市民の定住促進","takeaway":"過疎化が進む地域において、住民が定住し続けられるような持続可能なまちづくりが求められています。市民が戻りたいと思える便利で快適な環境を整え、地域の結束や協力を促進することが重要です。また、ゴミ削減やSDGsへの協力を通じて、環境に配慮した社会を築くことが期待されています。農業の持続可能性を確保し、清潔で活気のある街を実現することで、住みやすさと地域の魅力を高めることが目指されています。","value":13,"parent":"0","density_rank_percentile":0.25},{"level":1,"id":"1_6","label":"地域社会における相互支援とポジティブな人間関係の構築","takeaway":"地域社会での助け合いや相互尊重を通じて、ポジティブな人間関係を築くことの重要性が強調されています。自己肯定感を高め、他者に対する優しさや寛容さを持つことで、差別のない関係を育み、地域の多様な人材が協力し合う環境を作ることが求められています。また、支え合いや笑いを通じて深い絆を形成し、心の余裕を持って人々と接することが、より良いコミュニティの実現に寄与すると期待されています。","value":16,"parent":"0","density_rank_percentile":0.5},{"level":1,"id":"1_3","label":"誰もが快適に暮らせる共生社会の実現に向けた取り組み","takeaway":"地域住民が快適に生活できる環境を整えるため、公共施設の充実や交通手段の改善、子どもたちの権利を尊重した遊び場の提供が求められています。また、障害者や高齢者に優しい街づくりを進めることで、すべての市民が共に学び、支え合う社会の実現を目指しています。観光スポットの魅力向上や居場所の形成も重要な要素であり、地域全体の活性化と人口増加を促進することが期待されています。","value":18,"parent":"0","density_rank_percentile":0.875},{"level":1,"id":"1_4","label":"共創と尊重を基盤とした持続可能な社会の実現","takeaway":"若者と大人が共に成長し、互いに支え合うことで、平和で争いのない社会を築くことが求められています。また、技術の進歩と自然保護の調和を図りながら、すべての人が尊重される環境を整えることが重要です。市民が主体的に参加し、対話を重視することで、地域の活性化や助け合いの精神が育まれ、誰もが自由に生きられる社会の実現が期待されています。","value":27,"parent":"0","density_rank_percentile":0.75},{"level":1,"id":"1_7","label":"共感と楽しさに満ちた理想的なコミュニティの形成","takeaway":"個々の人々が互いに思いやりを持ち、笑顔で過ごすことができる社会を目指す意見が集まっています。失敗を受け入れ、再挑戦を支援することで、個人やコミュニティ全体が成長できる環境が求められています。また、娯楽や自由な自己実現を通じて、全ての人が楽しく充実した生活を送ることができる社会の実現が期待されています。","value":22,"parent":"0","density_rank_percentile":0.625},{"level":1,"id":"1_5","label":"自立した多様な生き方を支える社会の実現","takeaway":"個々の特性や自由を尊重し、自立した生活を送ることができる社会の重要性が強調されています。健康で自由な生活を送ることが個人の幸福に繋がるとともに、教育環境の改善や孤独対策を通じて、青少年が安心して成長できる場を提供することが求められています。また、全員が共通の行動基準を持ち、他者を尊重しながら自ら進んで行動する姿勢が、より良いコミュニティの形成に寄与することが期待されています。","value":14,"parent":"0","density_rank_percentile":0.375},{"level":2,"id":"2_4","label":"舞鶴市の地域振興と住民の幸福感向上","takeaway":"この意見グループは、舞鶴市の発展に向けた具体的な提案や願望が中心です。大きな施設の設立や就職先の増加を通じて地域の魅力を高め、住民が舞鶴での生活に誇りを持てるようにしたいという思いが表れています。また、地域活動を通じて育まれるコミュニティの重要性や、病気になっても舞鶴での生活を肯定的に捉えられるような環境づくりへの期待も含まれています。","value":5,"parent":"1_1","density_rank_percentile":0.375},{"level":2,"id":"2_49","label":"舞鶴の子どもたちが帰りたくなる街づくり","takeaway":"この意見グループは、舞鶴で育った子どもたちが成人後も地元に戻りたくなるような環境や街づくりの重要性を強調しています。具体的には、働く場所の確保や住みやすい環境の整備、人を大事にする文化の醸成が求められています。","value":4,"parent":"1_1","density_rank_percentile":0.640625},{"level":2,"id":"2_6","label":"世代を超えた活気ある共生環境の構築","takeaway":"この意見グループは、子どもから高齢者までが共に過ごし、触れ合うことができる環境の重要性を強調しています。子どもたちが活気に満ち、全世代が元気に過ごせるまちづくりを目指すことが、地域社会の活性化に寄与するという視点が中心です。","value":4,"parent":"1_8","density_rank_percentile":0.546875},{"level":2,"id":"2_60","label":"持続可能な地域社会の実現と市民の定住促進","takeaway":"この意見グループは、過疎化が進む地域において、住民が定住し続けられるような持続可能なまちづくりの重要性を強調しています。市民が出て行かないような魅力的な環境を整え、地域の発展を目指すことが求められています。また、「和をもって貴しとなす」という理念を実現することで、地域の結束や協力を促進し、より良い社会を築くことが期待されています。","value":4,"parent":"1_2","density_rank_percentile":0.5625},{"level":2,"id":"2_56","label":"安心・安全な医療環境の確立","takeaway":"この意見グループは、地域における医療の質と安全性に対する強い期待を示しています。住民が安心して医療を受けられる環境を求める声が中心であり、健康を最優先に考える姿勢が反映されています。","value":2,"parent":"1_8","density_rank_percentile":0.296875},{"level":2,"id":"2_25","label":"自己肯定感の向上とポジティブな自己認識","takeaway":"この意見グループは、自己肯定感が高いことに関連するポジティブな感情や自己認識の重要性を強調しています。自己肯定感が高い人々は、自分自身を受け入れ、価値を感じることで、より良い人間関係や生活の質を享受しているという点が共通しています。","value":1,"parent":"1_6","density_rank_percentile":0.015625},{"level":2,"id":"2_39","label":"地域施設の利便性向上に向けた要望","takeaway":"この意見グループは、地域のインフラや施設に対する具体的な改善要望が中心です。立体駐車場の復活は交通の利便性を高め、洋式トイレの設置は利用者の快適さを向上させることを目的としています。これらの要望は、地域住民や訪問者の利便性を重視した意見であり、公共施設の充実を求める声が反映されています。","value":2,"parent":"1_3","density_rank_percentile":0.484375},{"level":2,"id":"2_10","label":"障害者と高齢者に優しい共生社会の実現","takeaway":"この意見グループは、障害のある人々や高齢者が身近にいることを当たり前とし、偏見をなくすために教育を通じて共に学ぶことの重要性を強調しています。市民全体がその認識を持つことで、より優しい街づくりが進むことを目指しています。","value":3,"parent":"1_3","density_rank_percentile":0.75},{"level":2,"id":"2_13","label":"相互尊重と寛容な関係の構築","takeaway":"この意見グループは、他者を尊重し、縦と横のつながりを大切にすることの重要性に焦点を当てています。心の広さや寛容さを持つことで、差別のない関係を築き、お互いを気遣い合うことが強調されています。","value":4,"parent":"1_6","density_rank_percentile":0.921875},{"level":2,"id":"2_2","label":"便利で快適な田舎の実現","takeaway":"この意見グループは、便利で不満の少ない街づくりを目指すことに焦点を当てており、都会に出ても戻りたいと思えるような、過ごしやすく便利な田舎の必要性を強調しています。住みやすさと利便性を兼ね備えた地域の重要性が示されています。","value":2,"parent":"1_2","density_rank_percentile":0.28125},{"level":2,"id":"2_59","label":"地域の助け合いと多様な人材の共生","takeaway":"この意見グループは、地域社会における助け合いの重要性と、地域活動に多様な人材が関与することの必要性を強調しています。地域の絆を深めるためには、異なるバックグラウンドを持つ人々が協力し合い、共に活動することが不可欠であるという視点が中心です。","value":2,"parent":"1_6","density_rank_percentile":0.703125},{"level":2,"id":"2_12","label":"伝統と文化の継承と活用","takeaway":"この意見は、古き良き時代の価値や文化を重視し、それを現代に生かすべきだという考えを示しています。伝統的な知恵や技術を尊重し、未来に向けて活用することの重要性が強調されています。","value":1,"parent":"1_8","density_rank_percentile":0.03125},{"level":2,"id":"2_28","label":"若者の成長と大人の支援による共創社会","takeaway":"この意見グループは、若者が新しい実験を通じて成長し、その過程を見守る大人たちも共に成長することを理想としています。また、学び続ける素直さや、自己実現を目指す社会の重要性が強調されており、世代を超えた支援と共創の価値が中心テーマとなっています。","value":3,"parent":"1_4","density_rank_percentile":0.6875},{"level":2,"id":"2_7","label":"安心安全な共生社会の実現","takeaway":"この意見グループは、美しい自然環境と共存しながら、安心して自立した生活を送ることの重要性に焦点を当てています。特に、不安のない日々や家族全員が安心して過ごせる環境の必要性が強調されており、心地よい生活空間の確保が求められています。","value":4,"parent":"1_8","density_rank_percentile":0.796875},{"level":2,"id":"2_9","label":"技術進歩と自然保護の調和","takeaway":"この意見グループは、技術の進歩が社会全体に恩恵をもたらす一方で、すべての人がその進歩に適応できる環境が求められていること、また、自然環境を大切にすることが重要であるという視点が中心です。技術と自然の共存を目指す社会の実現が望まれています。","value":2,"parent":"1_4","density_rank_percentile":0.625},{"level":2,"id":"2_14","label":"共感と思いやりに満ちた理想社会の実現","takeaway":"この意見グループは、個々の人々が互いに思いやりを持ち、悲しみや嫌な思いをすることのない社会を望むという共通の願望を表しています。人々が互いに理解し合い、支え合うことで、より良い社会を築くことができるという理想が強調されています。","value":3,"parent":"1_7","density_rank_percentile":0.5},{"level":2,"id":"2_22","label":"人を大切にすることで争いのない社会の実現","takeaway":"この意見グループは、人々を大事にすることが争いを減少させ、社会全体の調和を促進するという考えに基づいています。大きな派閥や対立が存在しない、平和で協力的な町を目指すことが重要であるという前向きなビジョンが示されています。","value":2,"parent":"1_4","density_rank_percentile":0.671875},{"level":2,"id":"2_17","label":"誰もが尊重される平和で安全な社会の実現","takeaway":"この意見グループは、差別のない社会の構築や、犯罪率の低下、子どもの人権の尊重を通じて、すべての人々が互いに尊重し合い、安心して暮らせる社会の実現を目指すという共通の価値観を持っています。","value":4,"parent":"1_4","density_rank_percentile":0.765625},{"level":2,"id":"2_21","label":"すべての子どもに平等な進路とコミュニケーションの機会を提供する街","takeaway":"この意見グループは、経済的な理由によって子どもの進路が制限されることなく、すべての子どもが平等に教育や進路の選択肢を持つべきであるという考えに基づいています。また、障害の有無にかかわらず、すべての子どもが社会での基本的なコミュニケーション能力を身につけることが重要であるという点も強調されています。","value":2,"parent":"1_3","density_rank_percentile":0.34375},{"level":2,"id":"2_1","label":"高齢者の社会参加と生涯現役の推進","takeaway":"この意見グループは、高齢化社会において、年齢に関係なく仕事を持つことや、仕事を通じて社会に貢献することの重要性を強調しています。高齢者が活躍できる環境を整えることで、彼らの生きがいや社会的役割を高めることが理想とされています。","value":1,"parent":"1_8","density_rank_percentile":0.046875},{"level":2,"id":"2_62","label":"多様性を重視した労働環境の構築","takeaway":"この意見グループは、労働者の減少に対する企業の対応策として、多様な人材が適材適所で活躍できる環境を整えることの重要性を強調しています。企業のトップが長期的な雇用を求めるのではなく、柔軟で多様な働き方を促進することで、活性化に貢献できるという視点が中心です。","value":2,"parent":"1_8","density_rank_percentile":0.234375},{"level":2,"id":"2_31","label":"イクボスによる育休推進と地域交流の重要性","takeaway":"この意見グループは、育児休業（育休）を支援するイクボスの考え方を広めることで、誰もが働きやすい環境を整えることの重要性を強調しています。また、少子高齢化に対する対策として、ボランティア活動を通じて地域交流を促進し、特に高齢者や子供を支援する姿勢が求められるという点が中心となっています。","value":3,"parent":"1_8","density_rank_percentile":0.859375},{"level":2,"id":"2_8","label":"人間関係の改善と優しさの促進","takeaway":"この意見グループは、接点を増やす工夫や、他者に対する優しさを重視する姿勢が中心です。人々が気持ちに余裕を持ち、優しく接することができる環境を作ることが重要であるという共通の願望が表れています。","value":4,"parent":"1_6","density_rank_percentile":0.84375},{"level":2,"id":"2_16","label":"自由と自立を重視した生活の重要性","takeaway":"この意見グループは、自由に生きることや自給自足の重要性、そして障害や病気、高齢に関わらず自分で選択し行動できることの大切さに焦点を当てています。健康で自由な生活を送ることが、個人の自立や幸福に繋がるという考えが中心です。","value":5,"parent":"1_5","density_rank_percentile":0.875},{"level":2,"id":"2_19","label":"娯楽による豊かな生活の実現","takeaway":"この意見グループは、現代社会における娯楽の多様性とその重要性に焦点を当てています。娯楽が豊富に存在することで、人々が楽しく生活し、充実した日々を送ることができるという理想的なビジョンが表現されています。","value":2,"parent":"1_7","density_rank_percentile":0.578125},{"level":2,"id":"2_30","label":"失敗を受容する社会の実現","takeaway":"この意見グループは、失敗を恐れずに挑戦できる環境の重要性を強調しています。失敗を許容し、再挑戦を支援することで、個人や社会全体が成長できるという前向きな姿勢が表れています。","value":2,"parent":"1_7","density_rank_percentile":0.78125},{"level":2,"id":"2_20","label":"新店舗でのスターバックス勤務希望","takeaway":"この意見は、新しくオープンしたスターバックスでの勤務を希望するものであり、特に新しい環境での仕事に対する期待感や興味が表れています。新店舗での経験を通じて、ブランドの新たな一面を体験したいという意欲が感じられます。","value":1,"parent":"1_3","density_rank_percentile":0.0625},{"level":2,"id":"2_41","label":"多様性と相互尊重の共生社会の実現","takeaway":"この意見グループは、多様な人々が互いに尊重し合い、豊かな感性と教養を持つことが重要であるという考えに基づいています。また、年齢や背景に関係なく、互いに助け合い、繋がりを持つ社会の必要性が強調されています。これにより、より気持ちよく生きられる社会の実現が求められています。","value":4,"parent":"1_4","density_rank_percentile":0.953125},{"level":2,"id":"2_43","label":"子どもの権利と遊び環境の充実","takeaway":"この意見グループは、日本社会における子どもたちの権利や希望を尊重し、彼らが自由に遊び、自己表現できる環境を整えることの重要性を強調しています。特に、子どもたちに対して我慢を強いるのではなく、彼らの意見や希望を受け入れ、遊び場を増やすことが求められています。","value":3,"parent":"1_3","density_rank_percentile":0.8125},{"level":2,"id":"2_34","label":"若者向けの観光地と楽しめるスポットの拡充","takeaway":"この意見グループは、若者が楽しめる場所や観光地の不足を指摘し、彼らのニーズに応えるために新たなスポットを増やす必要性を強調しています。若者が集まりやすい環境を整えることで、地域活性化や観光促進につながることが期待されています。","value":1,"parent":"1_8","density_rank_percentile":0.078125},{"level":2,"id":"2_47","label":"子育て世代の自由な生活と成長を支える環境","takeaway":"この意見グループは、子育て世代がストレスなく生活できることの重要性や、部活動や学業において自由に取り組める環境の必要性に焦点を当てています。子どもたちがのびのびと成長できるための支援や環境整備が求められていることが示されています。","value":2,"parent":"1_8","density_rank_percentile":0.390625},{"level":2,"id":"2_36","label":"笑顔と幸せの共有を重視する価値観","takeaway":"この意見グループは、個人の幸せや笑顔を追求することに加え、周囲の人々が共に笑顔で過ごすことの重要性を強調しています。個人の幸福感が社会全体に波及することを願う姿勢が見受けられ、共感や連帯感を大切にする価値観が反映されています。","value":3,"parent":"1_7","density_rank_percentile":0.828125},{"level":2,"id":"2_0","label":"多様性と尊厳の共存を目指す社会の実現","takeaway":"この意見グループは、多様な人々が互いに尊重し合い、自由に生きることができる社会の重要性を強調しています。また、個々の尊厳を守ることが社会の基盤であるとし、多文化共生の必要性についても言及しています。全体として、個人の自由と多様性の尊重が共存する社会の実現を求める声が集まっています。","value":4,"parent":"1_4","density_rank_percentile":0.453125},{"level":2,"id":"2_26","label":"個々の特性に基づく多様な生き方の推奨","takeaway":"この意見は、各人が自分自身の特性や能力に応じた生き方を選択することの重要性を強調しています。個々の違いを尊重し、それぞれが自分に合った道を歩むことが、より豊かな社会を形成するために必要であるという考えが中心です。","value":1,"parent":"1_5","density_rank_percentile":0.09375},{"level":2,"id":"2_53","label":"自立と尊重を重視した行動の重要性","takeaway":"この意見グループは、他者を馬鹿にせず尊重することの重要性、自立した生き方の必要性、そして自分のふるさとを大切にする姿勢が強調されています。また、自ら進んで行動することが重要であるという考え方が共通しており、個人の成長や社会的な関係性の構築において、これらの要素が不可欠であることが示されています。","value":3,"parent":"1_5","density_rank_percentile":0.421875},{"level":2,"id":"2_54","label":"高齢者の移動自由を支えるインフラ整備","takeaway":"この意見は、高齢者が車を運転できない状況においても、自由に移動できる環境の重要性を強調しています。高齢者が安心して外出できるような公共交通機関や歩行者専用のインフラ整備が求められており、地域社会のバリアフリー化が必要であるという視点が中心です。","value":1,"parent":"1_8","density_rank_percentile":0.109375},{"level":2,"id":"2_11","label":"持続可能な社会の実現に向けた市民参加型のまちづくり","takeaway":"この意見グループは、ゴミ削減やSDGs（持続可能な開発目標）への協力を通じて、住みやすいまちを作ることの重要性を強調しています。市民が積極的に参加し、環境に配慮した社会を築くことが求められているという点が共通しています。","value":2,"parent":"1_2","density_rank_percentile":0.9375},{"level":2,"id":"2_15","label":"若者の夢を育む健全な環境の整備","takeaway":"この意見グループは、若者が将来に対して夢を持てるような環境の重要性を強調しています。偏見のない、健やかに育つことができる環境が、若者の成長や自己実現に寄与するという視点が中心です。","value":2,"parent":"1_8","density_rank_percentile":0.71875},{"level":2,"id":"2_29","label":"全世代が安心して楽しめる社会の実現","takeaway":"この意見グループは、年配の方々が安心して暮らせる社会の必要性と、すべての年代が楽しく生きられる環境の重要性を強調しています。年齢に関係なく、誰もが安心して生活できる社会の構築を目指す姿勢が見受けられます。","value":2,"parent":"1_8","density_rank_percentile":0.90625},{"level":2,"id":"2_42","label":"市民の助け合いと優しさを育むコミュニティ形成","takeaway":"この意見グループは、市全体が優しいコミュニティを目指し、市民同士が助け合いながら生活することの重要性を強調しています。市民が賢く、互いに支え合うことで、より良い社会を築くべきだという前向きな姿勢が見受けられます。","value":3,"parent":"1_4","density_rank_percentile":0.53125},{"level":2,"id":"2_33","label":"孤独対策と全員の行動規範の重要性","takeaway":"この意見グループは、孤独を避けるための配慮が必要であることと、全員が正しい行動を取ることの重要性に焦点を当てています。孤独を防ぐための具体的な施策や、全員が共通の行動基準を持つことが、より良い社会やコミュニティの形成に寄与するという考えが表れています。","value":2,"parent":"1_5","density_rank_percentile":0.515625},{"level":2,"id":"2_23","label":"市民の主体性と対話を重視した地域活性化","takeaway":"この意見グループは、地域社会において市民が自らの得意分野を活かし、主体的に考え、対話を通じて試行錯誤を行うことの重要性を強調しています。市民の参加と協力が地域の活性化に寄与するという前向きなビジョンが示されています。","value":2,"parent":"1_4","density_rank_percentile":0.328125},{"level":2,"id":"2_38","label":"投資利益の享受と生活の質向上","takeaway":"この意見は、投資によって得た利益を活用して、通販や外食、映画鑑賞などの娯楽活動を楽しむことが重要であるという考え方を示しています。投資の成果を生活の質向上に結びつけることで、より充実したライフスタイルを追求する姿勢が反映されています。","value":1,"parent":"1_8","density_rank_percentile":0.125},{"level":2,"id":"2_32","label":"持続可能な交通手段の推進と人口増加の期待","takeaway":"この意見グループは、車や公共交通機関の利用を促進することで、持続可能な社会を実現し、同時に人口の増加を望む声が中心です。交通手段の選択が環境への配慮や都市の発展に寄与することを期待しています。","value":2,"parent":"1_3","density_rank_percentile":0.609375},{"level":2,"id":"2_24","label":"農業と環境整備の重要性","takeaway":"この意見グループは、稲作や土地の整備に関する経済的な課題と、清潔なまちを維持することの重要性に焦点を当てています。農業の持続可能性を確保するための施策と、地域環境の整備が相互に関連していることを示唆しています。","value":2,"parent":"1_2","density_rank_percentile":0.25},{"level":2,"id":"2_18","label":"コミュニティ形成と居場所の重要性","takeaway":"この意見グループは、地域社会における居場所の必要性と、そこに集う人々の増加を望む声が中心です。居場所を作ることで、より多くの人々が集まり、交流や支え合いが生まれることを目指しています。","value":2,"parent":"1_3","density_rank_percentile":0.46875},{"level":2,"id":"2_52","label":"若い世代の活力と持続可能な社会の構築","takeaway":"この意見グループは、若い世代が自分らしく生き生きと生活できる環境の重要性を強調しており、若者の増加が社会の活力を保つために不可欠であるという認識が共有されています。若者の存在が社会の持続可能性に直結しているという視点が中心です。","value":2,"parent":"1_8","density_rank_percentile":0.4375},{"level":2,"id":"2_58","label":"世代を超えた共生と活気あるコミュニティの形成","takeaway":"この意見グループは、老若男女がそれぞれの役割を持ちながら、明るく仲良く過ごすことができる環境の重要性を強調しています。世代を問わず、活気に満ちたコミュニティの形成が求められており、相互の理解と協力が促進される場の必要性が示されています。","value":2,"parent":"1_8","density_rank_percentile":0.3125},{"level":2,"id":"2_5","label":"コミュニティの笑顔と相互理解の重要性","takeaway":"この意見グループは、笑顔やあいさつを通じて人々がつながり、互いに良い影響を与えることの重要性を強調しています。みんなが笑っている姿が望ましいとされ、コミュニティ全体の雰囲気や人間関係の向上に寄与することが期待されています。","value":6,"parent":"1_7","density_rank_percentile":0.734375},{"level":2,"id":"2_37","label":"楽しい自己実現とストレスフリーな環境","takeaway":"この意見グループは、自分のやりたいことを楽しみながら行うことができ、厳しい状況やストレスがないことに対するポジティブな感情が中心です。自己実現を楽しむことができる環境の重要性が強調されています。","value":2,"parent":"1_7","density_rank_percentile":1},{"level":2,"id":"2_61","label":"観光スポットの魅力向上とアクセス促進","takeaway":"この意見は、観光スポットの魅力を高め、多くの人々が訪れたくなるような施策が必要であるという考えを示しています。観光地の魅力を引き出し、アクセスを改善することで、観光客の増加を目指すべきという前向きな提案が中心です。","value":1,"parent":"1_3","density_rank_percentile":0.140625},{"level":2,"id":"2_44","label":"活気と暮らしやすさを兼ね備えた理想の街づくり","takeaway":"この意見グループは、街に対する愛着や理想像を表現しており、活気があり、住みやすい環境を重視する声が集まっています。街の活気や居住性の向上が、地域の魅力を高める重要な要素であると考えられています。","value":3,"parent":"1_2","density_rank_percentile":0.890625},{"level":2,"id":"2_51","label":"他者貢献の意欲とその重要性","takeaway":"この意見は、他人のために何かをすることへの関心や意欲を示しています。特に、時折でも他者に貢献することが重要であるという考えが中心にあり、社会的なつながりや助け合いの価値を強調しています。","value":1,"parent":"1_6","density_rank_percentile":0.15625},{"level":2,"id":"2_3","label":"教育環境の改善と青少年の健全育成","takeaway":"この意見グループは、青少年が安心して学べる環境を整えることの重要性に焦点を当てています。具体的には、教育への投資やヤンキーの減少を通じて、小中高校生がのびのびと学べる社会を目指す意見が集まっています。","value":3,"parent":"1_5","density_rank_percentile":0.984375},{"level":2,"id":"2_48","label":"相互尊重と自由な共生を促進する街づくり","takeaway":"この意見グループは、多様な人々が互いに尊重し合い、自由に生きることができる環境の重要性を強調しています。町の設計やコミュニティの形成において、相互尊重と共生を促進することが求められているという点が中心的なテーマです。","value":2,"parent":"1_4","density_rank_percentile":0.359375},{"level":2,"id":"2_27","label":"人間関係の構築と協力の重要性","takeaway":"この意見グループは、周囲の人々との協力や優しさが重要であるという考えに基づいています。人と人との繋がりを重視し、相互支援やコミュニケーションの重要性を強調する意見が中心です。","value":2,"parent":"1_6","density_rank_percentile":0.59375},{"level":2,"id":"2_40","label":"誰もが自由に暮らせる理想の街づくり","takeaway":"この意見グループは、全ての人々が住みたいと感じる場所を創出することの重要性に焦点を当てています。特に、自由に生きることができる環境を整えることが、理想的な街の実現に向けた目標であるという考えが中心です。","value":2,"parent":"1_3","density_rank_percentile":0.40625},{"level":2,"id":"2_35","label":"楽しく自由な生活の実現","takeaway":"この意見グループは、個人が楽しく自由に生きることの重要性を強調しており、全ての人が楽しく過ごせる社会を目指すべきだという前向きな姿勢が中心です。困難のない状況を求める声があり、幸福な生活の実現に向けた共通の願望が表れています。","value":3,"parent":"1_7","density_rank_percentile":0.65625},{"level":2,"id":"2_46","label":"世代を超えた交流と多様な楽しみの場","takeaway":"この意見グループは、若者から高齢者までの幅広い世代が集まり、楽しく過ごすことの重要性を強調しています。また、様々な店舗が存在することで、異なる年齢層の人々が繋がり、交流できる場を提供することが求められています。","value":3,"parent":"1_8","density_rank_percentile":0.96875},{"level":2,"id":"2_57","label":"舞鶴市の安全で快適な生活環境の促進","takeaway":"この意見グループは、舞鶴市における住環境の質に対する関心が高く、特にポイ捨ての少ない安全で安心な生活を求める声が中心です。住みやすさを重視し、地域の環境改善に向けた具体的な提案が含まれています。","value":2,"parent":"1_1","density_rank_percentile":0.265625},{"level":2,"id":"2_45","label":"失敗を受け入れる共感的なコミュニティの形成","takeaway":"この意見は、失敗を笑わずに受け入れ、仲間として支え合う姿勢が重要であることを強調しています。人々が互いに理解し合い、共感を持って接することで、より良い関係性が築かれることを望む声が集まっています。","value":1,"parent":"1_7","density_rank_percentile":0.171875},{"level":2,"id":"2_55","label":"協力によるチームワークの重要性","takeaway":"この意見は、チームメンバーが協力し合うことの重要性を強調しています。協力することで、個々の力を結集し、より良い成果を生み出すことができるという考えが中心にあります。","value":1,"parent":"1_6","density_rank_percentile":0.1875},{"level":2,"id":"2_50","label":"支え合いと笑いを通じた深い人間関係の構築","takeaway":"この意見は、困難な状況でもお互いに支え合い、共に笑い合うことで、より深い人間関係を築きたいという願望を表しています。人とのつながりの重要性や、感情的なサポートを求める姿勢が強調されています。","value":1,"parent":"1_6","density_rank_percentile":0.203125},{"level":2,"id":"2_63","label":"持続可能な平和の実現に向けた意識の高まり","takeaway":"この意見は、平和な世界の必要性を強調しており、持続可能な社会の実現に向けた意識の高まりを示しています。人々が平和を求める声が集まり、社会全体での協力や理解が重要であるというメッセージが込められています。","value":1,"parent":"1_4","density_rank_percentile":0.21875}],"comments":{},"propertyMap":{},"translations":{},"overview":"舞鶴市の地域振興に関する意見は、住民の幸福や地域の魅力向上を重視し、世代を超えた共生や持続可能な社会の構築が求められています。相互支援やポジティブな人間関係の形成、快適な生活環境の整備が強調され、特に子どもや高齢者に配慮した施策が重要視されています。また、共感や楽しさを重視し、自立した多様な生き方を支える社会の実現が期待されています。","config":{"name":"2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77","input":"2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77","question":"③市民として、2040年のありたい姿とは【1/31 15:00更新】","intro":"舞鶴市の未来に向けた意見は、地域活性化や人材育成、世代間の共生、持続可能なコミュニティ形成に集中しています。住民の幸福度を高めるためには、安心・安全な生活環境や多様性を尊重する社会の構築が求められています。また、地域のインフラ整備や観光資源の充実が商業活動を活性化し、全ての人々が楽しく暮らせる社会の実現が期待されています。\n分析対象となったデータの件数は508件で、これらのデータに対してOpenAI API (gpt-4o-mini)を用いて154件の意見（議論）を抽出し、クラスタリングを行った。\n","model":"gpt-4o-mini","provider":"openai","is_pubcom":true,"is_embedded_at_local":false,"local_llm_address":null,"extraction":{"prompt":"あなたは専門的なリサーチアシスタントです。与えられたテキストから、意見を抽出して整理してください。\n\n# 指示\n* 入出力の例に記載したような形式で文字列のリストを返してください\n  * 必要な場合は2つの別個の意見に分割してください。多くの場合は1つの議論にまとめる方が望ましいです。\n* 整理した意見は日本語で出力してください\n\n## 入出力の例\n/human\n\nAIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\"\n  ]\n}\n\n/human\n\nAIの能力、限界、倫理的考慮事項について、市民を教育する必要がある。また、教育できる人材を養成する必要がある。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIの能力、限界、倫理的考慮事項について、市民を教育すべき\",\n    \"AIに関する教育をできる人材を養成すべき\"\n  ]\n}\n\n/human\n\nAIはエネルギーグリッドを最適化し、無駄や炭素排出を削減できます。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIはエネルギーグリッドを最適化して炭素排出を削減できる\"\n  ]\n}\n","workers":30,"limit":508,"properties":[],"categories":{},"category_batch_size":5,"source_code":"$19","model":"gpt-4o-mini"},"hierarchical_clustering":{"cluster_nums":[8,64],"source_code":"$1a"},"hierarchical_initial_labelling":{"prompt":"あなたはKJ法が得意なデータ分析者です。userのinputはグループに集まったラベルです。なぜそのラベルが一つのグループであるか解説し、表札（label）をつけてください。\n表札については、グループ内の具体的な論点や特徴を反映した、具体性の高い名称を考案してください。\n出力はJSONとし、フォーマットは以下のサンプルを参考にしてください。\n\n\n# サンプルの入出力\n## 入力例\n- 手作業での意見分析は時間がかかりすぎる。AIで効率化できると嬉しい\n- 今のやり方だと分析に工数がかかりすぎるけど、AIならコストをかけずに分析できそう\n- AIが自動で意見を整理してくれると楽になって嬉しい\n\n\n## 出力例\n{\n    \"label\": \"AIによる業務効率の大幅向上とコスト効率化\",\n    \"description\": \"この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$1b","model":"gpt-4o-mini"},"hierarchical_merge_labelling":{"prompt":"あなたはデータ分析のエキスパートです。\n現在、テキストデータの階層クラスタリングを行っています。\n下層のクラスタ（意見グループ）のタイトルと説明、およびそれらのクラスタが所属する上層のクラスタのテキストのサンプルを与えるので、上層のクラスタのタイトルと説明を作成してください。\n\n# 指示\n- 統合後のクラスタ名は、統合前のクラスタ名称をそのまま引用せず、内容に基づいた新たな名称にしてください。\n- タイトルには、具体的な事象・行動（例：地域ごとの迅速対応、復興計画の着実な進展、効果的な情報共有・地域協力など）を含めてください\n  - 可能な限り具体的な表現を用いるようにし、抽象的な表現は避けてください\n    - 「多様な意見」などの抽象的な表現は避けてください\n- 出力例に示したJSON形式で出力してください\n\n\n# サンプルの入出力\n## 入力例\n- 「顧客フィードバックの自動集約」: この意見グループは、SNSやオンラインレビューなどから集めた大量の意見をAIが瞬時に解析し、企業が市場のトレンドや顧客の要望を即時に把握できる点についての期待を示しています。\n- 「AIによる業務効率の大幅向上とコスト効率化」: この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\n\n## 出力例\n{\n    \"label\": \"AI技術の導入による意見分析の効率化への期待\",\n    \"description\": \"大量の意見やフィードバックから迅速に洞察を抽出できるため、企業や自治体が消費者や市民の声を的確に把握し、戦略的な意思決定やサービス改善が可能になります。また、従来の手法と比べて作業負荷が軽減され、業務効率の向上やコスト削減といった実際の便益が得られると期待されています。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$1c","model":"gpt-4o-mini"},"hierarchical_overview":{"prompt":"/system \n\nあなたはシンクタンクで働く専門のリサーチアシスタントです。\nチームは特定のテーマに関してパブリック・コンサルテーションを実施し、異なる選択肢の意見グループを分析し始めています。\nこれから意見グループのリストとその簡単な分析が提供されます。\nあなたの仕事は、調査結果の簡潔な要約を返すことです。要約は非常に簡潔に（最大で1段落、最大4文）まとめ、無意味な言葉を避けてください。\n出力は日本語で行ってください。\n","source_code":"$1d","model":"gpt-4o-mini"},"hierarchical_aggregation":{"sampling_num":30,"hidden_properties":{},"source_code":"$1e"},"enable_source_link":false,"output_dir":"2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77","skip-interaction":true,"without-html":true,"embedding":{"model":"text-embedding-3-small","source_code":"$1f"},"hierarchical_visualization":{"replacements":[],"source_code":"import subprocess\n\n\ndef hierarchical_visualization(config):\n    output_dir = config[\"output_dir\"]\n    cwd = \"../report\"\n    command = f\"REPORT={output_dir} npm run build\"\n\n    try:\n        process = subprocess.Popen(\n            command,\n            shell=True,\n            cwd=cwd,\n            stdout=subprocess.PIPE,\n            stderr=subprocess.PIPE,\n            universal_newlines=True,\n        )\n        while True:\n            output_line = process.stdout.readline()\n            if output_line == \"\" and process.poll() is not None:\n                break\n            if output_line:\n                print(output_line.strip())\n        process.wait()\n        errors = process.stderr.read()\n        if errors:\n            print(\"Errors:\")\n            print(errors)\n    except subprocess.CalledProcessError as e:\n        print(\"Error: \", e)\n"},"plan":[{"step":"extraction","run":true,"reason":"not trace of previous run"},{"step":"embedding","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_clustering","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_initial_labelling","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_merge_labelling","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_overview","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_aggregation","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_visualization","run":false,"reason":"skipping html output"}],"status":"running","start_time":"2026-01-31T06:13:30.416220","completed_jobs":[{"step":"extraction","completed":"2026-01-31T06:17:17.624881","duration":227.202225,"params":{"prompt":"あなたは専門的なリサーチアシスタントです。与えられたテキストから、意見を抽出して整理してください。\n\n# 指示\n* 入出力の例に記載したような形式で文字列のリストを返してください\n  * 必要な場合は2つの別個の意見に分割してください。多くの場合は1つの議論にまとめる方が望ましいです。\n* 整理した意見は日本語で出力してください\n\n## 入出力の例\n/human\n\nAIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\"\n  ]\n}\n\n/human\n\nAIの能力、限界、倫理的考慮事項について、市民を教育する必要がある。また、教育できる人材を養成する必要がある。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIの能力、限界、倫理的考慮事項について、市民を教育すべき\",\n    \"AIに関する教育をできる人材を養成すべき\"\n  ]\n}\n\n/human\n\nAIはエネルギーグリッドを最適化し、無駄や炭素排出を削減できます。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIはエネルギーグリッドを最適化して炭素排出を削減できる\"\n  ]\n}\n","workers":30,"limit":508,"properties":[],"categories":{},"category_batch_size":5,"source_code":"$20","model":"gpt-4o-mini"},"token_usage":59518},{"step":"embedding","completed":"2026-01-31T06:17:23.635257","duration":5.998683,"params":{"model":"text-embedding-3-small","source_code":"$21"},"token_usage":0},{"step":"hierarchical_clustering","completed":"2026-01-31T09:19:56.060583","duration":10952.410298,"params":{"cluster_nums":[8,64],"source_code":"$22"},"token_usage":0},{"step":"hierarchical_initial_labelling","completed":"2026-01-31T09:21:34.116754","duration":98.048053,"params":{"prompt":"あなたはKJ法が得意なデータ分析者です。userのinputはグループに集まったラベルです。なぜそのラベルが一つのグループであるか解説し、表札（label）をつけてください。\n表札については、グループ内の具体的な論点や特徴を反映した、具体性の高い名称を考案してください。\n出力はJSONとし、フォーマットは以下のサンプルを参考にしてください。\n\n\n# サンプルの入出力\n## 入力例\n- 手作業での意見分析は時間がかかりすぎる。AIで効率化できると嬉しい\n- 今のやり方だと分析に工数がかかりすぎるけど、AIならコストをかけずに分析できそう\n- AIが自動で意見を整理してくれると楽になって嬉しい\n\n\n## 出力例\n{\n    \"label\": \"AIによる業務効率の大幅向上とコスト効率化\",\n    \"description\": \"この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$23","model":"gpt-4o-mini"},"token_usage":44013},{"step":"hierarchical_merge_labelling","completed":"2026-01-31T09:22:22.006166","duration":47.882961,"params":{"prompt":"あなたはデータ分析のエキスパートです。\n現在、テキストデータの階層クラスタリングを行っています。\n下層のクラスタ（意見グループ）のタイトルと説明、およびそれらのクラスタが所属する上層のクラスタのテキストのサンプルを与えるので、上層のクラスタのタイトルと説明を作成してください。\n\n# 指示\n- 統合後のクラスタ名は、統合前のクラスタ名称をそのまま引用せず、内容に基づいた新たな名称にしてください。\n- タイトルには、具体的な事象・行動（例：地域ごとの迅速対応、復興計画の着実な進展、効果的な情報共有・地域協力など）を含めてください\n  - 可能な限り具体的な表現を用いるようにし、抽象的な表現は避けてください\n    - 「多様な意見」などの抽象的な表現は避けてください\n- 出力例に示したJSON形式で出力してください\n\n\n# サンプルの入出力\n## 入力例\n- 「顧客フィードバックの自動集約」: この意見グループは、SNSやオンラインレビューなどから集めた大量の意見をAIが瞬時に解析し、企業が市場のトレンドや顧客の要望を即時に把握できる点についての期待を示しています。\n- 「AIによる業務効率の大幅向上とコスト効率化」: この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\n\n## 出力例\n{\n    \"label\": \"AI技術の導入による意見分析の効率化への期待\",\n    \"description\": \"大量の意見やフィードバックから迅速に洞察を抽出できるため、企業や自治体が消費者や市民の声を的確に把握し、戦略的な意思決定やサービス改善が可能になります。また、従来の手法と比べて作業負荷が軽減され、業務効率の向上やコスト削減といった実際の便益が得られると期待されています。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$24","model":"gpt-4o-mini"},"token_usage":17403},{"step":"hierarchical_overview","completed":"2026-01-31T09:22:24.352615","duration":2.338406,"params":{"prompt":"/system \n\nあなたはシンクタンクで働く専門のリサーチアシスタントです。\nチームは特定のテーマに関してパブリック・コンサルテーションを実施し、異なる選択肢の意見グループを分析し始めています。\nこれから意見グループのリストとその簡単な分析が提供されます。\nあなたの仕事は、調査結果の簡潔な要約を返すことです。要約は非常に簡潔に（最大で1段落、最大4文）まとめ、無意味な言葉を避けてください。\n出力は日本語で行ってください。\n","source_code":"$25","model":"gpt-4o-mini"},"token_usage":1776}],"total_token_usage":122710,"token_usage_input":109842,"token_usage_output":12868,"lock_until":"2026-01-31T09:27:24.361015","current_job":"hierarchical_aggregation","current_job_started":"2026-01-31T09:22:24.360984","estimated_cost":0.0241971,"current_job_progress":null,"current_jop_tasks":null},"comment_num":508,"visibility":"public"}}],"$L26","$L27","$L28","$L29"]}],"$L2a"]
2b:I[49985,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Analysis"]
2c:I[68443,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Separator"]
2e:I[27787,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Footer"]
26:["$","$L2b",null,{"result":"$8:1:props:children:1:props:result"}]
27:["$","$L14",null,{"w":"fit-content","mx":"auto","children":["$","$L6",null,{"href":"/","children":["$","$L7",null,{"variant":"outline","size":"md","children":[["$","svg",null,{"ref":"$undefined","xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-chevron-left","children":[["$","path","1wnfg3",{"d":"m15 18-6-6 6-6"}],"$undefined"]}],"一覧へ戻る"]}]}]}]
28:["$","$L2c",null,{"my":12,"maxW":"750px","mx":"auto"}]
29:["$","$L14",null,{"maxW":"750px","mx":"auto","mb":24,"children":"$L2d"}]
2a:["$","$L2e",null,{"meta":{"reporter":"「#みんなでつくる舞鶴2040」プロジェクト","message":"この取組では、みなさま一人ひとりの「2040年の舞鶴ってこうなってほしいな」「こんなことやってみたい！」といった想いを募集します。","webLink":"/","privacyLink":"/","termsLink":null,"brandColor":"#e7adb7","isDefault":false}}]
2f:I[24982,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"ReporterContent"]
2d:["$","$L2f",null,{"meta":"$2a:props:meta","children":"$L30"}]
31:I[21863,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Image"]
30:["$","$L31",null,{"src":"/kouchouAI-reports/meta/reporter.png","alt":"「#みんなでつくる舞鶴2040」プロジェクト","maxW":"150px"}]
c:{"metadata":[["$","title","0",{"children":"③市民として、2040年のありたい姿とは【1/31 15:00更新】 - 「#みんなでつくる舞鶴2040」プロジェクト"}],["$","meta","1",{"name":"description","content":"舞鶴市の地域振興に関する意見は、住民の幸福や地域の魅力向上を重視し、世代を超えた共生や持続可能な社会の構築が求められています。相互支援やポジティブな人間関係の形成、快適な生活環境の整備が強調され、特に子どもや高齢者に配慮した施策が重要視されています。また、共感や楽しさを重視し、自立した多様な生き方を支える社会の実現が期待されています。"}],["$","meta","2",{"property":"og:title","content":"③市民として、2040年のありたい姿とは【1/31 15:00更新】 - 「#みんなでつくる舞鶴2040」プロジェクト"}],["$","meta","3",{"property":"og:description","content":"舞鶴市の地域振興に関する意見は、住民の幸福や地域の魅力向上を重視し、世代を超えた共生や持続可能な社会の構築が求められています。相互支援やポジティブな人間関係の形成、快適な生活環境の整備が強調され、特に子どもや高齢者に配慮した施策が重要視されています。また、共感や楽しさを重視し、自立した多様な生き方を支える社会の実現が期待されています。"}],["$","meta","4",{"property":"og:image","content":"http://localhost:3000/2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77/opengraph-image.png"}],["$","meta","5",{"name":"twitter:card","content":"summary_large_image"}],["$","meta","6",{"name":"twitter:title","content":"③市民として、2040年のありたい姿とは【1/31 15:00更新】 - 「#みんなでつくる舞鶴2040」プロジェクト"}],["$","meta","7",{"name":"twitter:description","content":"舞鶴市の地域振興に関する意見は、住民の幸福や地域の魅力向上を重視し、世代を超えた共生や持続可能な社会の構築が求められています。相互支援やポジティブな人間関係の形成、快適な生活環境の整備が強調され、特に子どもや高齢者に配慮した施策が重要視されています。また、共感や楽しさを重視し、自立した多様な生き方を支える社会の実現が期待されています。"}],["$","meta","8",{"name":"twitter:image","content":"http://localhost:3000/2e375c4c-a0ec-4ffb-a3e2-8f08f4ee6e77/opengraph-image.png"}]],"error":null,"digest":"$undefined"}
11:"$c:metadata"
