1:"$Sreact.fragment"
2:I[16501,["771","static/chunks/771-6df7383af9cb22cb.js","732","static/chunks/732-1930f1f7bd6746a5.js","13","static/chunks/13-f12e2db1f59fda05.js","355","static/chunks/355-f98f115f8c6f56c6.js","177","static/chunks/app/layout-be7f58cf6f9b9f21.js"],"Provider"]
3:I[9766,[],""]
4:I[98924,[],""]
5:I[58923,["771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","335","static/chunks/app/%5Bslug%5D/error-b2784470a4722ef4.js"],"default"]
6:I[52619,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],""]
7:I[54921,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Button"]
9:I[24431,[],"OutletBoundary"]
b:I[15278,[],"AsyncMetadataOutlet"]
d:I[24431,[],"ViewportBoundary"]
f:I[24431,[],"MetadataBoundary"]
10:"$Sreact.suspense"
12:I[57150,[],""]
:HL["/kouchouAI-reports/_next/static/css/a63443551c7d7d9f.css","style"]
0:{"P":null,"b":"-Ec3yNVR6tcE1Pi-oBI6I","p":"/kouchouAI-reports","c":["","2455a1d9-5bf1-495f-a02d-67ca8ac37f0f",""],"i":false,"f":[[["",{"children":[["slug","2455a1d9-5bf1-495f-a02d-67ca8ac37f0f","d"],{"children":["__PAGE__",{}]}]},"$undefined","$undefined",true],["",["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/kouchouAI-reports/_next/static/css/a63443551c7d7d9f.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]],["$","html",null,{"suppressHydrationWarning":true,"lang":"ja","children":[["$","head",null,{"children":[["$","link",null,{"rel":"preconnect","href":"https://fonts.googleapis.com"}],["$","link",null,{"rel":"preconnect","href":"https://fonts.gstatic.com","crossOrigin":"anonymous"}],["$","link",null,{"href":"https://fonts.googleapis.com/css2?family=BIZ+UDPGothic&display=swap","rel":"stylesheet"}],["$","link",null,{"rel":"icon","href":"/kouchouAI-reports/meta/icon.png","sizes":"any"}],false]}],["$","body",null,{"children":["$","$L2",null,{"children":["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","title",null,{"children":"404: This page could not be found."}],["$","div",null,{"style":{"fontFamily":"system-ui,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif,\"Apple Color Emoji\",\"Segoe UI Emoji\"","height":"100vh","textAlign":"center","display":"flex","flexDirection":"column","alignItems":"center","justifyContent":"center"},"children":["$","div",null,{"children":[["$","style",null,{"dangerouslySetInnerHTML":{"__html":"body{color:#000;background:#fff;margin:0}.next-error-h1{border-right:1px solid rgba(0,0,0,.3)}@media (prefers-color-scheme:dark){body{color:#fff;background:#000}.next-error-h1{border-right:1px solid rgba(255,255,255,.3)}}"}}],["$","h1",null,{"className":"next-error-h1","style":{"display":"inline-block","margin":"0 20px 0 0","padding":"0 23px 0 0","fontSize":24,"fontWeight":500,"verticalAlign":"top","lineHeight":"49px"},"children":404}],["$","div",null,{"style":{"display":"inline-block"},"children":["$","h2",null,{"style":{"fontSize":14,"fontWeight":400,"lineHeight":"49px","margin":0},"children":"This page could not be found."}]}]]}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]}]}]]}]]}],{"children":[["slug","2455a1d9-5bf1-495f-a02d-67ca8ac37f0f","d"],["$","$1","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$5","errorStyles":[],"errorScripts":[],"template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","p",null,{"children":"ページが見つかりませんでした"}],["$","$L6",null,{"href":"/","children":["$","$L7",null,{"children":"トップに戻る"}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":["__PAGE__",["$","$1","c",{"children":["$L8",null,["$","$L9",null,{"children":["$La",["$","$Lb",null,{"promise":"$@c"}]]}]]}],{},null,false]},null,false]},null,false],["$","$1","h",{"children":[null,[["$","$Ld",null,{"children":"$Le"}],null],["$","$Lf",null,{"children":["$","div",null,{"hidden":true,"children":["$","$10",null,{"fallback":null,"children":"$L11"}]}]}]]}],false]],"m":"$undefined","G":["$12",[]],"s":false,"S":true}
e:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
a:null
13:I[67733,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Header"]
14:I[99347,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Box"]
15:I[55756,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Heading"]
16:I[48409,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Text"]
17:I[92091,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Icon"]
18:I[6026,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"ClientContainer"]
19:T194c,import concurrent.futures
import json
import logging
import os
import re

import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai
from services.parse_json_list import parse_extraction_response
from utils import update_progress

COMMA_AND_SPACE_AND_RIGHT_BRACKET = re.compile(r",\s*(\])")


class ExtractionResponse(BaseModel):
    extractedOpinionList: list[str] = Field(..., description="抽出した意見のリスト")


def _validate_property_columns(property_columns: list[str], comments: pd.DataFrame) -> None:
    if not all(property in comments.columns for property in property_columns):
        raise ValueError(f"Properties {property_columns} not found in comments. Columns are {comments.columns}")


def extraction(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/args.csv"
    model = config["extraction"]["model"]
    prompt = config["extraction"]["prompt"]
    workers = config["extraction"]["workers"]
    limit = config["extraction"]["limit"]
    property_columns = config["extraction"]["properties"]

    if "provider" not in config:
        raise RuntimeError("provider is not set")
    provider = config["provider"]

    # カラム名だけを読み込み、必要なカラムが含まれているか確認する
    comments = pd.read_csv(f"inputs/{config['input']}.csv", nrows=0)
    _validate_property_columns(property_columns, comments)
    # エラーが出なかった場合、すべての行を読み込む
    comments = pd.read_csv(
        f"inputs/{config['input']}.csv", usecols=["comment-id", "comment-body"] + config["extraction"]["properties"]
    )
    comment_ids = (comments["comment-id"].values)[:limit]
    comments.set_index("comment-id", inplace=True)
    results = pd.DataFrame()
    update_progress(config, total=len(comment_ids))

    argument_map = {}
    relation_rows = []

    for i in tqdm(range(0, len(comment_ids), workers)):
        batch = comment_ids[i : i + workers]
        batch_inputs = [comments.loc[id]["comment-body"] for id in batch]
        batch_results = extract_batch(
            batch_inputs, prompt, model, workers, provider, config.get("local_llm_address"), config
        )

        for comment_id, extracted_args in zip(batch, batch_results, strict=False):
            for j, arg in enumerate(extracted_args):
                if arg not in argument_map:
                    # argumentテーブルに追加
                    arg_id = f"A{comment_id}_{j}"
                    argument = arg
                    argument_map[arg] = {
                        "arg-id": arg_id,
                        "argument": argument,
                    }
                else:
                    arg_id = argument_map[arg]["arg-id"]

                # relationテーブルにcommentとargの関係を追加
                relation_row = {
                    "arg-id": arg_id,
                    "comment-id": comment_id,
                }
                relation_rows.append(relation_row)

        update_progress(config, incr=len(batch))

    # DataFrame化
    results = pd.DataFrame(argument_map.values())
    relation_df = pd.DataFrame(relation_rows)

    if results.empty:
        raise RuntimeError("result is empty, maybe bad prompt")

    results.to_csv(path, index=False)
    # comment-idとarg-idの関係を保存
    relation_df.to_csv(f"outputs/{dataset}/relations.csv", index=False)


logging.basicConfig(level=logging.DEBUG)


def extract_batch(batch, prompt, model, workers, provider="openai", local_llm_address=None, config=None):
    with concurrent.futures.ThreadPoolExecutor(max_workers=workers) as executor:
        futures_with_index = [
            (i, executor.submit(extract_arguments, input, prompt, model, provider, local_llm_address))
            for i, input in enumerate(batch)
        ]

        done, not_done = concurrent.futures.wait([f for _, f in futures_with_index], timeout=30)
        results = [[] for _ in range(len(batch))]
        total_token_input = 0
        total_token_output = 0
        total_token_usage = 0

        for _, future in futures_with_index:
            if future in not_done and not future.cancelled():
                future.cancel()

        for i, future in futures_with_index:
            if future in done:
                try:
                    result = future.result()
                    if isinstance(result, tuple) and len(result) == 4:
                        items, token_input, token_output, token_total = result
                        results[i] = items
                        total_token_input += token_input
                        total_token_output += token_output
                        total_token_usage += token_total
                    else:
                        results[i] = result
                except Exception as e:
                    logging.error(f"Task {future} failed with error: {e}")
                    results[i] = []

        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + total_token_usage
            config["token_usage_input"] = config.get("token_usage_input", 0) + total_token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + total_token_output
            print(
                f"Extraction batch: input={total_token_input}, output={total_token_output}, total={total_token_usage} tokens"
            )

        return results


def extract_arguments(input, prompt, model, provider="openai", local_llm_address=None):
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            is_json=False,
            json_schema=ExtractionResponse,
            provider=provider,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )
        items = parse_extraction_response(response)
        items = list(filter(None, items))  # omit empty strings
        return items, token_input, token_output, token_total
    except json.decoder.JSONDecodeError as e:
        print("JSON error:", e)
        print("Input was:", input)
        print("Response was:", response)
        print("Silently giving up on trying to generate valid list.")
        return []
1a:T1149,"""Cluster the arguments using UMAP + HDBSCAN and GPT-4."""

from importlib import import_module

import numpy as np
import pandas as pd
import scipy.cluster.hierarchy as sch
from sklearn.cluster import KMeans


def hierarchical_clustering(config):
    UMAP = import_module("umap").UMAP

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_clusters.csv"
    arguments_df = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings_df = pd.read_pickle(f"outputs/{dataset}/embeddings.pkl")
    embeddings_array = np.asarray(embeddings_df["embedding"].values.tolist())
    cluster_nums = config["hierarchical_clustering"]["cluster_nums"]

    n_samples = embeddings_array.shape[0]
    # デフォルト設定は15
    default_n_neighbors = 15

    # テスト等サンプルが少なすぎる場合、n_neighborsの設定値を下げる
    if n_samples <= default_n_neighbors:
        n_neighbors = max(2, n_samples - 1)  # 最低2以上
    else:
        n_neighbors = default_n_neighbors

    umap_model = UMAP(random_state=42, n_components=2, n_neighbors=n_neighbors)
    # TODO 詳細エラーメッセージを加える
    # 以下のエラーの場合、おそらく元の意見件数が少なすぎることが原因
    # TypeError: Cannot use scipy.linalg.eigh for sparse A with k >= N. Use scipy.linalg.eigh(A.toarray()) or reduce k.
    umap_embeds = umap_model.fit_transform(embeddings_array)

    cluster_results = hierarchical_clustering_embeddings(
        umap_embeds=umap_embeds,
        cluster_nums=cluster_nums,
    )
    result_df = pd.DataFrame(
        {
            "arg-id": arguments_df["arg-id"],
            "argument": arguments_df["argument"],
            "x": umap_embeds[:, 0],
            "y": umap_embeds[:, 1],
        }
    )

    for cluster_level, final_labels in enumerate(cluster_results.values(), start=1):
        result_df[f"cluster-level-{cluster_level}-id"] = [f"{cluster_level}_{label}" for label in final_labels]

    result_df.to_csv(path, index=False)


def generate_cluster_count_list(min_clusters: int, max_clusters: int):
    cluster_counts = []
    current = min_clusters
    cluster_counts.append(current)

    if min_clusters == max_clusters:
        return cluster_counts

    while True:
        next_double = current * 2
        next_triple = current * 3

        if next_double >= max_clusters:
            if cluster_counts[-1] != max_clusters:
                cluster_counts.append(max_clusters)
            break

        # 次の倍はまだ max_clusters に収まるが、3倍だと超える
        # -> (次の倍は細かすぎるので)スキップして max_clusters に飛ぶ
        if next_triple > max_clusters:
            cluster_counts.append(max_clusters)
            break

        cluster_counts.append(next_double)
        current = next_double

    return cluster_counts


def merge_clusters_with_hierarchy(
    cluster_centers: np.ndarray,
    kmeans_labels: np.ndarray,
    umap_array: np.ndarray,
    n_cluster_cut: int,
):
    Z = sch.linkage(cluster_centers, method="ward")
    cluster_labels_merged = sch.fcluster(Z, t=n_cluster_cut, criterion="maxclust")

    n_samples = umap_array.shape[0]
    final_labels = np.zeros(n_samples, dtype=int)

    for i in range(n_samples):
        original_label = kmeans_labels[i]
        final_labels[i] = cluster_labels_merged[original_label]

    return final_labels


def hierarchical_clustering_embeddings(
    umap_embeds,
    cluster_nums,
):
    # 最大分割数でクラスタリングを実施
    print("start initial clustering")
    initial_cluster_num = cluster_nums[-1]
    kmeans_model = KMeans(n_clusters=initial_cluster_num, random_state=42)
    kmeans_model.fit(umap_embeds)
    print("end initial clustering")

    results = {}
    print("start hierarchical clustering")
    cluster_nums.sort()
    print(cluster_nums)
    for n_cluster_cut in cluster_nums[:-1]:
        print("n_cluster_cut: ", n_cluster_cut)
        final_labels = merge_clusters_with_hierarchy(
            cluster_centers=kmeans_model.cluster_centers_,
            kmeans_labels=kmeans_model.labels_,
            umap_array=umap_embeds,
            n_cluster_cut=n_cluster_cut,
        )
        results[n_cluster_cut] = final_labels

    results[initial_cluster_num] = kmeans_model.labels_
    print("end hierarchical clustering")

    return results
1b:T1c36,import json
import os
from concurrent.futures import ThreadPoolExecutor
from functools import partial
from typing import TypedDict

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class LabellingResult(TypedDict):
    """各クラスタのラベリング結果を表す型"""

    cluster_id: str  # クラスタのID
    label: str  # クラスタのラベル名
    description: str  # クラスタの説明文


def hierarchical_initial_labelling(config: dict) -> None:
    """階層的クラスタリングの初期ラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_initial_labelling: 初期ラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_initial_labels.csv"
    clusters_argument_df = pd.read_csv(f"outputs/{dataset}/hierarchical_clusters.csv")

    cluster_id_columns = [col for col in clusters_argument_df.columns if col.startswith("cluster-level-")]
    initial_cluster_id_column = cluster_id_columns[-1]
    sampling_num = config["hierarchical_initial_labelling"]["sampling_num"]
    initial_labelling_prompt = config["hierarchical_initial_labelling"]["prompt"]
    model = config["hierarchical_initial_labelling"]["model"]
    workers = config["hierarchical_initial_labelling"]["workers"]

    # トークン使用量を追跡するための変数を初期化
    config["total_token_usage"] = config.get("total_token_usage", 0)

    initial_label_df = initial_labelling(
        initial_labelling_prompt,
        clusters_argument_df,
        sampling_num,
        model,
        workers,
        config["provider"],
        config.get("local_llm_address"),
        config,  # configを渡して、トークン使用量を累積できるようにする
    )
    print("start initial labelling")
    initial_clusters_argument_df = clusters_argument_df.merge(
        initial_label_df,
        left_on=initial_cluster_id_column,
        right_on="cluster_id",
        how="left",
    ).rename(
        columns={
            "label": f"{initial_cluster_id_column.replace('-id', '')}-label",
            "description": f"{initial_cluster_id_column.replace('-id', '')}-description",
        }
    )
    print("end initial labelling")
    initial_clusters_argument_df.to_csv(path, index=False)


def initial_labelling(
    prompt: str,
    clusters_df: pd.DataFrame,
    sampling_num: int,
    model: str,
    workers: int,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> pd.DataFrame:
    """各クラスタに対して初期ラベリングを実行する

    Args:
        prompt: LLMへのプロンプト
        clusters_df: クラスタリング結果のDataFrame
        sampling_num: 各クラスタからサンプリングする意見の数
        model: 使用するLLMモデル名
        workers: 並列処理のワーカー数
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        各クラスタのラベリング結果を含むDataFrame
    """
    cluster_columns = [col for col in clusters_df.columns if col.startswith("cluster-level-")]
    initial_cluster_column = cluster_columns[-1]
    cluster_ids = clusters_df[initial_cluster_column].unique()
    process_func = partial(
        process_initial_labelling,
        df=clusters_df,
        prompt=prompt,
        sampling_num=sampling_num,
        target_column=initial_cluster_column,
        model=model,
        provider=provider,
        local_llm_address=local_llm_address,
        config=config,  # configを渡す
    )
    with ThreadPoolExecutor(max_workers=workers) as executor:
        results = list(executor.map(process_func, cluster_ids))
    return pd.DataFrame(results)


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_initial_labelling(
    cluster_id: str,
    df: pd.DataFrame,
    prompt: str,
    sampling_num: int,
    target_column: str,
    model: str,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> LabellingResult:
    """個別のクラスタに対してラベリングを実行する

    Args:
        cluster_id: 処理対象のクラスタID
        df: クラスタリング結果のDataFrame
        prompt: LLMへのプロンプト
        sampling_num: サンプリングする意見の数
        target_column: クラスタIDが格納されている列名
        model: 使用するLLMモデル名
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        クラスタのラベリング結果
    """
    cluster_data = df[df[target_column] == cluster_id]
    sampling_num = min(sampling_num, len(cluster_data))
    cluster = cluster_data.sample(sampling_num)
    input = "\n".join(cluster["argument"].values)
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            provider=provider,
            json_schema=LabellingFromat,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )

        # トークン使用量を累積（configが渡されている場合）
        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
            config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + token_output

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return LabellingResult(
            cluster_id=cluster_id,
            label=response_json.get("label", "エラーでラベル名が取得できませんでした"),
            description=response_json.get("description", "エラーで解説が取得できませんでした"),
        )
    except Exception as e:
        print(e)
        return LabellingResult(
            cluster_id=cluster_id,
            label="エラーでラベル名が取得できませんでした",
            description="エラーで解説が取得できませんでした",
        )
1c:T337b,import json
import os
from concurrent.futures import ThreadPoolExecutor
from dataclasses import dataclass
from functools import partial

import numpy as np
import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai


@dataclass
class ClusterColumns:
    """同一階層のクラスター関連のカラム名を管理するクラス"""

    id: str
    label: str
    description: str

    @classmethod
    def from_id_column(cls, id_column: str) -> "ClusterColumns":
        """ID列名から関連するカラム名を生成"""
        return cls(
            id=id_column,
            label=id_column.replace("-id", "-label"),
            description=id_column.replace("-id", "-description"),
        )


@dataclass
class ClusterValues:
    """対象クラスタのlabel/descriptionを管理するクラス"""

    label: str
    description: str

    def to_prompt_text(self) -> str:
        return f"- {self.label}: {self.description}"


def hierarchical_merge_labelling(config: dict) -> None:
    """階層的クラスタリングの結果に対してマージラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_merge_labelling: マージラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    merge_path = f"outputs/{dataset}/hierarchical_merge_labels.csv"
    clusters_df = pd.read_csv(f"outputs/{dataset}/hierarchical_initial_labels.csv")

    cluster_id_columns: list[str] = _filter_id_columns(clusters_df.columns)
    # ボトムクラスタのラベル・説明とクラスタid付きの各argumentを入力し、各階層のクラスタラベル・説明を生成し、argumentに付けたdfを作成
    merge_result_df = merge_labelling(
        clusters_df=clusters_df,
        cluster_id_columns=sorted(cluster_id_columns, reverse=True),
        config=config,
    )
    # 上記のdfから各クラスタのlevel, id, label, description, valueを取得してdfを作成
    melted_df = melt_cluster_data(merge_result_df)
    # 上記のdfに親子関係を追加
    parent_child_df = _build_parent_child_mapping(merge_result_df, cluster_id_columns)
    melted_df = melted_df.merge(parent_child_df, on=["level", "id"], how="left")
    density_df = calculate_cluster_density(melted_df, config)
    density_df.to_csv(merge_path, index=False)


def _build_parent_child_mapping(df: pd.DataFrame, cluster_id_columns: list[str]):
    """クラスタ間の親子関係をマッピングする

    Args:
        df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト

    Returns:
        親子関係のマッピング情報を含むDataFrame
    """
    results = []
    top_cluster_column = cluster_id_columns[0]
    top_cluster_values = df[top_cluster_column].unique()
    for c in top_cluster_values:
        results.append(
            {
                "level": 1,
                "id": c,
                "parent": "0",  # aggregationで追加する全体クラスタのid
            }
        )

    for idx in range(len(cluster_id_columns) - 1):
        current_column = cluster_id_columns[idx]
        children_column = cluster_id_columns[idx + 1]
        current_level = current_column.replace("-id", "").replace("cluster-level-", "")
        # 現在のレベルのクラスタid
        current_cluster_values = df[current_column].unique()
        for current_id in current_cluster_values:
            children_ids = df.loc[df[current_column] == current_id, children_column].unique()
            for child_id in children_ids:
                results.append(
                    {
                        "level": int(current_level) + 1,
                        "id": child_id,
                        "parent": current_id,
                    }
                )
    return pd.DataFrame(results)


def _filter_id_columns(columns: list[str]) -> list[str]:
    """クラスタIDのカラム名をフィルタリングする

    Args:
        columns: 全カラム名のリスト

    Returns:
        クラスタIDのカラム名のリスト
    """
    return [col for col in columns if col.startswith("cluster-level-") and col.endswith("-id")]


def melt_cluster_data(df: pd.DataFrame) -> pd.DataFrame:
    """クラスタデータを行形式に変換する

    cluster-level-n-(id|label|description) を行形式 (level, id, label, description, value) にまとめる。
    [cluster-level-n-id, cluster-level-n-label, cluster-level-n-description] を [level, id, label, description, value(件数)] に変換する。

    Args:
        df: クラスタリング結果のDataFrame

    Returns:
        行形式に変換されたDataFrame
    """
    id_columns: list[str] = _filter_id_columns(df.columns)
    levels: set[int] = {int(col.replace("cluster-level-", "").replace("-id", "")) for col in id_columns}
    all_rows: list[dict] = []

    # levelごとに各クラスタの出現件数を集計・縦持ちにする
    for level in levels:
        cluster_columns = ClusterColumns.from_id_column(f"cluster-level-{level}-id")
        # クラスタidごとの件数集計
        level_count_df = df.groupby(cluster_columns.id).size().reset_index(name="value")

        level_unique_val_df = df[
            [cluster_columns.id, cluster_columns.label, cluster_columns.description]
        ].drop_duplicates()
        level_unique_val_df = level_unique_val_df.merge(level_count_df, on=cluster_columns.id, how="left")
        level_unique_vals = [
            {
                "level": level,
                "id": row[cluster_columns.id],
                "label": row[cluster_columns.label],
                "description": row[cluster_columns.description],
                "value": row["value"],
            }
            for _, row in level_unique_val_df.iterrows()
        ]
        all_rows.extend(level_unique_vals)
    return pd.DataFrame(all_rows)


def merge_labelling(clusters_df: pd.DataFrame, cluster_id_columns: list[str], config) -> pd.DataFrame:
    """階層的なクラスタのマージラベリングを実行する

    Args:
        clusters_df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含むDataFrame
    """
    for idx in tqdm(range(len(cluster_id_columns) - 1)):
        previous_columns = ClusterColumns.from_id_column(cluster_id_columns[idx])
        current_columns = ClusterColumns.from_id_column(cluster_id_columns[idx + 1])

        process_fn = partial(
            process_merge_labelling,
            result_df=clusters_df,
            current_columns=current_columns,
            previous_columns=previous_columns,
            config=config,
        )

        current_cluster_ids = sorted(clusters_df[current_columns.id].unique())
        with ThreadPoolExecutor(max_workers=config["hierarchical_merge_labelling"]["workers"]) as executor:
            responses = list(
                tqdm(
                    executor.map(process_fn, current_cluster_ids),
                    total=len(current_cluster_ids),
                )
            )

        current_result_df = pd.DataFrame(responses)
        clusters_df = clusters_df.merge(current_result_df, on=[current_columns.id])
    return clusters_df


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_merge_labelling(
    target_cluster_id: str,
    result_df: pd.DataFrame,
    current_columns: ClusterColumns,
    previous_columns: ClusterColumns,
    config,
):
    """個別のクラスタに対してマージラベリングを実行する

    Args:
        target_cluster_id: 処理対象のクラスタID
        result_df: クラスタリング結果のDataFrame
        current_columns: 現在のレベルのカラム情報
        previous_columns: 前のレベルのカラム情報
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含む辞書
    """

    def filter_previous_values(df: pd.DataFrame, previous_columns: ClusterColumns) -> list[ClusterValues]:
        """前のレベルのクラスタ情報を取得する"""
        previous_records = df[df[current_columns.id] == target_cluster_id][
            [previous_columns.label, previous_columns.description]
        ].drop_duplicates()
        previous_values = [
            ClusterValues(
                label=row[previous_columns.label],
                description=row[previous_columns.description],
            )
            for _, row in previous_records.iterrows()
        ]
        return previous_values

    previous_values = filter_previous_values(result_df, previous_columns)
    if len(previous_values) == 1:
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: previous_values[0].label,
            current_columns.description: previous_values[0].description,
        }
    elif len(previous_values) == 0:
        raise ValueError(f"クラスタ {target_cluster_id} には前のレベルのクラスタが存在しません。")

    current_cluster_data = result_df[result_df[current_columns.id] == target_cluster_id]
    sampling_num = min(
        config["hierarchical_merge_labelling"]["sampling_num"],
        len(current_cluster_data),
    )
    sampled_data = current_cluster_data.sample(sampling_num)
    sampled_argument_text = "\n".join(sampled_data["argument"].values)
    cluster_text = "\n".join([value.to_prompt_text() for value in previous_values])
    messages = [
        {"role": "system", "content": config["hierarchical_merge_labelling"]["prompt"]},
        {
            "role": "user",
            "content": "クラスタラベル\n" + cluster_text + "\n" + "クラスタの意見\n" + sampled_argument_text,
        },
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=config["hierarchical_merge_labelling"]["model"],
            json_schema=LabellingFromat,
            provider=config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )

        config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
        config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
        config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
        print(f"Merge labelling: input={token_input}, output={token_output}, total={token_total} tokens")

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: response_json.get("label", "エラーでラベル名が取得できませんでした"),
            current_columns.description: response_json.get("description", "エラーで解説が取得できませんでした"),
        }
    except Exception as e:
        print(f"エラーが発生しました: {e}")
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: "エラーでラベル名が取得できませんでした",
            current_columns.description: "エラーで解説が取得できませんでした",
        }


def calculate_cluster_density(melted_df: pd.DataFrame, config: dict):
    """クラスタ内の密度計算"""
    hierarchical_cluster_df = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_clusters.csv")

    densities = []
    for level, c_id in zip(melted_df["level"], melted_df["id"], strict=False):
        cluster_embeds = hierarchical_cluster_df[hierarchical_cluster_df[f"cluster-level-{level}-id"] == c_id][
            ["x", "y"]
        ].values
        density = calculate_density(cluster_embeds)
        densities.append(density)

    # 密度のランクを計算
    melted_df["density"] = densities
    melted_df["density_rank"] = melted_df.groupby("level")["density"].rank(ascending=False, method="first")
    melted_df["density_rank_percentile"] = melted_df.groupby("level")["density_rank"].transform(lambda x: x / len(x))
    return melted_df


def calculate_density(embeds: np.ndarray):
    """平均距離に基づいて密度を計算"""
    center = np.mean(embeds, axis=0)
    distances = np.linalg.norm(embeds - center, axis=1)
    avg_distance = np.mean(distances)
    density = 1 / (avg_distance + 1e-10)
    return density
1d:Ta82,"""Create summaries for the clusters."""

import json
import os
import re

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class OverviewResponse(BaseModel):
    summary: str = Field(..., description="クラスターの全体的な要約")


def hierarchical_overview(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_overview.txt"

    hierarchical_label_df = pd.read_csv(f"outputs/{dataset}/hierarchical_merge_labels.csv")

    prompt = config["hierarchical_overview"]["prompt"]
    model = config["hierarchical_overview"]["model"]

    # TODO: level1で固定にしているが、設定で変えられるようにする
    target_level = 1
    target_records = hierarchical_label_df[hierarchical_label_df["level"] == target_level]
    ids = target_records["id"].to_list()
    labels = target_records["label"].to_list()
    descriptions = target_records["description"].to_list()
    target_records.set_index("id", inplace=True)

    input_text = ""
    for i, _ in enumerate(ids):
        input_text += f"# Cluster {i}/{len(ids)}: {labels[i]}\n\n"
        input_text += descriptions[i] + "\n\n"

    messages = [{"role": "system", "content": prompt}, {"role": "user", "content": input_text}]
    response_text, token_input, token_output, token_total = request_to_chat_ai(
        messages=messages,
        model=model,
        provider=config["provider"],
        local_llm_address=config.get("local_llm_address"),
        user_api_key=os.getenv("USER_API_KEY"),
        json_schema=OverviewResponse,
    )

    # トークン使用量を累積
    config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
    config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
    config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
    print(f"Hierarchical overview: input={token_input}, output={token_output}, total={token_total} tokens")

    try:
        # structured outputとしてパースできるなら処理する
        if isinstance(response_text, dict):
            parsed_response = response_text
        else:
            parsed_response = json.loads(response_text)

        with open(path, "w") as file:
            file.write(parsed_response["summary"])

    except Exception:
        # thinkタグが出力されるReasoningモデル用に、thinkタグを除去する
        thinking_removed = re.sub(
            r"<think\b[^>]*>.*?</think>",
            "",
            response_text,
            flags=re.DOTALL,
        )

        with open(path, "w") as file:
            file.write(thinking_removed)
1e:T3f38,"""Generate a convenient JSON output file."""

import json
from collections import defaultdict
from pathlib import Path
from typing import Any, TypedDict

import numpy as np
import pandas as pd

ROOT_DIR = Path(__file__).parent.parent.parent.parent
CONFIG_DIR = ROOT_DIR / "scatter" / "pipeline" / "configs"
PIPELINE_DIR = ROOT_DIR / "broadlistening" / "pipeline"


def json_serialize_numpy(obj: Any) -> Any:
    """
    Recursively convert NumPy data types to native Python types for JSON serialization.

    Args:
        obj: Any Python object which might contain NumPy data types

    Returns:
        The same object structure with NumPy types converted to Python native types
    """
    if isinstance(obj, np.integer):
        return int(obj)
    elif isinstance(obj, np.floating):
        return float(obj)
    elif isinstance(obj, np.ndarray):
        return obj.tolist()
    elif isinstance(obj, dict):
        return {k: json_serialize_numpy(v) for k, v in obj.items()}
    elif isinstance(obj, list):
        return [json_serialize_numpy(item) for item in obj]
    elif isinstance(obj, tuple):
        return tuple(json_serialize_numpy(item) for item in obj)
    else:
        return obj


class Argument(TypedDict):
    arg_id: str
    argument: str
    comment_id: str
    x: float
    y: float
    p: float
    cluster_ids: list[str]
    attributes: dict[str, str] | None
    url: str | None


class Cluster(TypedDict):
    level: int
    id: str
    label: str
    takeaway: str
    value: int
    parent: str
    density_rank_percentile: float | None


def hierarchical_aggregation(config) -> bool:
    try:
        path = f"outputs/{config['output_dir']}/hierarchical_result.json"
        results = {
            "arguments": [],
            "clusters": [],
            "comments": {},
            "propertyMap": {},
            "translations": {},
            "overview": "",
            "config": config,
        }

        arguments = pd.read_csv(f"outputs/{config['output_dir']}/args.csv")
        arguments.set_index("arg-id", inplace=True)
        arg_num = len(arguments)
        relation_df = pd.read_csv(f"outputs/{config['output_dir']}/relations.csv")
        comments = pd.read_csv(f"inputs/{config['input']}.csv")
        clusters = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_clusters.csv")
        labels = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_merge_labels.csv")

        hidden_properties_map: dict[str, list[str]] = config["hierarchical_aggregation"]["hidden_properties"]

        results["arguments"] = _build_arguments(clusters, comments, relation_df, config)
        results["clusters"] = _build_cluster_value(labels, arg_num)

        # results["comments"] = _build_comments_value(
        #     comments, arguments, hidden_properties_map
        # )
        results["comment_num"] = len(comments)
        results["translations"] = _build_translations(config)
        # 属性情報のカラムは、元データに対して指定したカラムとclassificationするカテゴリを合わせたもの
        results["propertyMap"] = _build_property_map(arguments, comments, hidden_properties_map, config)

        with open(f"outputs/{config['output_dir']}/hierarchical_overview.txt") as f:
            overview = f.read()
        print("overview")
        print(overview)
        results["overview"] = overview

        # Convert non-serializable NumPy types to native Python types
        results = json_serialize_numpy(results)

        with open(path, "w") as file:
            json.dump(results, file, indent=2, ensure_ascii=False)
        # TODO: サンプリングロジックを実装したいが、現状は全件抽出
        create_custom_intro(config)
        if config["is_pubcom"]:
            add_original_comments(labels, arguments, relation_df, clusters, config)
        return True
    except Exception as e:
        print("error")
        print(e)
        return False


def create_custom_intro(config):
    dataset = config["output_dir"]
    args_path = PIPELINE_DIR / f"outputs/{dataset}/args.csv"
    comments = pd.read_csv(PIPELINE_DIR / f"inputs/{config['input']}.csv")
    result_path = PIPELINE_DIR / f"outputs/{dataset}/hierarchical_result.json"

    input_count = len(comments)
    args_count = len(pd.read_csv(args_path))
    processed_num = min(input_count, config["extraction"]["limit"])

    print(f"Input count: {input_count}")
    print(f"Args count: {args_count}")

    # LLMプロバイダーとモデル名の判定
    def get_llm_provider_display():
        # configからプロバイダー情報を取得（優先）
        provider = config.get("provider", "openai")
        model = config.get("model", "unknown")

        # プロバイダー名をマッピング
        provider_names = {
            "openai": "OpenAI API",
            "azure": "Azure OpenAI API",
            "openrouter": "OpenRouter API",
            "local": "Local LLM",
        }

        provider_name = provider_names.get(provider, f"{provider} API")
        return f"{provider_name} ({model})"

    llm_provider = get_llm_provider_display()

    base_custom_intro = """{intro}
分析対象となったデータの件数は{processed_num}件で、これらのデータに対して{llm_provider}を用いて{args_count}件の意見（議論）を抽出し、クラスタリングを行った。
"""

    intro = config["intro"]
    custom_intro = base_custom_intro.format(
        intro=intro, processed_num=processed_num, args_count=args_count, llm_provider=llm_provider
    )

    with open(result_path) as f:
        result = json.load(f)
    result["config"]["intro"] = custom_intro
    with open(result_path, "w") as f:
        json.dump(result, f, indent=2, ensure_ascii=False)


def add_original_comments(labels, arguments, relation_df, clusters, config):
    # 大カテゴリ（cluster-level-1）に該当するラベルだけ抽出
    labels_lv1 = labels[labels["level"] == 1][["id", "label"]].rename(
        columns={"id": "cluster-level-1-id", "label": "category_label"}
    )

    # arguments と clusters をマージ（カテゴリ情報付与）
    merged = arguments.merge(clusters[["arg-id", "cluster-level-1-id"]], on="arg-id").merge(
        labels_lv1, on="cluster-level-1-id", how="left"
    )

    # relation_df と結合
    merged = merged.merge(relation_df, on="arg-id", how="left")

    # 元コメント取得
    comments = pd.read_csv(PIPELINE_DIR / f"inputs/{config['input']}.csv")
    comments["comment-id"] = comments["comment-id"].astype(str)
    merged["comment-id"] = merged["comment-id"].astype(str)

    # 元コメント本文などとマージ
    final_df = merged.merge(comments, on="comment-id", how="left")

    # 必要カラムのみ整形
    final_cols = ["comment-id", "comment-body", "arg-id", "argument", "cluster-level-1-id", "category_label"]

    # 基本カラム
    for col in ["x", "y", "source", "url"]:
        if col in comments.columns:
            final_cols.append(col)

    # 属性カラムを追加
    attribute_columns = []
    for col in comments.columns:
        # attributeプレフィックスが付いたカラムを探す
        if col.startswith("attribute_"):
            attribute_columns.append(col)
            final_cols.append(col)

    print(f"属性カラム検出: {attribute_columns}")

    # 必要なカラムだけ選択
    final_df = final_df[final_cols]
    final_df = final_df.rename(
        columns={
            "cluster-level-1-id": "category_id",
            "category_label": "category",
            "arg-id": "arg_id",
            "argument": "argument",
            "comment-body": "original-comment",
        }
    )

    # 保存
    final_df.to_csv(PIPELINE_DIR / f"outputs/{config['output_dir']}/final_result_with_comments.csv", index=False)


def _build_arguments(
    clusters: pd.DataFrame, comments: pd.DataFrame, relation_df: pd.DataFrame, config: dict
) -> list[Argument]:
    """
    Build the arguments list including attribute information from original comments

    Args:
        clusters: DataFrame containing cluster information for each argument
        comments: DataFrame containing original comments with attribute columns
        relation_df: DataFrame relating arguments to original comments
        config: Configuration dictionary containing enable_source_link setting
    """
    cluster_columns = [col for col in clusters.columns if col.startswith("cluster-level-") and "id" in col]

    # Prepare for merging with original comments to get attributes
    comments_copy = comments.copy()
    comments_copy["comment-id"] = comments_copy["comment-id"].astype(str)

    # Get argument to comment mapping
    arg_comment_map = {}
    if "comment-id" in relation_df.columns:
        relation_df["comment-id"] = relation_df["comment-id"].astype(str)
        arg_comment_map = dict(zip(relation_df["arg-id"], relation_df["comment-id"], strict=False))

    # Find attribute columns in comments dataframe
    attribute_columns = [col for col in comments.columns if col.startswith("attribute_")]
    print(f"属性カラム検出: {attribute_columns}")

    arguments: list[Argument] = []
    for _, row in clusters.iterrows():
        cluster_ids = ["0"]
        for cluster_column in cluster_columns:
            cluster_ids.append(str(row[cluster_column]))  # Convert to string to ensure serializable

        # Create base argument
        argument: Argument = {
            "arg_id": str(row["arg-id"]),  # Convert to string to ensure serializable
            "argument": str(row["argument"]),
            "x": float(row["x"]),  # Convert to native float
            "y": float(row["y"]),  # Convert to native float
            "p": 0,  # NOTE: 一旦全部0でいれる
            "cluster_ids": cluster_ids,
            "attributes": None,
            "url": None,
        }

        # Add attributes and URL if available
        if row["arg-id"] in arg_comment_map:
            comment_id = arg_comment_map[row["arg-id"]]
            comment_rows = comments_copy[comments_copy["comment-id"] == comment_id]

            if not comment_rows.empty:
                comment_row = comment_rows.iloc[0]

                # Add URL if available and enabled
                if config.get("enable_source_link", False) and "url" in comment_row and comment_row["url"] is not None:
                    argument["url"] = str(comment_row["url"])

                # Add attributes if available
                if attribute_columns:
                    attributes = {}
                    for attr_col in attribute_columns:
                        # Remove "attribute_" prefix for cleaner attribute names
                        attr_name = attr_col[len("attribute_") :]
                        # Convert potential numpy types to Python native types
                        attr_value = comment_row.get(attr_col, None)
                        if attr_value is not None:
                            if isinstance(attr_value, np.integer):
                                attr_value = int(attr_value)
                            elif isinstance(attr_value, np.floating):
                                attr_value = float(attr_value)
                            elif isinstance(attr_value, np.ndarray):
                                attr_value = attr_value.tolist()
                        attributes[attr_name] = attr_value

                    # Only add non-empty attributes
                    if any(v is not None for v in attributes.values()):
                        argument["attributes"] = attributes

        arguments.append(argument)
    return arguments


def _build_cluster_value(melted_labels: pd.DataFrame, total_num: int) -> list[Cluster]:
    results: list[Cluster] = [
        Cluster(
            level=0,
            id="0",
            label="全体",
            takeaway="",
            value=int(total_num),  # Convert to native int
            parent="",
            density_rank_percentile=0,
        )
    ]

    for _, melted_label in melted_labels.iterrows():
        # Convert potential NumPy types to native Python types
        level = (
            int(melted_label["level"]) if isinstance(melted_label["level"], int | np.integer) else melted_label["level"]
        )
        cluster_id = str(melted_label["id"])
        label = str(melted_label["label"])
        takeaway = str(melted_label["description"])
        value = (
            int(melted_label["value"]) if isinstance(melted_label["value"], int | np.integer) else melted_label["value"]
        )
        parent = str(melted_label.get("parent", "全体"))

        # Handle density_rank_percentile which might be None or a numeric value
        density_rank = melted_label.get("density_rank_percentile")
        if density_rank is not None:
            if isinstance(density_rank, float | np.floating):
                density_rank = float(density_rank)
            elif isinstance(density_rank, int | np.integer):
                density_rank = int(density_rank)

        cluster_value = Cluster(
            level=level,
            id=cluster_id,
            label=label,
            takeaway=takeaway,
            value=value,
            parent=parent,
            density_rank_percentile=density_rank,
        )
        results.append(cluster_value)
    return results


def _build_comments_value(
    comments: pd.DataFrame,
    arguments: pd.DataFrame,
    hidden_properties_map: dict[str, list[str]],
):
    comment_dict: dict[str, dict[str, str]] = {}
    useful_comment_ids = set(arguments["comment-id"].values)
    for _, row in comments.iterrows():
        id = row["comment-id"]
        if id in useful_comment_ids:
            res = {"comment": row["comment-body"]}
            should_skip = any(row[prop] in hidden_values for prop, hidden_values in hidden_properties_map.items())
            if should_skip:
                continue
            comment_dict[str(id)] = res

    return comment_dict


def _build_translations(config):
    languages = list(config.get("translation", {}).get("languages", []))
    if len(languages) > 0:
        with open(PIPELINE_DIR / f"outputs/{config['output_dir']}/translations.json") as f:
            translations = f.read()
        return json.loads(translations)
    return {}


def _build_property_map(
    arguments: pd.DataFrame, comments: pd.DataFrame, hidden_properties_map: dict[str, list[str]], config: dict
) -> dict[str, dict[str, str]]:
    property_columns = list(hidden_properties_map.keys()) + list(config["extraction"]["categories"].keys())
    property_map = defaultdict(dict)

    # 指定された property_columns が arguments に存在するかチェック
    missing_cols = [col for col in property_columns if col not in arguments.columns]
    if missing_cols:
        raise ValueError(
            f"指定されたカラム {missing_cols} が args.csv に存在しません。"
            "設定ファイルaggregation / hidden_propertiesから該当カラムを取り除いてください。"
        )

    for prop in property_columns:
        for arg_id, row in arguments.iterrows():
            # LLMによるcategory classificationがうまく行かず、NaNの場合はNoneにする
            value = row[prop] if not pd.isna(row[prop]) else None

            # Convert NumPy types to Python native types
            if value is not None:
                if isinstance(value, np.integer):
                    value = int(value)
                elif isinstance(value, np.floating):
                    value = float(value)
                elif isinstance(value, np.ndarray):
                    value = value.tolist()
                else:
                    # Convert any other types to string to ensure serialization
                    try:
                        value = str(value)
                    except Exception as e:
                        print(f"Error converting value to string: {e}")
                        value = None

            # Make sure arg_id is string
            str_arg_id = str(arg_id)
            property_map[prop][str_arg_id] = value

    return property_map
1f:T458,import os

import pandas as pd
from tqdm import tqdm

from services.llm import request_to_embed


def embedding(config):
    model = config["embedding"]["model"]
    is_embedded_at_local = config["is_embedded_at_local"]
    # print("start embedding")
    # print(f"embedding model: {model}, is_embedded_at_local: {is_embedded_at_local}")

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/embeddings.pkl"
    arguments = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings = []
    batch_size = 1000
    for i in tqdm(range(0, len(arguments), batch_size)):
        args = arguments["argument"].tolist()[i : i + batch_size]
        embeds = request_to_embed(
            args,
            model,
            is_embedded_at_local,
            config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )
        embeddings.extend(embeds)
    df = pd.DataFrame([{"arg-id": arguments.iloc[i]["arg-id"], "embedding": e} for i, e in enumerate(embeddings)])
    df.to_pickle(path)
20:T194c,import concurrent.futures
import json
import logging
import os
import re

import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai
from services.parse_json_list import parse_extraction_response
from utils import update_progress

COMMA_AND_SPACE_AND_RIGHT_BRACKET = re.compile(r",\s*(\])")


class ExtractionResponse(BaseModel):
    extractedOpinionList: list[str] = Field(..., description="抽出した意見のリスト")


def _validate_property_columns(property_columns: list[str], comments: pd.DataFrame) -> None:
    if not all(property in comments.columns for property in property_columns):
        raise ValueError(f"Properties {property_columns} not found in comments. Columns are {comments.columns}")


def extraction(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/args.csv"
    model = config["extraction"]["model"]
    prompt = config["extraction"]["prompt"]
    workers = config["extraction"]["workers"]
    limit = config["extraction"]["limit"]
    property_columns = config["extraction"]["properties"]

    if "provider" not in config:
        raise RuntimeError("provider is not set")
    provider = config["provider"]

    # カラム名だけを読み込み、必要なカラムが含まれているか確認する
    comments = pd.read_csv(f"inputs/{config['input']}.csv", nrows=0)
    _validate_property_columns(property_columns, comments)
    # エラーが出なかった場合、すべての行を読み込む
    comments = pd.read_csv(
        f"inputs/{config['input']}.csv", usecols=["comment-id", "comment-body"] + config["extraction"]["properties"]
    )
    comment_ids = (comments["comment-id"].values)[:limit]
    comments.set_index("comment-id", inplace=True)
    results = pd.DataFrame()
    update_progress(config, total=len(comment_ids))

    argument_map = {}
    relation_rows = []

    for i in tqdm(range(0, len(comment_ids), workers)):
        batch = comment_ids[i : i + workers]
        batch_inputs = [comments.loc[id]["comment-body"] for id in batch]
        batch_results = extract_batch(
            batch_inputs, prompt, model, workers, provider, config.get("local_llm_address"), config
        )

        for comment_id, extracted_args in zip(batch, batch_results, strict=False):
            for j, arg in enumerate(extracted_args):
                if arg not in argument_map:
                    # argumentテーブルに追加
                    arg_id = f"A{comment_id}_{j}"
                    argument = arg
                    argument_map[arg] = {
                        "arg-id": arg_id,
                        "argument": argument,
                    }
                else:
                    arg_id = argument_map[arg]["arg-id"]

                # relationテーブルにcommentとargの関係を追加
                relation_row = {
                    "arg-id": arg_id,
                    "comment-id": comment_id,
                }
                relation_rows.append(relation_row)

        update_progress(config, incr=len(batch))

    # DataFrame化
    results = pd.DataFrame(argument_map.values())
    relation_df = pd.DataFrame(relation_rows)

    if results.empty:
        raise RuntimeError("result is empty, maybe bad prompt")

    results.to_csv(path, index=False)
    # comment-idとarg-idの関係を保存
    relation_df.to_csv(f"outputs/{dataset}/relations.csv", index=False)


logging.basicConfig(level=logging.DEBUG)


def extract_batch(batch, prompt, model, workers, provider="openai", local_llm_address=None, config=None):
    with concurrent.futures.ThreadPoolExecutor(max_workers=workers) as executor:
        futures_with_index = [
            (i, executor.submit(extract_arguments, input, prompt, model, provider, local_llm_address))
            for i, input in enumerate(batch)
        ]

        done, not_done = concurrent.futures.wait([f for _, f in futures_with_index], timeout=30)
        results = [[] for _ in range(len(batch))]
        total_token_input = 0
        total_token_output = 0
        total_token_usage = 0

        for _, future in futures_with_index:
            if future in not_done and not future.cancelled():
                future.cancel()

        for i, future in futures_with_index:
            if future in done:
                try:
                    result = future.result()
                    if isinstance(result, tuple) and len(result) == 4:
                        items, token_input, token_output, token_total = result
                        results[i] = items
                        total_token_input += token_input
                        total_token_output += token_output
                        total_token_usage += token_total
                    else:
                        results[i] = result
                except Exception as e:
                    logging.error(f"Task {future} failed with error: {e}")
                    results[i] = []

        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + total_token_usage
            config["token_usage_input"] = config.get("token_usage_input", 0) + total_token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + total_token_output
            print(
                f"Extraction batch: input={total_token_input}, output={total_token_output}, total={total_token_usage} tokens"
            )

        return results


def extract_arguments(input, prompt, model, provider="openai", local_llm_address=None):
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            is_json=False,
            json_schema=ExtractionResponse,
            provider=provider,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )
        items = parse_extraction_response(response)
        items = list(filter(None, items))  # omit empty strings
        return items, token_input, token_output, token_total
    except json.decoder.JSONDecodeError as e:
        print("JSON error:", e)
        print("Input was:", input)
        print("Response was:", response)
        print("Silently giving up on trying to generate valid list.")
        return []
21:T458,import os

import pandas as pd
from tqdm import tqdm

from services.llm import request_to_embed


def embedding(config):
    model = config["embedding"]["model"]
    is_embedded_at_local = config["is_embedded_at_local"]
    # print("start embedding")
    # print(f"embedding model: {model}, is_embedded_at_local: {is_embedded_at_local}")

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/embeddings.pkl"
    arguments = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings = []
    batch_size = 1000
    for i in tqdm(range(0, len(arguments), batch_size)):
        args = arguments["argument"].tolist()[i : i + batch_size]
        embeds = request_to_embed(
            args,
            model,
            is_embedded_at_local,
            config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )
        embeddings.extend(embeds)
    df = pd.DataFrame([{"arg-id": arguments.iloc[i]["arg-id"], "embedding": e} for i, e in enumerate(embeddings)])
    df.to_pickle(path)
22:T1149,"""Cluster the arguments using UMAP + HDBSCAN and GPT-4."""

from importlib import import_module

import numpy as np
import pandas as pd
import scipy.cluster.hierarchy as sch
from sklearn.cluster import KMeans


def hierarchical_clustering(config):
    UMAP = import_module("umap").UMAP

    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_clusters.csv"
    arguments_df = pd.read_csv(f"outputs/{dataset}/args.csv", usecols=["arg-id", "argument"])
    embeddings_df = pd.read_pickle(f"outputs/{dataset}/embeddings.pkl")
    embeddings_array = np.asarray(embeddings_df["embedding"].values.tolist())
    cluster_nums = config["hierarchical_clustering"]["cluster_nums"]

    n_samples = embeddings_array.shape[0]
    # デフォルト設定は15
    default_n_neighbors = 15

    # テスト等サンプルが少なすぎる場合、n_neighborsの設定値を下げる
    if n_samples <= default_n_neighbors:
        n_neighbors = max(2, n_samples - 1)  # 最低2以上
    else:
        n_neighbors = default_n_neighbors

    umap_model = UMAP(random_state=42, n_components=2, n_neighbors=n_neighbors)
    # TODO 詳細エラーメッセージを加える
    # 以下のエラーの場合、おそらく元の意見件数が少なすぎることが原因
    # TypeError: Cannot use scipy.linalg.eigh for sparse A with k >= N. Use scipy.linalg.eigh(A.toarray()) or reduce k.
    umap_embeds = umap_model.fit_transform(embeddings_array)

    cluster_results = hierarchical_clustering_embeddings(
        umap_embeds=umap_embeds,
        cluster_nums=cluster_nums,
    )
    result_df = pd.DataFrame(
        {
            "arg-id": arguments_df["arg-id"],
            "argument": arguments_df["argument"],
            "x": umap_embeds[:, 0],
            "y": umap_embeds[:, 1],
        }
    )

    for cluster_level, final_labels in enumerate(cluster_results.values(), start=1):
        result_df[f"cluster-level-{cluster_level}-id"] = [f"{cluster_level}_{label}" for label in final_labels]

    result_df.to_csv(path, index=False)


def generate_cluster_count_list(min_clusters: int, max_clusters: int):
    cluster_counts = []
    current = min_clusters
    cluster_counts.append(current)

    if min_clusters == max_clusters:
        return cluster_counts

    while True:
        next_double = current * 2
        next_triple = current * 3

        if next_double >= max_clusters:
            if cluster_counts[-1] != max_clusters:
                cluster_counts.append(max_clusters)
            break

        # 次の倍はまだ max_clusters に収まるが、3倍だと超える
        # -> (次の倍は細かすぎるので)スキップして max_clusters に飛ぶ
        if next_triple > max_clusters:
            cluster_counts.append(max_clusters)
            break

        cluster_counts.append(next_double)
        current = next_double

    return cluster_counts


def merge_clusters_with_hierarchy(
    cluster_centers: np.ndarray,
    kmeans_labels: np.ndarray,
    umap_array: np.ndarray,
    n_cluster_cut: int,
):
    Z = sch.linkage(cluster_centers, method="ward")
    cluster_labels_merged = sch.fcluster(Z, t=n_cluster_cut, criterion="maxclust")

    n_samples = umap_array.shape[0]
    final_labels = np.zeros(n_samples, dtype=int)

    for i in range(n_samples):
        original_label = kmeans_labels[i]
        final_labels[i] = cluster_labels_merged[original_label]

    return final_labels


def hierarchical_clustering_embeddings(
    umap_embeds,
    cluster_nums,
):
    # 最大分割数でクラスタリングを実施
    print("start initial clustering")
    initial_cluster_num = cluster_nums[-1]
    kmeans_model = KMeans(n_clusters=initial_cluster_num, random_state=42)
    kmeans_model.fit(umap_embeds)
    print("end initial clustering")

    results = {}
    print("start hierarchical clustering")
    cluster_nums.sort()
    print(cluster_nums)
    for n_cluster_cut in cluster_nums[:-1]:
        print("n_cluster_cut: ", n_cluster_cut)
        final_labels = merge_clusters_with_hierarchy(
            cluster_centers=kmeans_model.cluster_centers_,
            kmeans_labels=kmeans_model.labels_,
            umap_array=umap_embeds,
            n_cluster_cut=n_cluster_cut,
        )
        results[n_cluster_cut] = final_labels

    results[initial_cluster_num] = kmeans_model.labels_
    print("end hierarchical clustering")

    return results
23:T1c36,import json
import os
from concurrent.futures import ThreadPoolExecutor
from functools import partial
from typing import TypedDict

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class LabellingResult(TypedDict):
    """各クラスタのラベリング結果を表す型"""

    cluster_id: str  # クラスタのID
    label: str  # クラスタのラベル名
    description: str  # クラスタの説明文


def hierarchical_initial_labelling(config: dict) -> None:
    """階層的クラスタリングの初期ラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_initial_labelling: 初期ラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_initial_labels.csv"
    clusters_argument_df = pd.read_csv(f"outputs/{dataset}/hierarchical_clusters.csv")

    cluster_id_columns = [col for col in clusters_argument_df.columns if col.startswith("cluster-level-")]
    initial_cluster_id_column = cluster_id_columns[-1]
    sampling_num = config["hierarchical_initial_labelling"]["sampling_num"]
    initial_labelling_prompt = config["hierarchical_initial_labelling"]["prompt"]
    model = config["hierarchical_initial_labelling"]["model"]
    workers = config["hierarchical_initial_labelling"]["workers"]

    # トークン使用量を追跡するための変数を初期化
    config["total_token_usage"] = config.get("total_token_usage", 0)

    initial_label_df = initial_labelling(
        initial_labelling_prompt,
        clusters_argument_df,
        sampling_num,
        model,
        workers,
        config["provider"],
        config.get("local_llm_address"),
        config,  # configを渡して、トークン使用量を累積できるようにする
    )
    print("start initial labelling")
    initial_clusters_argument_df = clusters_argument_df.merge(
        initial_label_df,
        left_on=initial_cluster_id_column,
        right_on="cluster_id",
        how="left",
    ).rename(
        columns={
            "label": f"{initial_cluster_id_column.replace('-id', '')}-label",
            "description": f"{initial_cluster_id_column.replace('-id', '')}-description",
        }
    )
    print("end initial labelling")
    initial_clusters_argument_df.to_csv(path, index=False)


def initial_labelling(
    prompt: str,
    clusters_df: pd.DataFrame,
    sampling_num: int,
    model: str,
    workers: int,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> pd.DataFrame:
    """各クラスタに対して初期ラベリングを実行する

    Args:
        prompt: LLMへのプロンプト
        clusters_df: クラスタリング結果のDataFrame
        sampling_num: 各クラスタからサンプリングする意見の数
        model: 使用するLLMモデル名
        workers: 並列処理のワーカー数
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        各クラスタのラベリング結果を含むDataFrame
    """
    cluster_columns = [col for col in clusters_df.columns if col.startswith("cluster-level-")]
    initial_cluster_column = cluster_columns[-1]
    cluster_ids = clusters_df[initial_cluster_column].unique()
    process_func = partial(
        process_initial_labelling,
        df=clusters_df,
        prompt=prompt,
        sampling_num=sampling_num,
        target_column=initial_cluster_column,
        model=model,
        provider=provider,
        local_llm_address=local_llm_address,
        config=config,  # configを渡す
    )
    with ThreadPoolExecutor(max_workers=workers) as executor:
        results = list(executor.map(process_func, cluster_ids))
    return pd.DataFrame(results)


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_initial_labelling(
    cluster_id: str,
    df: pd.DataFrame,
    prompt: str,
    sampling_num: int,
    target_column: str,
    model: str,
    provider: str = "openai",
    local_llm_address: str | None = None,
    config: dict | None = None,  # configを追加
) -> LabellingResult:
    """個別のクラスタに対してラベリングを実行する

    Args:
        cluster_id: 処理対象のクラスタID
        df: クラスタリング結果のDataFrame
        prompt: LLMへのプロンプト
        sampling_num: サンプリングする意見の数
        target_column: クラスタIDが格納されている列名
        model: 使用するLLMモデル名
        provider: LLMプロバイダー
        local_llm_address: ローカルLLMのアドレス
        config: 設定情報を含む辞書（トークン使用量の累積に使用）

    Returns:
        クラスタのラベリング結果
    """
    cluster_data = df[df[target_column] == cluster_id]
    sampling_num = min(sampling_num, len(cluster_data))
    cluster = cluster_data.sample(sampling_num)
    input = "\n".join(cluster["argument"].values)
    messages = [
        {"role": "system", "content": prompt},
        {"role": "user", "content": input},
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=model,
            provider=provider,
            json_schema=LabellingFromat,
            local_llm_address=local_llm_address,
            user_api_key=os.getenv("USER_API_KEY"),
        )

        # トークン使用量を累積（configが渡されている場合）
        if config is not None:
            config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
            config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
            config["token_usage_output"] = config.get("token_usage_output", 0) + token_output

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return LabellingResult(
            cluster_id=cluster_id,
            label=response_json.get("label", "エラーでラベル名が取得できませんでした"),
            description=response_json.get("description", "エラーで解説が取得できませんでした"),
        )
    except Exception as e:
        print(e)
        return LabellingResult(
            cluster_id=cluster_id,
            label="エラーでラベル名が取得できませんでした",
            description="エラーで解説が取得できませんでした",
        )
24:T337b,import json
import os
from concurrent.futures import ThreadPoolExecutor
from dataclasses import dataclass
from functools import partial

import numpy as np
import pandas as pd
from pydantic import BaseModel, Field
from tqdm import tqdm

from services.llm import request_to_chat_ai


@dataclass
class ClusterColumns:
    """同一階層のクラスター関連のカラム名を管理するクラス"""

    id: str
    label: str
    description: str

    @classmethod
    def from_id_column(cls, id_column: str) -> "ClusterColumns":
        """ID列名から関連するカラム名を生成"""
        return cls(
            id=id_column,
            label=id_column.replace("-id", "-label"),
            description=id_column.replace("-id", "-description"),
        )


@dataclass
class ClusterValues:
    """対象クラスタのlabel/descriptionを管理するクラス"""

    label: str
    description: str

    def to_prompt_text(self) -> str:
        return f"- {self.label}: {self.description}"


def hierarchical_merge_labelling(config: dict) -> None:
    """階層的クラスタリングの結果に対してマージラベリングを実行する

    Args:
        config: 設定情報を含む辞書
            - output_dir: 出力ディレクトリ名
            - hierarchical_merge_labelling: マージラベリングの設定
                - sampling_num: サンプリング数
                - prompt: LLMへのプロンプト
                - model: 使用するLLMモデル名
                - workers: 並列処理のワーカー数
            - provider: LLMプロバイダー
    """
    dataset = config["output_dir"]
    merge_path = f"outputs/{dataset}/hierarchical_merge_labels.csv"
    clusters_df = pd.read_csv(f"outputs/{dataset}/hierarchical_initial_labels.csv")

    cluster_id_columns: list[str] = _filter_id_columns(clusters_df.columns)
    # ボトムクラスタのラベル・説明とクラスタid付きの各argumentを入力し、各階層のクラスタラベル・説明を生成し、argumentに付けたdfを作成
    merge_result_df = merge_labelling(
        clusters_df=clusters_df,
        cluster_id_columns=sorted(cluster_id_columns, reverse=True),
        config=config,
    )
    # 上記のdfから各クラスタのlevel, id, label, description, valueを取得してdfを作成
    melted_df = melt_cluster_data(merge_result_df)
    # 上記のdfに親子関係を追加
    parent_child_df = _build_parent_child_mapping(merge_result_df, cluster_id_columns)
    melted_df = melted_df.merge(parent_child_df, on=["level", "id"], how="left")
    density_df = calculate_cluster_density(melted_df, config)
    density_df.to_csv(merge_path, index=False)


def _build_parent_child_mapping(df: pd.DataFrame, cluster_id_columns: list[str]):
    """クラスタ間の親子関係をマッピングする

    Args:
        df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト

    Returns:
        親子関係のマッピング情報を含むDataFrame
    """
    results = []
    top_cluster_column = cluster_id_columns[0]
    top_cluster_values = df[top_cluster_column].unique()
    for c in top_cluster_values:
        results.append(
            {
                "level": 1,
                "id": c,
                "parent": "0",  # aggregationで追加する全体クラスタのid
            }
        )

    for idx in range(len(cluster_id_columns) - 1):
        current_column = cluster_id_columns[idx]
        children_column = cluster_id_columns[idx + 1]
        current_level = current_column.replace("-id", "").replace("cluster-level-", "")
        # 現在のレベルのクラスタid
        current_cluster_values = df[current_column].unique()
        for current_id in current_cluster_values:
            children_ids = df.loc[df[current_column] == current_id, children_column].unique()
            for child_id in children_ids:
                results.append(
                    {
                        "level": int(current_level) + 1,
                        "id": child_id,
                        "parent": current_id,
                    }
                )
    return pd.DataFrame(results)


def _filter_id_columns(columns: list[str]) -> list[str]:
    """クラスタIDのカラム名をフィルタリングする

    Args:
        columns: 全カラム名のリスト

    Returns:
        クラスタIDのカラム名のリスト
    """
    return [col for col in columns if col.startswith("cluster-level-") and col.endswith("-id")]


def melt_cluster_data(df: pd.DataFrame) -> pd.DataFrame:
    """クラスタデータを行形式に変換する

    cluster-level-n-(id|label|description) を行形式 (level, id, label, description, value) にまとめる。
    [cluster-level-n-id, cluster-level-n-label, cluster-level-n-description] を [level, id, label, description, value(件数)] に変換する。

    Args:
        df: クラスタリング結果のDataFrame

    Returns:
        行形式に変換されたDataFrame
    """
    id_columns: list[str] = _filter_id_columns(df.columns)
    levels: set[int] = {int(col.replace("cluster-level-", "").replace("-id", "")) for col in id_columns}
    all_rows: list[dict] = []

    # levelごとに各クラスタの出現件数を集計・縦持ちにする
    for level in levels:
        cluster_columns = ClusterColumns.from_id_column(f"cluster-level-{level}-id")
        # クラスタidごとの件数集計
        level_count_df = df.groupby(cluster_columns.id).size().reset_index(name="value")

        level_unique_val_df = df[
            [cluster_columns.id, cluster_columns.label, cluster_columns.description]
        ].drop_duplicates()
        level_unique_val_df = level_unique_val_df.merge(level_count_df, on=cluster_columns.id, how="left")
        level_unique_vals = [
            {
                "level": level,
                "id": row[cluster_columns.id],
                "label": row[cluster_columns.label],
                "description": row[cluster_columns.description],
                "value": row["value"],
            }
            for _, row in level_unique_val_df.iterrows()
        ]
        all_rows.extend(level_unique_vals)
    return pd.DataFrame(all_rows)


def merge_labelling(clusters_df: pd.DataFrame, cluster_id_columns: list[str], config) -> pd.DataFrame:
    """階層的なクラスタのマージラベリングを実行する

    Args:
        clusters_df: クラスタリング結果のDataFrame
        cluster_id_columns: クラスタIDのカラム名のリスト
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含むDataFrame
    """
    for idx in tqdm(range(len(cluster_id_columns) - 1)):
        previous_columns = ClusterColumns.from_id_column(cluster_id_columns[idx])
        current_columns = ClusterColumns.from_id_column(cluster_id_columns[idx + 1])

        process_fn = partial(
            process_merge_labelling,
            result_df=clusters_df,
            current_columns=current_columns,
            previous_columns=previous_columns,
            config=config,
        )

        current_cluster_ids = sorted(clusters_df[current_columns.id].unique())
        with ThreadPoolExecutor(max_workers=config["hierarchical_merge_labelling"]["workers"]) as executor:
            responses = list(
                tqdm(
                    executor.map(process_fn, current_cluster_ids),
                    total=len(current_cluster_ids),
                )
            )

        current_result_df = pd.DataFrame(responses)
        clusters_df = clusters_df.merge(current_result_df, on=[current_columns.id])
    return clusters_df


class LabellingFromat(BaseModel):
    """ラベリング結果のフォーマットを定義する"""

    label: str = Field(..., description="クラスタのラベル名")
    description: str = Field(..., description="クラスタの説明文")


def process_merge_labelling(
    target_cluster_id: str,
    result_df: pd.DataFrame,
    current_columns: ClusterColumns,
    previous_columns: ClusterColumns,
    config,
):
    """個別のクラスタに対してマージラベリングを実行する

    Args:
        target_cluster_id: 処理対象のクラスタID
        result_df: クラスタリング結果のDataFrame
        current_columns: 現在のレベルのカラム情報
        previous_columns: 前のレベルのカラム情報
        config: 設定情報を含む辞書

    Returns:
        マージラベリング結果を含む辞書
    """

    def filter_previous_values(df: pd.DataFrame, previous_columns: ClusterColumns) -> list[ClusterValues]:
        """前のレベルのクラスタ情報を取得する"""
        previous_records = df[df[current_columns.id] == target_cluster_id][
            [previous_columns.label, previous_columns.description]
        ].drop_duplicates()
        previous_values = [
            ClusterValues(
                label=row[previous_columns.label],
                description=row[previous_columns.description],
            )
            for _, row in previous_records.iterrows()
        ]
        return previous_values

    previous_values = filter_previous_values(result_df, previous_columns)
    if len(previous_values) == 1:
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: previous_values[0].label,
            current_columns.description: previous_values[0].description,
        }
    elif len(previous_values) == 0:
        raise ValueError(f"クラスタ {target_cluster_id} には前のレベルのクラスタが存在しません。")

    current_cluster_data = result_df[result_df[current_columns.id] == target_cluster_id]
    sampling_num = min(
        config["hierarchical_merge_labelling"]["sampling_num"],
        len(current_cluster_data),
    )
    sampled_data = current_cluster_data.sample(sampling_num)
    sampled_argument_text = "\n".join(sampled_data["argument"].values)
    cluster_text = "\n".join([value.to_prompt_text() for value in previous_values])
    messages = [
        {"role": "system", "content": config["hierarchical_merge_labelling"]["prompt"]},
        {
            "role": "user",
            "content": "クラスタラベル\n" + cluster_text + "\n" + "クラスタの意見\n" + sampled_argument_text,
        },
    ]
    try:
        response_text, token_input, token_output, token_total = request_to_chat_ai(
            messages=messages,
            model=config["hierarchical_merge_labelling"]["model"],
            json_schema=LabellingFromat,
            provider=config["provider"],
            local_llm_address=config.get("local_llm_address"),
            user_api_key=os.getenv("USER_API_KEY"),
        )

        config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
        config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
        config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
        print(f"Merge labelling: input={token_input}, output={token_output}, total={token_total} tokens")

        response_json = json.loads(response_text) if isinstance(response_text, str) else response_text
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: response_json.get("label", "エラーでラベル名が取得できませんでした"),
            current_columns.description: response_json.get("description", "エラーで解説が取得できませんでした"),
        }
    except Exception as e:
        print(f"エラーが発生しました: {e}")
        return {
            current_columns.id: target_cluster_id,
            current_columns.label: "エラーでラベル名が取得できませんでした",
            current_columns.description: "エラーで解説が取得できませんでした",
        }


def calculate_cluster_density(melted_df: pd.DataFrame, config: dict):
    """クラスタ内の密度計算"""
    hierarchical_cluster_df = pd.read_csv(f"outputs/{config['output_dir']}/hierarchical_clusters.csv")

    densities = []
    for level, c_id in zip(melted_df["level"], melted_df["id"], strict=False):
        cluster_embeds = hierarchical_cluster_df[hierarchical_cluster_df[f"cluster-level-{level}-id"] == c_id][
            ["x", "y"]
        ].values
        density = calculate_density(cluster_embeds)
        densities.append(density)

    # 密度のランクを計算
    melted_df["density"] = densities
    melted_df["density_rank"] = melted_df.groupby("level")["density"].rank(ascending=False, method="first")
    melted_df["density_rank_percentile"] = melted_df.groupby("level")["density_rank"].transform(lambda x: x / len(x))
    return melted_df


def calculate_density(embeds: np.ndarray):
    """平均距離に基づいて密度を計算"""
    center = np.mean(embeds, axis=0)
    distances = np.linalg.norm(embeds - center, axis=1)
    avg_distance = np.mean(distances)
    density = 1 / (avg_distance + 1e-10)
    return density
25:Ta82,"""Create summaries for the clusters."""

import json
import os
import re

import pandas as pd
from pydantic import BaseModel, Field

from services.llm import request_to_chat_ai


class OverviewResponse(BaseModel):
    summary: str = Field(..., description="クラスターの全体的な要約")


def hierarchical_overview(config):
    dataset = config["output_dir"]
    path = f"outputs/{dataset}/hierarchical_overview.txt"

    hierarchical_label_df = pd.read_csv(f"outputs/{dataset}/hierarchical_merge_labels.csv")

    prompt = config["hierarchical_overview"]["prompt"]
    model = config["hierarchical_overview"]["model"]

    # TODO: level1で固定にしているが、設定で変えられるようにする
    target_level = 1
    target_records = hierarchical_label_df[hierarchical_label_df["level"] == target_level]
    ids = target_records["id"].to_list()
    labels = target_records["label"].to_list()
    descriptions = target_records["description"].to_list()
    target_records.set_index("id", inplace=True)

    input_text = ""
    for i, _ in enumerate(ids):
        input_text += f"# Cluster {i}/{len(ids)}: {labels[i]}\n\n"
        input_text += descriptions[i] + "\n\n"

    messages = [{"role": "system", "content": prompt}, {"role": "user", "content": input_text}]
    response_text, token_input, token_output, token_total = request_to_chat_ai(
        messages=messages,
        model=model,
        provider=config["provider"],
        local_llm_address=config.get("local_llm_address"),
        user_api_key=os.getenv("USER_API_KEY"),
        json_schema=OverviewResponse,
    )

    # トークン使用量を累積
    config["total_token_usage"] = config.get("total_token_usage", 0) + token_total
    config["token_usage_input"] = config.get("token_usage_input", 0) + token_input
    config["token_usage_output"] = config.get("token_usage_output", 0) + token_output
    print(f"Hierarchical overview: input={token_input}, output={token_output}, total={token_total} tokens")

    try:
        # structured outputとしてパースできるなら処理する
        if isinstance(response_text, dict):
            parsed_response = response_text
        else:
            parsed_response = json.loads(response_text)

        with open(path, "w") as file:
            file.write(parsed_response["summary"])

    except Exception:
        # thinkタグが出力されるReasoningモデル用に、thinkタグを除去する
        thinking_removed = re.sub(
            r"<think\b[^>]*>.*?</think>",
            "",
            response_text,
            flags=re.DOTALL,
        )

        with open(path, "w") as file:
            file.write(thinking_removed)
8:[["$","$L13",null,{}],["$","$L14",null,{"className":"container","mt":"8","children":[["$","$L14",null,{"mx":"auto","maxW":"750px","mb":8,"children":[["$","$L15",null,{"textAlign":"left","fontSize":"xl","mb":5,"children":"レポート"}],["$","$L15",null,{"as":"h2","size":"4xl","mb":2,"className":"headingColor","children":"④市民として、2040年に向けてできることとは【1/31 15:00 更新】"}],["$","$L16",null,{"fontWeight":"bold","fontSize":"xl","mb":2,"children":[["$","$L17",null,{"mr":1,"children":["$","svg",null,{"ref":"$undefined","xmlns":"http://www.w3.org/2000/svg","width":20,"height":20,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-messages-square","children":[["$","path","p1xzt8",{"d":"M14 9a2 2 0 0 1-2 2H6l-4 4V4a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2z"}],["$","path","1cx29u",{"d":"M18 9h2a2 2 0 0 1 2 2v11l-4-4h-6a2 2 0 0 1-2-2v-1"}],"$undefined"]}]}],"133","件"]}],["$","p",null,{"children":"地域社会の活性化には市民参加が不可欠であり、交流やボランティア活動を通じて信頼関係を築くことが求められています。また、健康維持や子どもたちの成長を促すための教育や実体験の重要性が強調され、世代を超えたつながりや地域環境の保護も重要なテーマです。舞鶴市では地域イベントの拡大や子育て支援が求められ、住民の協力による持続可能な発展が期待されています。"}]]}],["$","$L18",null,{"result":{"arguments":[{"arg_id":"Acsv-2_0","argument":"たくさんの人と繋がって話をしたい。","x":11.101179,"y":5.23884,"p":0,"cluster_ids":["0","1_6","2_52"],"attributes":null,"url":null},{"arg_id":"Acsv-3_0","argument":"海洋少年団の活動をもっと活発化させ、団員たちに様々な経験や体験をさせるべき","x":12.022615,"y":5.807432,"p":0,"cluster_ids":["0","1_6","2_48"],"attributes":null,"url":null},{"arg_id":"Acsv-3_1","argument":"青少年団体が繋がれる枠組みを作るべき","x":12.130135,"y":6.240586,"p":0,"cluster_ids":["0","1_7","2_17"],"attributes":null,"url":null},{"arg_id":"Acsv-4_0","argument":"健康寿命を延ばすためのトレーニング法を普及させるべき","x":11.21734,"y":2.7239401,"p":0,"cluster_ids":["0","1_2","2_53"],"attributes":null,"url":null},{"arg_id":"Acsv-4_1","argument":"健康寿命を延ばすための食事療法を普及させるべき","x":10.7407875,"y":2.6635113,"p":0,"cluster_ids":["0","1_2","2_25"],"attributes":null,"url":null},{"arg_id":"Acsv-5_0","argument":"子どもたちにとって手触りのある実体験が成長に必要であるが、スマホやテレビゲームがその時間を奪っていることが懸念される。","x":12.349925,"y":4.7205434,"p":0,"cluster_ids":["0","1_4","2_29"],"attributes":null,"url":null},{"arg_id":"Acsv-5_1","argument":"工作や実験などの実体験を推奨したい。","x":12.072557,"y":5.478393,"p":0,"cluster_ids":["0","1_4","2_37"],"attributes":null,"url":null},{"arg_id":"Acsv-5_2","argument":"子どもたちの相手をする機会があれば、体力の許す限り参加したい。","x":12.57472,"y":5.587112,"p":0,"cluster_ids":["0","1_4","2_14"],"attributes":null,"url":null},{"arg_id":"Acsv-5_3","argument":"爺さん婆さんの健康維持にも役立つのではないか。","x":11.030478,"y":2.95957,"p":0,"cluster_ids":["0","1_2","2_35"],"attributes":null,"url":null},{"arg_id":"Acsv-6_0","argument":"年代を問わず、誰でも参加しやすい場を作りたい","x":12.622294,"y":6.200404,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-9_0","argument":"舞鶴市政に興味を持つべきである。","x":13.591248,"y":9.48315,"p":0,"cluster_ids":["0","1_1","2_50"],"attributes":null,"url":null},{"arg_id":"Acsv-9_1","argument":"市議会は是々非々であるべきだ。","x":10.419432,"y":6.7575283,"p":0,"cluster_ids":["0","1_6","2_45"],"attributes":null,"url":null},{"arg_id":"Acsv-11_0","argument":"地域の高齢者に体操を続けてもらうためには、旅行などの特典を提供することが効果的である。","x":11.612995,"y":3.0512774,"p":0,"cluster_ids":["0","1_2","2_34"],"attributes":null,"url":null},{"arg_id":"Acsv-11_1","argument":"健康やお得感を提供することで、より多くの高齢者に参加してもらえる可能性がある。","x":11.525316,"y":3.3538933,"p":0,"cluster_ids":["0","1_2","2_57"],"attributes":null,"url":null},{"arg_id":"Acsv-11_2","argument":"ポイントを集めて買い物ができるような仕組みも良いかもしれない。","x":12.768449,"y":6.1625915,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-12_0","argument":"公園ですれ違う人への挨拶を大切にしたい。","x":10.318645,"y":5.3208575,"p":0,"cluster_ids":["0","1_6","2_6"],"attributes":null,"url":null},{"arg_id":"Acsv-12_1","argument":"幼い子から高齢者への思いやりや配慮を大切にしたい。","x":11.455998,"y":4.025388,"p":0,"cluster_ids":["0","1_3","2_27"],"attributes":null,"url":null},{"arg_id":"Acsv-13_0","argument":"舞鶴のイベントに積極的に参加すべき","x":13.709061,"y":9.031704,"p":0,"cluster_ids":["0","1_1","2_42"],"attributes":null,"url":null},{"arg_id":"Acsv-14_0","argument":"皆さんを応援させていただきます。","x":10.554439,"y":4.697773,"p":0,"cluster_ids":["0","1_3","2_12"],"attributes":null,"url":null},{"arg_id":"Acsv-15_0","argument":"健康が一番であり、身体の健康と心の健康を持つことで自分の生活が潤い、他の人の幸せも願えると思う。","x":11.004113,"y":3.073606,"p":0,"cluster_ids":["0","1_2","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-15_1","argument":"地域の行事に参加し、普段から地域の人たちと顔を合わせておくことが大事だと思う。","x":11.776898,"y":7.2613287,"p":0,"cluster_ids":["0","1_5","2_9"],"attributes":null,"url":null},{"arg_id":"Acsv-16_0","argument":"舞鶴はのんびり過ごせる雨の町である","x":13.782299,"y":9.609728,"p":0,"cluster_ids":["0","1_1","2_50"],"attributes":null,"url":null},{"arg_id":"Acsv-16_1","argument":"凪凪の舞鶴湾が魅力的である","x":14.169364,"y":9.060643,"p":0,"cluster_ids":["0","1_1","2_39"],"attributes":null,"url":null},{"arg_id":"Acsv-16_2","argument":"赤レンガ以外にも多くの遺構がある","x":14.273072,"y":6.3009353,"p":0,"cluster_ids":["0","1_8","2_41"],"attributes":null,"url":null},{"arg_id":"Acsv-16_3","argument":"白崎の滝が美しい","x":14.141612,"y":8.670343,"p":0,"cluster_ids":["0","1_1","2_22"],"attributes":null,"url":null},{"arg_id":"Acsv-16_4","argument":"舞鶴の人々はひとなつこくて親切である","x":13.946741,"y":9.436,"p":0,"cluster_ids":["0","1_1","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-16_5","argument":"舞鶴の良さを知ってもらい、近郊から楽しみに来てもらいたい","x":13.925135,"y":9.237628,"p":0,"cluster_ids":["0","1_1","2_42"],"attributes":null,"url":null},{"arg_id":"Acsv-17_0","argument":"ボランティア活動が推し進められるようになるとよい","x":11.841999,"y":6.2080193,"p":0,"cluster_ids":["0","1_6","2_21"],"attributes":null,"url":null},{"arg_id":"Acsv-17_1","argument":"65歳になった人にボランティア活動の案内を送るべき","x":11.663671,"y":6.3071685,"p":0,"cluster_ids":["0","1_6","2_55"],"attributes":null,"url":null},{"arg_id":"Acsv-17_2","argument":"ボランティア活動をポイント制にして参加者を増やすべき","x":11.862397,"y":6.5462317,"p":0,"cluster_ids":["0","1_6","2_38"],"attributes":null,"url":null},{"arg_id":"Acsv-18_0","argument":"ダメなことはダメと言えるようになりたい","x":12.345473,"y":4.188492,"p":0,"cluster_ids":["0","1_4","2_54"],"attributes":null,"url":null},{"arg_id":"Acsv-19_0","argument":"舞鶴の良さを発信すべき","x":14.13647,"y":9.317082,"p":0,"cluster_ids":["0","1_1","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-20_0","argument":"真面目に勉強するべきである。","x":12.077431,"y":4.5121975,"p":0,"cluster_ids":["0","1_4","2_31"],"attributes":null,"url":null},{"arg_id":"Acsv-21_0","argument":"挨拶をすることは重要である。","x":10.129297,"y":4.7698298,"p":0,"cluster_ids":["0","1_3","2_40"],"attributes":null,"url":null},{"arg_id":"Acsv-23_0","argument":"支援制度の見直しが必要である。","x":10.945134,"y":6.975974,"p":0,"cluster_ids":["0","1_5","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-23_1","argument":"現行の制度は条件が厳しく、支援金を払う気がないように思える。","x":10.874182,"y":7.027269,"p":0,"cluster_ids":["0","1_5","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-24_0","argument":"若い人を応援したい。","x":10.718567,"y":4.657731,"p":0,"cluster_ids":["0","1_3","2_12"],"attributes":null,"url":null},{"arg_id":"Acsv-24_1","argument":"市がすることで自分が同感できる事は一緒にやりたい。","x":10.273633,"y":6.303987,"p":0,"cluster_ids":["0","1_6","2_30"],"attributes":null,"url":null},{"arg_id":"Acsv-24_2","argument":"舞鶴市の子育て支援をさらに良くして、安心して子を産め、育てられるまちにしたい。","x":13.703025,"y":9.739088,"p":0,"cluster_ids":["0","1_1","2_50"],"attributes":null,"url":null},{"arg_id":"Acsv-25_0","argument":"公園や海、山などの美化活動をしていくべきである。","x":13.382204,"y":6.052645,"p":0,"cluster_ids":["0","1_7","2_19"],"attributes":null,"url":null},{"arg_id":"Acsv-26_0","argument":"ごみの分別などで公害を起こさないコミュニティをつくるべき","x":14.664126,"y":6.1668563,"p":0,"cluster_ids":["0","1_8","2_11"],"attributes":null,"url":null},{"arg_id":"Acsv-26_1","argument":"皆がつながることが重要である","x":11.003963,"y":3.9989562,"p":0,"cluster_ids":["0","1_3","2_27"],"attributes":null,"url":null},{"arg_id":"Acsv-27_0","argument":"高専祭を盛り上げるべきである。","x":11.91247,"y":4.950407,"p":0,"cluster_ids":["0","1_4","2_8"],"attributes":null,"url":null},{"arg_id":"Acsv-28_0","argument":"健康第一で生活するべきである。","x":10.780531,"y":2.9960365,"p":0,"cluster_ids":["0","1_2","2_35"],"attributes":null,"url":null},{"arg_id":"Acsv-29_0","argument":"高専は素晴らしい教育機関であることをアピールすべき","x":11.775223,"y":4.511306,"p":0,"cluster_ids":["0","1_4","2_31"],"attributes":null,"url":null},{"arg_id":"Acsv-30_0","argument":"相手を思いやった行動を心掛けるべき","x":12.76627,"y":5.044144,"p":0,"cluster_ids":["0","1_4","2_29"],"attributes":null,"url":null},{"arg_id":"Acsv-62_0","argument":"バレーで全中に行くべきである","x":10.871367,"y":5.920159,"p":0,"cluster_ids":["0","1_6","2_15"],"attributes":null,"url":null},{"arg_id":"Acsv-63_0","argument":"観光地などのポスターを作成するべきである。","x":13.218446,"y":6.46188,"p":0,"cluster_ids":["0","1_7","2_58"],"attributes":null,"url":null},{"arg_id":"Acsv-68_0","argument":"スポーツの指導者として関わるべきである。","x":10.755969,"y":3.9432743,"p":0,"cluster_ids":["0","1_3","2_61"],"attributes":null,"url":null},{"arg_id":"Acsv-69_0","argument":"市で開催される催しに積極的に参加し、今の舞鶴市を知るべき","x":13.294799,"y":9.196399,"p":0,"cluster_ids":["0","1_1","2_0"],"attributes":null,"url":null},{"arg_id":"Acsv-69_1","argument":"舞鶴市の情報を発信するべき","x":13.601338,"y":9.527484,"p":0,"cluster_ids":["0","1_1","2_50"],"attributes":null,"url":null},{"arg_id":"Acsv-69_2","argument":"いつも元気でいるために運動をしたい","x":11.447055,"y":2.2697237,"p":0,"cluster_ids":["0","1_2","2_10"],"attributes":null,"url":null},{"arg_id":"Acsv-284_0","argument":"看護師として舞鶴を支えていきたい","x":14.144534,"y":9.454431,"p":0,"cluster_ids":["0","1_1","2_7"],"attributes":null,"url":null},{"arg_id":"Acsv-286_0","argument":"この街を好きでいられることが重要である。","x":10.318167,"y":6.0332465,"p":0,"cluster_ids":["0","1_6","2_33"],"attributes":null,"url":null},{"arg_id":"Acsv-361_0","argument":"農地を使った地場産業が発展するような取り組みを進めるべきである。","x":12.150375,"y":7.276597,"p":0,"cluster_ids":["0","1_5","2_43"],"attributes":null,"url":null},{"arg_id":"Acsv-361_1","argument":"体を使うことで疲れるため、その疲れを癒したり健康維持できるケアも併せて続けるべきである。","x":11.552481,"y":2.5209408,"p":0,"cluster_ids":["0","1_2","2_53"],"attributes":null,"url":null},{"arg_id":"Acsv-363_0","argument":"働く、遊ぶ、学ぶという活動を通じて、有志が夢を成し遂げることを期待する。","x":11.75708,"y":5.6061087,"p":0,"cluster_ids":["0","1_6","2_48"],"attributes":null,"url":null},{"arg_id":"Acsv-363_1","argument":"その結果、恩恵を受けることができることを望む。","x":12.284944,"y":5.1940947,"p":0,"cluster_ids":["0","1_4","2_37"],"attributes":null,"url":null},{"arg_id":"Acsv-365_0","argument":"「インクルーシブな学校運営モデル事業」を市内全域で展開することに対する理解や支援をしていきたい。","x":11.216961,"y":7.1360946,"p":0,"cluster_ids":["0","1_5","2_46"],"attributes":null,"url":null},{"arg_id":"Acsv-367_0","argument":"イベントの参加を促進すべき","x":12.657597,"y":6.860181,"p":0,"cluster_ids":["0","1_7","2_51"],"attributes":null,"url":null},{"arg_id":"Acsv-367_1","argument":"イベントを盛り上げるための取り組みが必要","x":12.845864,"y":6.868745,"p":0,"cluster_ids":["0","1_7","2_26"],"attributes":null,"url":null},{"arg_id":"Acsv-368_0","argument":"健康でいることが重要である。","x":11.0703125,"y":3.2375896,"p":0,"cluster_ids":["0","1_2","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-368_1","argument":"デザインや文化芸術の面から、自分のできることを一つずつ取り組んで発表していくべきである。","x":13.147827,"y":5.8663087,"p":0,"cluster_ids":["0","1_7","2_59"],"attributes":null,"url":null},{"arg_id":"Acsv-368_2","argument":"人口の少なさから需要が少ない産業や事業に対して、市からの支援が必要である。","x":10.989232,"y":7.354747,"p":0,"cluster_ids":["0","1_5","2_46"],"attributes":null,"url":null},{"arg_id":"Acsv-368_3","argument":"市は予算割合として都会に負けないくらいの支援を行うべきである。","x":10.693904,"y":7.1731734,"p":0,"cluster_ids":["0","1_5","2_4"],"attributes":null,"url":null},{"arg_id":"Acsv-368_4","argument":"新しいことを始められる機会を提供するために、そういうことをしたいと考えている人を公募すべきである。","x":11.771409,"y":5.8917103,"p":0,"cluster_ids":["0","1_6","2_48"],"attributes":null,"url":null},{"arg_id":"Acsv-369_0","argument":"舞鶴、京都府北部で安心して妊娠、出産、子育てができるスタートの場所をつくるべきである。","x":13.817432,"y":9.923142,"p":0,"cluster_ids":["0","1_1","2_32"],"attributes":null,"url":null},{"arg_id":"Acsv-369_1","argument":"健康な家族や次世代が育つための学びの場や医療、福祉系の学生がいのちについて学べる場を作るべきである。","x":10.8701725,"y":3.1809173,"p":0,"cluster_ids":["0","1_2","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-369_2","argument":"京都府北部で唯一お産ができる助産院を公的な助産院として運営できる組織にする必要がある。","x":10.899916,"y":7.4106035,"p":0,"cluster_ids":["0","1_5","2_46"],"attributes":null,"url":null},{"arg_id":"Acsv-369_3","argument":"行政、医療機関、教育機関と連携することが重要である。","x":10.801237,"y":3.6994133,"p":0,"cluster_ids":["0","1_3","2_61"],"attributes":null,"url":null},{"arg_id":"Acsv-370_0","argument":"福祉業界で働き貢献したい","x":11.54313,"y":6.498799,"p":0,"cluster_ids":["0","1_6","2_55"],"attributes":null,"url":null},{"arg_id":"Acsv-421_0","argument":"舞鶴の良さをSNSにあげて、興味を持ってもらえるようにするべきである。","x":14.112588,"y":9.264629,"p":0,"cluster_ids":["0","1_1","2_39"],"attributes":null,"url":null},{"arg_id":"Acsv-421_1","argument":"できるだけたくさんの取り組みに参加するべきである。","x":12.605236,"y":6.116918,"p":0,"cluster_ids":["0","1_7","2_5"],"attributes":null,"url":null},{"arg_id":"Acsv-422_0","argument":"舞鶴の良さを伝えていきたい。","x":14.44892,"y":9.627885,"p":0,"cluster_ids":["0","1_1","2_23"],"attributes":null,"url":null},{"arg_id":"Acsv-423_0","argument":"舞鶴ではポイ捨てをしないことを発信すべき","x":14.516482,"y":9.018038,"p":0,"cluster_ids":["0","1_1","2_60"],"attributes":null,"url":null},{"arg_id":"Acsv-424_0","argument":"舞鶴の今の良さを2040年でも続けていきたい","x":14.296665,"y":9.584376,"p":0,"cluster_ids":["0","1_1","2_23"],"attributes":null,"url":null},{"arg_id":"Acsv-425_0","argument":"舞鶴の良さを残していきたい。","x":14.264419,"y":9.531698,"p":0,"cluster_ids":["0","1_1","2_23"],"attributes":null,"url":null},{"arg_id":"Acsv-426_0","argument":"この街を好きになり、街のことに詳しくなりたい。","x":10.233853,"y":5.899317,"p":0,"cluster_ids":["0","1_6","2_33"],"attributes":null,"url":null},{"arg_id":"Acsv-435_0","argument":"子どもたちが自分の人生を自分で決められるという実感を持てる場を作るべきである。","x":12.48267,"y":4.8277884,"p":0,"cluster_ids":["0","1_4","2_29"],"attributes":null,"url":null},{"arg_id":"Acsv-435_1","argument":"子どもたちが学ぶことや挑戦することを楽しめるように、大人が本気で向き合い対話することが重要である。","x":12.045966,"y":4.579796,"p":0,"cluster_ids":["0","1_4","2_31"],"attributes":null,"url":null},{"arg_id":"Acsv-441_0","argument":"みんなを応援したい","x":10.847976,"y":4.739665,"p":0,"cluster_ids":["0","1_3","2_12"],"attributes":null,"url":null},{"arg_id":"Acsv-442_0","argument":"ポイ捨てをしないべきである","x":15.242917,"y":6.4489017,"p":0,"cluster_ids":["0","1_8","2_24"],"attributes":null,"url":null},{"arg_id":"Acsv-445_0","argument":"運動することは重要である。","x":11.450232,"y":2.6146657,"p":0,"cluster_ids":["0","1_2","2_53"],"attributes":null,"url":null},{"arg_id":"Acsv-446_0","argument":"みんなを応援するべき","x":10.9688,"y":4.8461227,"p":0,"cluster_ids":["0","1_3","2_12"],"attributes":null,"url":null},{"arg_id":"Acsv-447_0","argument":"できることを探すべきである。","x":12.470527,"y":5.3712325,"p":0,"cluster_ids":["0","1_4","2_14"],"attributes":null,"url":null},{"arg_id":"Acsv-449_0","argument":"店を増やすべきである","x":12.332766,"y":5.9934707,"p":0,"cluster_ids":["0","1_7","2_17"],"attributes":null,"url":null},{"arg_id":"Acsv-450_0","argument":"ゴミを減らすべきである。","x":14.708331,"y":5.881556,"p":0,"cluster_ids":["0","1_8","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-451_0","argument":"ボランティア活動を進んでするべきである","x":11.611959,"y":6.3532805,"p":0,"cluster_ids":["0","1_6","2_55"],"attributes":null,"url":null},{"arg_id":"Acsv-453_0","argument":"呼びかけることが重要である。","x":10.334835,"y":4.3304386,"p":0,"cluster_ids":["0","1_3","2_20"],"attributes":null,"url":null},{"arg_id":"Acsv-454_0","argument":"地域の活動に参加すべき","x":11.668603,"y":6.920479,"p":0,"cluster_ids":["0","1_5","2_9"],"attributes":null,"url":null},{"arg_id":"Acsv-456_0","argument":"運動したり、食生活を見直したりして健康に気をつけるべき","x":11.088955,"y":2.396043,"p":0,"cluster_ids":["0","1_2","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-459_0","argument":"地域おこしは地域の活性化に重要である。","x":12.011934,"y":7.367614,"p":0,"cluster_ids":["0","1_5","2_43"],"attributes":null,"url":null},{"arg_id":"Acsv-459_1","argument":"地域おこしには地域住民の参加が不可欠である。","x":11.828673,"y":7.5387797,"p":0,"cluster_ids":["0","1_5","2_43"],"attributes":null,"url":null},{"arg_id":"Acsv-460_0","argument":"挨拶をしあい、市民の交流を増やすべき","x":10.267073,"y":5.7126293,"p":0,"cluster_ids":["0","1_6","2_33"],"attributes":null,"url":null},{"arg_id":"Acsv-461_0","argument":"施設をきれいに扱うべきである","x":13.939311,"y":5.6174355,"p":0,"cluster_ids":["0","1_8","2_63"],"attributes":null,"url":null},{"arg_id":"Acsv-462_0","argument":"楽しめるようなイベントを増やすべき","x":12.53535,"y":6.7410526,"p":0,"cluster_ids":["0","1_7","2_51"],"attributes":null,"url":null},{"arg_id":"Acsv-463_0","argument":"市民の意見も取り入れるべきである。","x":10.270938,"y":6.6732388,"p":0,"cluster_ids":["0","1_6","2_45"],"attributes":null,"url":null},{"arg_id":"Acsv-464_0","argument":"ちゃんと運動してご飯もちゃんと食べるべきである。","x":11.211937,"y":2.2186816,"p":0,"cluster_ids":["0","1_2","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-467_0","argument":"ゴミを捨てないべきである","x":14.898607,"y":6.0614347,"p":0,"cluster_ids":["0","1_8","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-468_0","argument":"選挙に行くべきである","x":11.244188,"y":5.8851333,"p":0,"cluster_ids":["0","1_6","2_16"],"attributes":null,"url":null},{"arg_id":"Acsv-469_0","argument":"建物等を丁重に扱うべきである","x":13.556024,"y":5.391554,"p":0,"cluster_ids":["0","1_8","2_28"],"attributes":null,"url":null},{"arg_id":"Acsv-470_0","argument":"いろんなイベントに参加するべきである。","x":12.534214,"y":6.5658727,"p":0,"cluster_ids":["0","1_7","2_56"],"attributes":null,"url":null},{"arg_id":"Acsv-473_0","argument":"いろんなことに挑戦して人との関わりを増やすべき","x":11.488862,"y":5.4134164,"p":0,"cluster_ids":["0","1_6","2_1"],"attributes":null,"url":null},{"arg_id":"Acsv-474_0","argument":"近所に落ちているゴミを拾うべきである。","x":14.733333,"y":5.934043,"p":0,"cluster_ids":["0","1_8","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-475_0","argument":"環境が良い状態であるためにポイ捨てをしないべきである。","x":15.270698,"y":6.3036003,"p":0,"cluster_ids":["0","1_8","2_24"],"attributes":null,"url":null},{"arg_id":"Acsv-477_0","argument":"ゴミのポイ捨てをやめるべきである。","x":14.952599,"y":6.000615,"p":0,"cluster_ids":["0","1_8","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-478_0","argument":"まちを綺麗にするためにゴミ拾いをするべきである。","x":14.19076,"y":5.9157243,"p":0,"cluster_ids":["0","1_8","2_2"],"attributes":null,"url":null},{"arg_id":"Acsv-479_0","argument":"ゴミを捨てないことがゴミ問題に貢献する。","x":14.805732,"y":6.318231,"p":0,"cluster_ids":["0","1_8","2_11"],"attributes":null,"url":null},{"arg_id":"Acsv-480_0","argument":"健康でいるために運動を始めるべきである。","x":11.148477,"y":2.3737192,"p":0,"cluster_ids":["0","1_2","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-481_0","argument":"自然を大切にすべき","x":12.055659,"y":3.8913991,"p":0,"cluster_ids":["0","1_4","2_13"],"attributes":null,"url":null},{"arg_id":"Acsv-481_1","argument":"街・物・食に感謝して利用し、大事にすべき","x":10.385631,"y":5.8830867,"p":0,"cluster_ids":["0","1_6","2_33"],"attributes":null,"url":null},{"arg_id":"Acsv-482_0","argument":"仕事を頑張るべきである。","x":12.2307825,"y":5.263741,"p":0,"cluster_ids":["0","1_4","2_37"],"attributes":null,"url":null},{"arg_id":"Acsv-483_0","argument":"地域の交流に参加すべき","x":11.805973,"y":7.1458244,"p":0,"cluster_ids":["0","1_5","2_9"],"attributes":null,"url":null},{"arg_id":"Acsv-485_0","argument":"みんなが笑顔になれるようなことをするべきである。","x":11.385199,"y":4.7324586,"p":0,"cluster_ids":["0","1_3","2_44"],"attributes":null,"url":null},{"arg_id":"Acsv-487_0","argument":"取組に対して取り組むべきである","x":12.799945,"y":5.7318234,"p":0,"cluster_ids":["0","1_7","2_47"],"attributes":null,"url":null},{"arg_id":"Acsv-488_0","argument":"健康を大切にするべきである","x":11.174222,"y":3.1496336,"p":0,"cluster_ids":["0","1_2","2_3"],"attributes":null,"url":null},{"arg_id":"Acsv-488_1","argument":"ごみ拾いのボランティアを作るべきである","x":14.018011,"y":6.278107,"p":0,"cluster_ids":["0","1_8","2_41"],"attributes":null,"url":null},{"arg_id":"Acsv-489_0","argument":"応援することは重要である。","x":10.471066,"y":4.252047,"p":0,"cluster_ids":["0","1_3","2_20"],"attributes":null,"url":null},{"arg_id":"Acsv-492_0","argument":"様々なことに挑戦するべきである。","x":12.520794,"y":5.363571,"p":0,"cluster_ids":["0","1_4","2_14"],"attributes":null,"url":null},{"arg_id":"Acsv-494_0","argument":"ボランティア活動に参加すべき","x":11.723781,"y":6.4934034,"p":0,"cluster_ids":["0","1_6","2_38"],"attributes":null,"url":null},{"arg_id":"Acsv-494_1","argument":"ゴミを捨てないようにすべき","x":14.89446,"y":6.1140556,"p":0,"cluster_ids":["0","1_8","2_49"],"attributes":null,"url":null},{"arg_id":"Acsv-495_0","argument":"たくさんの人と関わって活気ある街にするために頑張るべき","x":10.654011,"y":6.0088115,"p":0,"cluster_ids":["0","1_6","2_15"],"attributes":null,"url":null},{"arg_id":"Acsv-499_0","argument":"健康でいるために生活習慣をきちんとするべきである","x":11.000316,"y":2.7504563,"p":0,"cluster_ids":["0","1_2","2_35"],"attributes":null,"url":null},{"arg_id":"Acsv-500_0","argument":"繁栄をするために路上にゴミがあったら拾って綺麗にするべきである。","x":14.466901,"y":5.725,"p":0,"cluster_ids":["0","1_8","2_62"],"attributes":null,"url":null},{"arg_id":"Acsv-501_0","argument":"地域住民と観光客が楽しめるイベントの発達が必要である。","x":12.414167,"y":7.127841,"p":0,"cluster_ids":["0","1_7","2_18"],"attributes":null,"url":null},{"arg_id":"Acsv-501_1","argument":"舞鶴フェスの規模を拡大すべきである。","x":13.786782,"y":9.284785,"p":0,"cluster_ids":["0","1_1","2_42"],"attributes":null,"url":null},{"arg_id":"Acsv-501_2","argument":"赤レンガパークでのイベントを積極的に発信するべきである。","x":13.09358,"y":6.9560823,"p":0,"cluster_ids":["0","1_7","2_26"],"attributes":null,"url":null},{"arg_id":"Acsv-501_3","argument":"舞鶴の歴史的景観についての説明を行うべきである。","x":14.420164,"y":9.705555,"p":0,"cluster_ids":["0","1_1","2_23"],"attributes":null,"url":null},{"arg_id":"Acsv-502_0","argument":"栄養バランスの取れた食事を食べるべき","x":10.872497,"y":2.3018146,"p":0,"cluster_ids":["0","1_2","2_36"],"attributes":null,"url":null},{"arg_id":"Acsv-502_1","argument":"適度な運動をするべき","x":11.311479,"y":2.1579437,"p":0,"cluster_ids":["0","1_2","2_10"],"attributes":null,"url":null},{"arg_id":"Acsv-503_0","argument":"一人一人がポイ捨てをしないべきである。","x":15.354019,"y":6.1812305,"p":0,"cluster_ids":["0","1_8","2_24"],"attributes":null,"url":null},{"arg_id":"Acsv-507_0","argument":"活動している人を応援したい","x":10.730781,"y":4.845741,"p":0,"cluster_ids":["0","1_3","2_12"],"attributes":null,"url":null},{"arg_id":"Acsv-507_1","argument":"体が崩れないよう運動をするべき","x":11.728903,"y":2.1546063,"p":0,"cluster_ids":["0","1_2","2_10"],"attributes":null,"url":null}],"clusters":[{"level":0,"id":"0","label":"全体","takeaway":"","value":133,"parent":"","density_rank_percentile":0},{"level":1,"id":"1_6","label":"地域社会の活性化と市民参加の促進","takeaway":"地域社会の活性化を目指し、市民同士の交流やコミュニケーションを重視することが求められています。挨拶やボランティア活動を通じて人とのつながりを深めることが重要であり、特に高齢者や若者が共に参加できる機会を提供することが期待されています。また、市議会の透明性を高め、市民の意見を反映させることで、より信頼される地域づくりが進むと考えられています。地域愛を育むためには、街の文化や特産物を大切にし、選挙参加を通じて市民の責任を果たすことも重要です。","value":22,"parent":"0","density_rank_percentile":1},{"level":1,"id":"1_7","label":"地域活性化と参加促進のための多様なイベントの推進","takeaway":"地域住民や観光客が共に楽しめるイベントの充実を図るため、青少年団体のネットワーク強化や参加型コミュニティの促進が求められています。また、観光地の魅力を伝えるためのポスター制作や、自然環境の美化活動を通じて地域の魅力を発信することが重要です。さらに、参加者が楽しめるイベントの質を向上させる施策や、インセンティブ制度の導入により、より多くの人々が参加しやすい環境を整えることが期待されています。","value":15,"parent":"0","density_rank_percentile":0.875},{"level":1,"id":"1_2","label":"健康維持と寿命延伸のための包括的なライフスタイル改善","takeaway":"健康を維持し、健康寿命を延ばすためには、運動、食事、生活習慣の見直しが不可欠です。特に高齢者に向けた体操の継続促進や、食事療法の普及が重要視されています。また、健康的な生活を送るための教育や、次世代への意識づけも必要であり、地域全体で健康を支える取り組みが求められています。","value":20,"parent":"0","density_rank_percentile":0.25},{"level":1,"id":"1_4","label":"子どもたちの主体的な成長を促す実体験の重要性","takeaway":"子どもたちが自分の人生を主体的に選択できる環境を整えることが重要であり、実体験を通じた学びや成長が不可欠です。工作や実験などの実践的な経験を重視し、子どもたちが挑戦する機会を提供することで、自己表現やコミュニケーション能力の向上が期待されます。また、大人が真剣に向き合い、子どもたちとの関わりを通じて共に成長することが、教育の質を高める鍵となります。さらに、自然環境の保護や高専祭の活性化を通じて、コミュニティの絆を深めることも重要です。","value":15,"parent":"0","density_rank_percentile":0.625},{"level":1,"id":"1_1","label":"舞鶴市の地域活性化と子育て支援の強化","takeaway":"舞鶴市における地域活性化と子育て支援の重要性が強調されています。地域のイベントやフェスの拡大を通じて、舞鶴の魅力を広め、近隣地域からの来場者を増やすことが求められています。また、安心して妊娠・出産・子育てができる環境を整えることや、舞鶴の自然や歴史的景観を次世代に継承する取り組みが必要です。市民の積極的な参加や地域愛が、舞鶴の活力を生む鍵となるでしょう。","value":20,"parent":"0","density_rank_percentile":0.125},{"level":1,"id":"1_3","label":"世代を超えたつながりと共感の促進","takeaway":"世代を超えた思いやりや配慮を重視し、コミュニティ全体の絆を深めることが求められています。挨拶や呼びかけを通じて良好な人間関係を築き、若い人々や活動している人々を応援することで、共感と支援の輪を広げることが重要です。また、スポーツ指導においては、行政や医療機関、教育機関との連携を強化し、より効果的な指導と選手の健康管理を実現することが期待されています。全体として、笑顔を生む活動を通じて、より良い社会を築くことが目指されています。","value":13,"parent":"0","density_rank_percentile":0.5},{"level":1,"id":"1_5","label":"地域活性化に向けた住民参加と支援制度の強化","takeaway":"地域の活性化には住民の積極的な参加が不可欠であり、地域活動や交流を通じてコミュニケーションを深めることが重要です。また、地方自治体は都会と同等の支援を行うべきであり、現行の支援制度の見直しが求められています。特に、人口が少ない地域においては、インクルーシブなサービスの充実や地域資源を活用した地場産業の発展が必要であり、地域住民が協力して取り組むことが地域の持続可能な発展に繋がると期待されています。","value":12,"parent":"0","density_rank_percentile":0.75},{"level":1,"id":"1_8","label":"地域環境保護と美化のための責任ある行動の促進","takeaway":"地域の遺構や環境を保護し、美化するためには、住民一人一人の責任ある行動が不可欠です。ゴミの分別やポイ捨ての禁止、ゴミ拾いのボランティア活動を通じて、持続可能なコミュニティを形成し、地域全体の魅力を高めることが求められています。また、公共施設や建物を丁寧に扱うことも、地域の文化的価値を尊重し、快適な環境を維持するために重要です。","value":16,"parent":"0","density_rank_percentile":0.375},{"level":2,"id":"2_52","label":"人とのつながりを求めるコミュニケーションの重要性","takeaway":"この意見は、多くの人と交流し、意見や情報を共有することの重要性を強調しています。人とのつながりを通じて得られる新たな視点や経験が、個人の成長や理解を深めることに寄与するという考えが中心です。","value":1,"parent":"1_6","density_rank_percentile":0.015625},{"level":2,"id":"2_48","label":"夢の実現を支援する多様な活動の促進","takeaway":"この意見グループは、働く、遊ぶ、学ぶという多様な活動を通じて、有志が夢を実現することを目指しています。また、新しい挑戦の機会を提供するために、参加者を公募することや、海洋少年団の活動を活発化させることが重要であると考えています。これにより、団員たちに多様な経験や体験を提供し、成長を促すことが期待されています。","value":3,"parent":"1_6","density_rank_percentile":0.953125},{"level":2,"id":"2_17","label":"青少年団体のネットワーク強化と活動拠点の拡充","takeaway":"この意見グループは、青少年団体が相互に繋がるための枠組みの必要性と、活動の場を提供するための店舗の増加を求める声が中心です。青少年が集まりやすい環境を整えることで、より多くの交流や活動が促進されることを目指しています。","value":2,"parent":"1_7","density_rank_percentile":0.921875},{"level":2,"id":"2_53","label":"運動と健康維持の重要性とトレーニング法の普及","takeaway":"この意見グループは、運動の重要性を強調し、体を動かすことによる疲労回復や健康維持のためのケアの必要性を訴えています。また、健康寿命を延ばすための効果的なトレーニング法の普及が求められている点が特徴です。","value":3,"parent":"1_2","density_rank_percentile":0.859375},{"level":2,"id":"2_25","label":"健康寿命延伸のための食事療法の普及","takeaway":"この意見は、健康寿命を延ばすために食事療法の重要性を強調しており、食事の改善が健康維持に寄与することを訴えています。食事療法の普及が、より多くの人々に健康的な生活を促進する手段として必要であるという考えが中心です。","value":1,"parent":"1_2","density_rank_percentile":0.03125},{"level":2,"id":"2_29","label":"子どもの自立と実体験の重要性","takeaway":"この意見グループは、子どもたちが自分の人生を主体的に選択できる環境を整えることの重要性や、思いやりのある行動を促すこと、さらには実体験を通じた成長の必要性についての懸念が中心です。また、スマホやテレビゲームが子どもたちの貴重な体験時間を奪っていることへの警鐘も含まれています。","value":3,"parent":"1_4","density_rank_percentile":0.984375},{"level":2,"id":"2_37","label":"実体験を通じた学びと成長の促進","takeaway":"この意見グループは、実体験を重視し、工作や実験を通じて得られる学びの重要性を強調しています。また、努力を通じて得られる成果や恩恵を期待する姿勢が見受けられ、実践的な経験が成長に繋がることを望む意見が中心です。","value":3,"parent":"1_4","density_rank_percentile":0.828125},{"level":2,"id":"2_14","label":"子どもたちとの活動を通じた自己成長と挑戦","takeaway":"この意見グループは、子どもたちとの関わりを通じて自己の体力や能力を活かし、様々な挑戦をすることの重要性を強調しています。参加することで新たな経験を得ることや、自分の可能性を広げることに対する前向きな姿勢が見受けられます。","value":3,"parent":"1_4","density_rank_percentile":0.609375},{"level":2,"id":"2_35","label":"健康維持のための生活習慣の重要性","takeaway":"この意見グループは、健康を維持するためには日々の生活習慣を整えることが不可欠であるという考えが中心です。また、特に高齢者にとっても健康的な生活習慣が重要であり、全世代にわたって健康第一の意識を持つべきだという点が強調されています。","value":3,"parent":"1_2","density_rank_percentile":0.90625},{"level":2,"id":"2_5","label":"参加型コミュニティの促進とインセンティブ制度の導入","takeaway":"この意見グループは、年代を問わず誰でも参加しやすい取り組みを推進し、参加を促すためのインセンティブ制度（ポイント制度など）を導入することの重要性を強調しています。多様な参加者が集まる場を作ることで、コミュニティの活性化を図ることが目的です。","value":3,"parent":"1_7","density_rank_percentile":0.484375},{"level":2,"id":"2_50","label":"舞鶴市の子育て環境と地域情報の充実","takeaway":"この意見グループは、舞鶴市における子育て支援の向上や地域の魅力を発信することに焦点を当てています。子どもを安心して産み育てられる環境を整えることや、舞鶴市の特性を活かした情報発信の重要性が強調されており、地域の活性化と市政への関心を高めることが求められています。","value":4,"parent":"1_1","density_rank_percentile":0.703125},{"level":2,"id":"2_45","label":"市議会の透明性と市民参加の重要性","takeaway":"この意見グループは、市議会が是々非々の姿勢を持ち、市民の意見を積極的に取り入れることの重要性を強調しています。市民の声を反映させることで、より透明で信頼される政治が実現されるべきだという考えが中心です。","value":2,"parent":"1_6","density_rank_percentile":0.515625},{"level":2,"id":"2_34","label":"高齢者向け体操継続促進のための特典提供","takeaway":"この意見は、地域の高齢者が体操を継続するために、旅行などの特典を提供することが効果的であるという考えに基づいています。特典を通じて高齢者のモチベーションを高め、体操の参加率を向上させることが期待されています。","value":1,"parent":"1_2","density_rank_percentile":0.046875},{"level":2,"id":"2_57","label":"高齢者向け健康促進と参加促進の戦略","takeaway":"この意見は、高齢者の参加を促すために健康やお得感を提供することの重要性に焦点を当てています。具体的には、健康を意識したプログラムや特典を通じて、より多くの高齢者が活動に参加する可能性が高まるという前向きな視点が示されています。","value":1,"parent":"1_2","density_rank_percentile":0.0625},{"level":2,"id":"2_6","label":"公園でのコミュニケーション促進と地域のつながり","takeaway":"この意見は、公園でのすれ違う人々との挨拶を通じて、地域社会のつながりやコミュニケーションを大切にしたいという思いが表れています。挨拶を重視することで、より親しみやすい雰囲気を作り出し、地域の人々の交流を促進することが目的とされています。","value":1,"parent":"1_6","density_rank_percentile":0.078125},{"level":2,"id":"2_27","label":"世代を超えた思いやりとつながりの重要性","takeaway":"この意見グループは、幼い子供から高齢者まで、すべての世代に対する思いやりや配慮の重要性を強調しています。また、世代間のつながりを大切にすることで、コミュニティ全体の絆が深まることが重要であるという考えが中心です。","value":2,"parent":"1_3","density_rank_percentile":1},{"level":2,"id":"2_42","label":"舞鶴フェスの拡大と地域活性化","takeaway":"この意見グループは、舞鶴フェスの規模を拡大することで、舞鶴の魅力を広く知ってもらい、近隣地域からの来場者を増やすことを目指しています。また、地域のイベントへの参加を促進することで、地域全体の活性化を図る意見が中心となっています。","value":3,"parent":"1_1","density_rank_percentile":0.8125},{"level":2,"id":"2_12","label":"共感と支援の輪を広げる応援の精神","takeaway":"この意見グループは、若い人々や活動している人々に対する応援の気持ちが強く表れています。個々の努力や活動を支援し、共に成長していくことの重要性を強調しており、全体として「みんなを応援する」という共通のテーマが見受けられます。","value":5,"parent":"1_3","density_rank_percentile":0.875},{"level":2,"id":"2_3","label":"健康の重要性と次世代への教育","takeaway":"この意見グループは、健康が個人の生活の質を向上させるだけでなく、他者への思いやりや社会全体の幸福にも寄与するという考えに基づいています。また、健康な家族や次世代を育成するために、医療や福祉に関する教育の重要性を強調しており、健康を維持するための学びの場の必要性が示されています。","value":4,"parent":"1_2","density_rank_percentile":0.671875},{"level":2,"id":"2_9","label":"地域交流と参加の重要性","takeaway":"この意見グループは、地域活動や交流に参加することの重要性を強調しています。地域の行事に参加することで、普段から地域の人々と顔を合わせ、コミュニケーションを深めることが大切であるという考えが中心です。","value":3,"parent":"1_5","density_rank_percentile":0.84375},{"level":2,"id":"2_39","label":"舞鶴湾の魅力をSNSで発信する重要性","takeaway":"この意見グループは、舞鶴の美しい自然や観光地をSNSを通じて広めることの重要性に焦点を当てています。特に、凪凪とした舞鶴湾の魅力を伝えることで、他者の興味を引き、舞鶴の観光促進につながる可能性があるという点が強調されています。","value":2,"parent":"1_1","density_rank_percentile":0.59375},{"level":2,"id":"2_41","label":"地域の遺構保護と環境美化のためのボランティア活動","takeaway":"この意見グループは、地域の遺構を保護し、環境を美化するためにボランティア活動を推進する必要性を強調しています。特に、赤レンガ以外の遺構にも注目し、地域全体の魅力を高めるための取り組みが求められています。","value":2,"parent":"1_8","density_rank_percentile":0.75},{"level":2,"id":"2_22","label":"白崎の滝の美しさと自然の魅力","takeaway":"この意見は、白崎の滝の美しさに対する感動や評価を表現しています。滝の景観が持つ自然の魅力や、訪れることで得られる癒しの体験に焦点を当てており、観光地としての価値や自然の美しさを称賛する内容が中心です。","value":1,"parent":"1_1","density_rank_percentile":0.09375},{"level":2,"id":"2_7","label":"舞鶴の地域愛と支援の意志","takeaway":"この意見グループは、舞鶴の人々の親しみやすさや親切さに対する評価と、看護師として地域を支えたいという強い意志、さらに舞鶴の魅力を広める必要性に焦点を当てています。地域への愛情と貢献の意欲が表れています。","value":3,"parent":"1_1","density_rank_percentile":0.625},{"level":2,"id":"2_21","label":"ボランティア活動の促進と社会貢献の重要性","takeaway":"この意見は、ボランティア活動がより広く推進されることへの期待を表現しています。社会貢献の意義や、地域社会への影響を考慮し、ボランティア活動が活発化することで、より良い社会を築くことができるという前向きな視点が中心です。","value":1,"parent":"1_6","density_rank_percentile":0.109375},{"level":2,"id":"2_55","label":"高齢者の社会貢献とボランティア活動の推進","takeaway":"この意見グループは、65歳以上の高齢者に対してボランティア活動を促進することの重要性を強調しています。福祉業界での貢献を希望する声や、ボランティア活動を積極的に行うべきとの意見が集まり、高齢者が社会に貢献する機会を提供することの意義が示されています。","value":3,"parent":"1_6","density_rank_percentile":0.53125},{"level":2,"id":"2_38","label":"ボランティア活動の参加促進とポイント制度の導入","takeaway":"この意見グループは、ボランティア活動への参加を促進するためにポイント制度を導入することの重要性を強調しています。参加者が報酬を得られる仕組みを作ることで、より多くの人々がボランティア活動に参加しやすくなるという前向きな提案が中心です。","value":2,"parent":"1_6","density_rank_percentile":0.453125},{"level":2,"id":"2_54","label":"自己表現と意見の明確化の重要性","takeaway":"この意見は、自分の意見や感情を率直に表現することの重要性に焦点を当てています。特に、否定的な意見や不満をしっかりと伝えることで、より良いコミュニケーションや関係構築が可能になるという考えが示されています。","value":1,"parent":"1_4","density_rank_percentile":0.125},{"level":2,"id":"2_31","label":"高専教育の重要性と大人の関与","takeaway":"この意見グループは、高専教育の価値を強調し、学生が学びや挑戦を楽しむためには、大人が真剣に向き合い対話することが不可欠であるという考えが中心です。また、高専の教育機関としての素晴らしさを広める必要性も示されています。","value":3,"parent":"1_4","density_rank_percentile":0.78125},{"level":2,"id":"2_40","label":"挨拶の重要性とコミュニケーションの基盤","takeaway":"この意見は、挨拶が人間関係やコミュニケーションの基本であり、相手に対する敬意や親しみを示す重要な行為であることを強調しています。挨拶を通じて、良好な関係を築くための第一歩としての役割が認識されています。","value":1,"parent":"1_3","density_rank_percentile":0.140625},{"level":2,"id":"2_4","label":"地方自治体の支援制度の見直しと強化","takeaway":"この意見グループは、地方自治体が都会と同等の支援を行うべきであるという主張を中心に、現行の支援制度の厳しさや不十分さに対する不満が表れています。支援制度の見直しを通じて、より多くの市民が恩恵を受けられるようにする必要性が強調されています。","value":3,"parent":"1_5","density_rank_percentile":0.6875},{"level":2,"id":"2_30","label":"市との協働による共感活動の推進","takeaway":"この意見は、市が行う活動に対して個人が共感し、一緒に参加したいという意欲を示しています。市の取り組みと個人の価値観が一致することで、より積極的に地域活動に関与したいという姿勢が表れています。","value":1,"parent":"1_6","density_rank_percentile":0.15625},{"level":2,"id":"2_19","label":"自然環境の美化と保護活動の推進","takeaway":"この意見グループは、公園や海、山などの自然環境に対する美化活動の重要性を強調しており、地域の自然を守り、より良い環境を次世代に引き継ぐための具体的な行動を促す内容が中心です。","value":1,"parent":"1_7","density_rank_percentile":0.171875},{"level":2,"id":"2_11","label":"公害防止と持続可能なコミュニティ形成","takeaway":"この意見グループは、ゴミの分別や廃棄物の管理を通じて公害を防ぎ、環境に配慮した持続可能なコミュニティを構築することの重要性を強調しています。ゴミを捨てないことが、地域社会の環境問題解決に寄与するという前向きな視点が中心です。","value":2,"parent":"1_8","density_rank_percentile":0.578125},{"level":2,"id":"2_8","label":"高専祭の活性化と盛り上げの重要性","takeaway":"この意見は、高専祭をより盛り上げることの重要性に焦点を当てています。参加者や関係者が一体となってイベントを楽しむことで、学校のコミュニティが強化され、学生生活がより充実したものになるという前向きな視点が示されています。","value":1,"parent":"1_4","density_rank_percentile":0.1875},{"level":2,"id":"2_15","label":"地域活性化とスポーツ振興の重要性","takeaway":"この意見グループは、地域の活気を高めるために多くの人々と関わることの重要性と、スポーツ活動、特にバレーを通じて目標を達成することの意義に焦点を当てています。地域の活性化とスポーツの振興が相互に関連していることを示唆しています。","value":2,"parent":"1_6","density_rank_percentile":0.640625},{"level":2,"id":"2_58","label":"観光地プロモーションの重要性とポスター制作の必要性","takeaway":"この意見は、観光地の魅力を効果的に伝えるためにポスターを作成することの重要性を強調しています。観光地の認知度を高め、訪問者を増やすための視覚的なアプローチとして、ポスターが有効であるという点が中心にあります。","value":1,"parent":"1_7","density_rank_percentile":0.203125},{"level":2,"id":"2_61","label":"多機関連携によるスポーツ指導の重要性","takeaway":"この意見グループは、スポーツ指導者が行政、医療機関、教育機関と連携することの重要性に焦点を当てています。各機関との協力を通じて、より効果的なスポーツ指導や選手の健康管理、教育的支援が可能になるという視点が強調されています。","value":2,"parent":"1_3","density_rank_percentile":0.71875},{"level":2,"id":"2_0","label":"舞鶴市の地域活性化と市民参加の重要性","takeaway":"この意見グループは、舞鶴市で開催される催しに参加することの重要性を強調しており、地域のイベントを通じて市民が市の現状を理解し、地域活性化に寄与することが求められています。市民の積極的な参加が、地域のつながりや活力を生む鍵であるという視点が中心です。","value":1,"parent":"1_1","density_rank_percentile":0.21875},{"level":2,"id":"2_10","label":"健康維持のための適度な運動の重要性","takeaway":"この意見グループは、身体の健康を維持し、元気でいるために適度な運動が必要であるという共通の認識を示しています。運動が身体の崩れを防ぎ、日常生活の活力を保つための重要な要素であることが強調されています。","value":3,"parent":"1_2","density_rank_percentile":0.96875},{"level":2,"id":"2_33","label":"地域愛と市民交流の促進","takeaway":"この意見グループは、地域に対する愛着や感謝の気持ちを持ち、街の文化や特産物を大切にすることの重要性を強調しています。また、市民同士の交流を促進するための挨拶やコミュニケーションの重要性についても言及されており、地域の活性化に向けた前向きな姿勢が見受けられます。","value":4,"parent":"1_6","density_rank_percentile":0.65625},{"level":2,"id":"2_43","label":"地域活性化に向けた住民参加型の地域おこし","takeaway":"この意見グループは、地域おこしが地域の活性化において重要な役割を果たすことを強調しています。また、農地を活用した地場産業の発展や地域住民の積極的な参加が不可欠であるという点が共通しており、地域の資源を最大限に活かすための協力と取り組みの重要性が示されています。","value":3,"parent":"1_5","density_rank_percentile":0.890625},{"level":2,"id":"2_46","label":"地域支援とインクルーシブなサービスの推進","takeaway":"この意見グループは、人口が少ない地域における産業や事業の支援の必要性に焦点を当てています。特に、インクルーシブな学校運営モデルや公的な助産院の運営に関する理解と支援を求める声があり、地域の特性に応じたサービスの充実が重要であることが示されています。","value":3,"parent":"1_5","density_rank_percentile":0.9375},{"level":2,"id":"2_51","label":"参加者を楽しませるイベントの充実と促進","takeaway":"この意見グループは、参加者が楽しめるイベントの増加と、参加を促す施策の必要性に焦点を当てています。イベントの質を向上させることで、より多くの人々が参加しやすくなり、コミュニティの活性化が期待されるという前向きな提案が含まれています。","value":2,"parent":"1_7","density_rank_percentile":0.5},{"level":2,"id":"2_26","label":"赤レンガパークイベントの活性化と広報強化","takeaway":"この意見グループは、赤レンガパークでのイベントをより多くの人々に知ってもらうための積極的な広報活動と、イベント自体を盛り上げるための具体的な取り組みの必要性に焦点を当てています。イベントの魅力を引き出し、参加者を増やすための戦略が求められています。","value":2,"parent":"1_7","density_rank_percentile":0.765625},{"level":2,"id":"2_59","label":"個人のデザイン・文化芸術への貢献と発表の重要性","takeaway":"この意見は、デザインや文化芸術の分野において、個人が自分の能力を活かし、一つずつ取り組んで成果を発表することの重要性を強調しています。自己表現やスキルの向上を通じて、文化的な価値を創造し、他者との共有を促進する姿勢が見受けられます。","value":1,"parent":"1_7","density_rank_percentile":0.234375},{"level":2,"id":"2_32","label":"舞鶴における安心の妊娠・出産・子育て支援拠点の必要性","takeaway":"この意見は、舞鶴市および京都府北部において、妊娠、出産、子育てを安心して行える環境を整えることの重要性を強調しています。地域における支援体制の充実が求められており、特に新たなスタート地点としての拠点の設立が必要であるという視点が中心です。","value":1,"parent":"1_1","density_rank_percentile":0.25},{"level":2,"id":"2_23","label":"舞鶴の魅力と歴史的景観の継承","takeaway":"この意見グループは、舞鶴の魅力を伝え、歴史的景観を保護し、未来にわたってその良さを継承していくことの重要性に焦点を当てています。舞鶴の文化や歴史を次世代に引き継ぐための具体的な取り組みや、2040年に向けた持続可能な発展の必要性が強調されています。","value":4,"parent":"1_1","density_rank_percentile":0.546875},{"level":2,"id":"2_60","label":"舞鶴市におけるポイ捨て防止の啓発活動","takeaway":"この意見は、舞鶴市においてポイ捨てをしないことの重要性を広める必要性を強調しています。地域の環境保護や美化を促進するために、ポイ捨て防止のメッセージを発信することが求められているという点が中心です。","value":1,"parent":"1_1","density_rank_percentile":0.265625},{"level":2,"id":"2_24","label":"ポイ捨て禁止による環境保護の重要性","takeaway":"この意見グループは、個々の行動としてのポイ捨てを避けることが、環境を良好に保つために不可欠であるという認識を示しています。ポイ捨てをしないことが、持続可能な環境を維持するための基本的な責任であるという共通の意見が集約されています。","value":3,"parent":"1_8","density_rank_percentile":0.5625},{"level":2,"id":"2_49","label":"地域環境保護とゴミ削減の重要性","takeaway":"この意見グループは、ゴミのポイ捨てをやめることや、ゴミを減らすことの重要性を強調しています。また、地域の環境を守るために、近所のゴミを拾う行動を促す意見も含まれており、地域社会全体での環境保護の意識向上が求められています。","value":5,"parent":"1_8","density_rank_percentile":0.734375},{"level":2,"id":"2_20","label":"コミュニケーションとサポートの重要性","takeaway":"この意見グループは、他者に対する呼びかけや応援が重要であるという共通の認識を示しています。コミュニケーションを通じてのつながりや、相手を支えることが、個人や集団の成長やモチベーション向上に寄与するという点が強調されています。","value":2,"parent":"1_3","density_rank_percentile":0.46875},{"level":2,"id":"2_36","label":"健康維持のための運動と栄養管理の重要性","takeaway":"この意見グループは、健康を維持するために運動を行い、食生活を見直すことの重要性に焦点を当てています。運動と栄養バランスの取れた食事が、健康的な生活を送るために不可欠であるという共通の認識が見られます。","value":4,"parent":"1_2","density_rank_percentile":0.796875},{"level":2,"id":"2_63","label":"施設の清潔さと利用マナーの重要性","takeaway":"この意見は、公共施設や利用する場所に対する清潔さの維持と、利用者のマナーの重要性を強調しています。施設をきれいに扱うことは、他の利用者に対する配慮や、快適な環境を保つために必要不可欠であるという認識が示されています。","value":1,"parent":"1_8","density_rank_percentile":0.28125},{"level":2,"id":"2_16","label":"市民の責任としての選挙参加の重要性","takeaway":"この意見は、選挙に参加することが市民の重要な責任であるという認識を示しています。選挙を通じて自分の意見を反映させることが民主主義の基本であり、社会に対する影響力を持つことが強調されています。","value":1,"parent":"1_6","density_rank_percentile":0.296875},{"level":2,"id":"2_28","label":"建物の尊重と適切な管理の重要性","takeaway":"この意見は、建物や施設を丁重に扱うことの重要性に焦点を当てています。建物は文化的、歴史的な価値を持つ場合が多く、その適切な管理や尊重が求められるという考えが中心です。","value":1,"parent":"1_8","density_rank_percentile":0.3125},{"level":2,"id":"2_56","label":"多様なイベント参加の重要性","takeaway":"この意見は、様々なイベントに参加することの意義や利点を強調しています。イベント参加を通じて得られる経験やネットワーキングの機会、知識の拡充などが重要視されており、個人の成長やキャリア形成に寄与することが期待されています。","value":1,"parent":"1_7","density_rank_percentile":0.328125},{"level":2,"id":"2_1","label":"人との関わりを深めるための挑戦と成長","takeaway":"この意見グループは、個人が新しいことに挑戦することで人間関係を広げ、より多くの人と関わることの重要性を強調しています。挑戦を通じて得られる経験や学びが、社会的なつながりを強化し、自己成長につながるという前向きな視点が中心です。","value":1,"parent":"1_6","density_rank_percentile":0.34375},{"level":2,"id":"2_2","label":"地域美化活動としてのゴミ拾いの重要性","takeaway":"この意見は、地域の環境を改善し、まちを綺麗に保つためにゴミ拾いが必要であるという考えを示しています。地域住民が積極的に参加することで、環境意識の向上やコミュニティの結束を促進することが期待されます。","value":1,"parent":"1_8","density_rank_percentile":0.359375},{"level":2,"id":"2_13","label":"自然環境保護の重要性","takeaway":"この意見は、自然環境の保護とその重要性に対する認識を示しています。自然を大切にすることは、持続可能な社会の実現や生態系の保全に繋がるという考えが中心となっており、環境問題への関心が高まっていることが反映されています。","value":1,"parent":"1_4","density_rank_percentile":0.375},{"level":2,"id":"2_44","label":"笑顔を生む活動の重要性","takeaway":"この意見は、社会やコミュニティにおいて、みんなが笑顔になれるような活動や取り組みの必要性を強調しています。人々の幸福感やポジティブな感情を促進することが、より良い環境を作るために重要であるという考えが中心です。","value":1,"parent":"1_3","density_rank_percentile":0.390625},{"level":2,"id":"2_47","label":"積極的な取り組みの重要性","takeaway":"この意見は、特定の取組に対して積極的に取り組むべきであるという姿勢を示しています。取り組むことの重要性や、その結果として得られる成果に対する期待が中心となっており、行動を促す意義が強調されています。","value":1,"parent":"1_7","density_rank_percentile":0.40625},{"level":2,"id":"2_62","label":"地域の美化と繁栄のための責任ある行動","takeaway":"この意見は、地域の繁栄を促進するためには、路上のゴミを拾って環境を綺麗に保つことが重要であるという考えを示しています。地域の美化は、住民の意識や行動に依存しており、個々の責任感が地域全体の発展に寄与するという前向きなメッセージが含まれています。","value":1,"parent":"1_8","density_rank_percentile":0.421875},{"level":2,"id":"2_18","label":"地域住民と観光客の共生を促進するイベントの必要性","takeaway":"この意見は、地域住民と観光客が共に楽しめるイベントの重要性を強調しています。地域の活性化や観光促進を図るためには、両者が参加できるイベントの発展が求められており、地域の文化や魅力を発信する機会が必要であるという視点が中心です。","value":1,"parent":"1_7","density_rank_percentile":0.4375}],"comments":{},"propertyMap":{},"translations":{},"overview":"地域社会の活性化には市民参加が不可欠であり、交流やボランティア活動を通じて信頼関係を築くことが求められています。また、健康維持や子どもたちの成長を促すための教育や実体験の重要性が強調され、世代を超えたつながりや地域環境の保護も重要なテーマです。舞鶴市では地域イベントの拡大や子育て支援が求められ、住民の協力による持続可能な発展が期待されています。","config":{"name":"2455a1d9-5bf1-495f-a02d-67ca8ac37f0f","input":"2455a1d9-5bf1-495f-a02d-67ca8ac37f0f","question":"④市民として、2040年に向けてできることとは【1/31 15:00 更新】","intro":"地域社会の活性化には、住民同士の交流や多様なイベントの開催が重要であり、青少年育成や健康促進、世代間の対話が求められています。また、舞鶴市の魅力を発信し、観光を促進する取り組みや市民参加による透明性のある運営が地域の信頼を築く基盤となります。持続可能性を高めるための環境美化活動やコミュニティの協力も強調され、地域全体の活力向上が期待されています。\n分析対象となったデータの件数は508件で、これらのデータに対してOpenAI API (gpt-4o-mini)を用いて133件の意見（議論）を抽出し、クラスタリングを行った。\n","model":"gpt-4o-mini","provider":"openai","is_pubcom":true,"is_embedded_at_local":false,"local_llm_address":null,"extraction":{"prompt":"あなたは専門的なリサーチアシスタントです。与えられたテキストから、意見を抽出して整理してください。\n\n# 指示\n* 入出力の例に記載したような形式で文字列のリストを返してください\n  * 必要な場合は2つの別個の意見に分割してください。多くの場合は1つの議論にまとめる方が望ましいです。\n* 整理した意見は日本語で出力してください\n\n## 入出力の例\n/human\n\nAIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\"\n  ]\n}\n\n/human\n\nAIの能力、限界、倫理的考慮事項について、市民を教育する必要がある。また、教育できる人材を養成する必要がある。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIの能力、限界、倫理的考慮事項について、市民を教育すべき\",\n    \"AIに関する教育をできる人材を養成すべき\"\n  ]\n}\n\n/human\n\nAIはエネルギーグリッドを最適化し、無駄や炭素排出を削減できます。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIはエネルギーグリッドを最適化して炭素排出を削減できる\"\n  ]\n}\n","workers":30,"limit":508,"properties":[],"categories":{},"category_batch_size":5,"source_code":"$19","model":"gpt-4o-mini"},"hierarchical_clustering":{"cluster_nums":[8,64],"source_code":"$1a"},"hierarchical_initial_labelling":{"prompt":"あなたはKJ法が得意なデータ分析者です。userのinputはグループに集まったラベルです。なぜそのラベルが一つのグループであるか解説し、表札（label）をつけてください。\n表札については、グループ内の具体的な論点や特徴を反映した、具体性の高い名称を考案してください。\n出力はJSONとし、フォーマットは以下のサンプルを参考にしてください。\n\n\n# サンプルの入出力\n## 入力例\n- 手作業での意見分析は時間がかかりすぎる。AIで効率化できると嬉しい\n- 今のやり方だと分析に工数がかかりすぎるけど、AIならコストをかけずに分析できそう\n- AIが自動で意見を整理してくれると楽になって嬉しい\n\n\n## 出力例\n{\n    \"label\": \"AIによる業務効率の大幅向上とコスト効率化\",\n    \"description\": \"この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$1b","model":"gpt-4o-mini"},"hierarchical_merge_labelling":{"prompt":"あなたはデータ分析のエキスパートです。\n現在、テキストデータの階層クラスタリングを行っています。\n下層のクラスタ（意見グループ）のタイトルと説明、およびそれらのクラスタが所属する上層のクラスタのテキストのサンプルを与えるので、上層のクラスタのタイトルと説明を作成してください。\n\n# 指示\n- 統合後のクラスタ名は、統合前のクラスタ名称をそのまま引用せず、内容に基づいた新たな名称にしてください。\n- タイトルには、具体的な事象・行動（例：地域ごとの迅速対応、復興計画の着実な進展、効果的な情報共有・地域協力など）を含めてください\n  - 可能な限り具体的な表現を用いるようにし、抽象的な表現は避けてください\n    - 「多様な意見」などの抽象的な表現は避けてください\n- 出力例に示したJSON形式で出力してください\n\n\n# サンプルの入出力\n## 入力例\n- 「顧客フィードバックの自動集約」: この意見グループは、SNSやオンラインレビューなどから集めた大量の意見をAIが瞬時に解析し、企業が市場のトレンドや顧客の要望を即時に把握できる点についての期待を示しています。\n- 「AIによる業務効率の大幅向上とコスト効率化」: この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\n\n## 出力例\n{\n    \"label\": \"AI技術の導入による意見分析の効率化への期待\",\n    \"description\": \"大量の意見やフィードバックから迅速に洞察を抽出できるため、企業や自治体が消費者や市民の声を的確に把握し、戦略的な意思決定やサービス改善が可能になります。また、従来の手法と比べて作業負荷が軽減され、業務効率の向上やコスト削減といった実際の便益が得られると期待されています。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$1c","model":"gpt-4o-mini"},"hierarchical_overview":{"prompt":"/system \n\nあなたはシンクタンクで働く専門のリサーチアシスタントです。\nチームは特定のテーマに関してパブリック・コンサルテーションを実施し、異なる選択肢の意見グループを分析し始めています。\nこれから意見グループのリストとその簡単な分析が提供されます。\nあなたの仕事は、調査結果の簡潔な要約を返すことです。要約は非常に簡潔に（最大で1段落、最大4文）まとめ、無意味な言葉を避けてください。\n出力は日本語で行ってください。\n","source_code":"$1d","model":"gpt-4o-mini"},"hierarchical_aggregation":{"sampling_num":30,"hidden_properties":{},"source_code":"$1e"},"enable_source_link":false,"output_dir":"2455a1d9-5bf1-495f-a02d-67ca8ac37f0f","skip-interaction":true,"without-html":true,"embedding":{"model":"text-embedding-3-small","source_code":"$1f"},"hierarchical_visualization":{"replacements":[],"source_code":"import subprocess\n\n\ndef hierarchical_visualization(config):\n    output_dir = config[\"output_dir\"]\n    cwd = \"../report\"\n    command = f\"REPORT={output_dir} npm run build\"\n\n    try:\n        process = subprocess.Popen(\n            command,\n            shell=True,\n            cwd=cwd,\n            stdout=subprocess.PIPE,\n            stderr=subprocess.PIPE,\n            universal_newlines=True,\n        )\n        while True:\n            output_line = process.stdout.readline()\n            if output_line == \"\" and process.poll() is not None:\n                break\n            if output_line:\n                print(output_line.strip())\n        process.wait()\n        errors = process.stderr.read()\n        if errors:\n            print(\"Errors:\")\n            print(errors)\n    except subprocess.CalledProcessError as e:\n        print(\"Error: \", e)\n"},"plan":[{"step":"extraction","run":true,"reason":"not trace of previous run"},{"step":"embedding","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_clustering","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_initial_labelling","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_merge_labelling","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_overview","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_aggregation","run":true,"reason":"not trace of previous run"},{"step":"hierarchical_visualization","run":false,"reason":"skipping html output"}],"status":"running","start_time":"2026-01-31T06:13:30.417346","completed_jobs":[{"step":"extraction","completed":"2026-01-31T06:17:05.826989","duration":215.404505,"params":{"prompt":"あなたは専門的なリサーチアシスタントです。与えられたテキストから、意見を抽出して整理してください。\n\n# 指示\n* 入出力の例に記載したような形式で文字列のリストを返してください\n  * 必要な場合は2つの別個の意見に分割してください。多くの場合は1つの議論にまとめる方が望ましいです。\n* 整理した意見は日本語で出力してください\n\n## 入出力の例\n/human\n\nAIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIテクノロジーは、そのライフサイクル全体における環境負荷を削減することに焦点を当てて開発されるべきです。\"\n  ]\n}\n\n/human\n\nAIの能力、限界、倫理的考慮事項について、市民を教育する必要がある。また、教育できる人材を養成する必要がある。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIの能力、限界、倫理的考慮事項について、市民を教育すべき\",\n    \"AIに関する教育をできる人材を養成すべき\"\n  ]\n}\n\n/human\n\nAIはエネルギーグリッドを最適化し、無駄や炭素排出を削減できます。\n\n/ai\n\n{\n  \"extractedOpinionList\": [\n    \"AIはエネルギーグリッドを最適化して炭素排出を削減できる\"\n  ]\n}\n","workers":30,"limit":508,"properties":[],"categories":{},"category_batch_size":5,"source_code":"$20","model":"gpt-4o-mini"},"token_usage":51523},{"step":"embedding","completed":"2026-01-31T06:17:07.406839","duration":1.573006,"params":{"model":"text-embedding-3-small","source_code":"$21"},"token_usage":0},{"step":"hierarchical_clustering","completed":"2026-01-31T09:17:28.190708","duration":10820.776846,"params":{"cluster_nums":[8,64],"source_code":"$22"},"token_usage":0},{"step":"hierarchical_initial_labelling","completed":"2026-01-31T09:20:44.152859","duration":195.955539,"params":{"prompt":"あなたはKJ法が得意なデータ分析者です。userのinputはグループに集まったラベルです。なぜそのラベルが一つのグループであるか解説し、表札（label）をつけてください。\n表札については、グループ内の具体的な論点や特徴を反映した、具体性の高い名称を考案してください。\n出力はJSONとし、フォーマットは以下のサンプルを参考にしてください。\n\n\n# サンプルの入出力\n## 入力例\n- 手作業での意見分析は時間がかかりすぎる。AIで効率化できると嬉しい\n- 今のやり方だと分析に工数がかかりすぎるけど、AIならコストをかけずに分析できそう\n- AIが自動で意見を整理してくれると楽になって嬉しい\n\n\n## 出力例\n{\n    \"label\": \"AIによる業務効率の大幅向上とコスト効率化\",\n    \"description\": \"この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$23","model":"gpt-4o-mini"},"token_usage":43423},{"step":"hierarchical_merge_labelling","completed":"2026-01-31T09:21:32.380835","duration":48.219273,"params":{"prompt":"あなたはデータ分析のエキスパートです。\n現在、テキストデータの階層クラスタリングを行っています。\n下層のクラスタ（意見グループ）のタイトルと説明、およびそれらのクラスタが所属する上層のクラスタのテキストのサンプルを与えるので、上層のクラスタのタイトルと説明を作成してください。\n\n# 指示\n- 統合後のクラスタ名は、統合前のクラスタ名称をそのまま引用せず、内容に基づいた新たな名称にしてください。\n- タイトルには、具体的な事象・行動（例：地域ごとの迅速対応、復興計画の着実な進展、効果的な情報共有・地域協力など）を含めてください\n  - 可能な限り具体的な表現を用いるようにし、抽象的な表現は避けてください\n    - 「多様な意見」などの抽象的な表現は避けてください\n- 出力例に示したJSON形式で出力してください\n\n\n# サンプルの入出力\n## 入力例\n- 「顧客フィードバックの自動集約」: この意見グループは、SNSやオンラインレビューなどから集めた大量の意見をAIが瞬時に解析し、企業が市場のトレンドや顧客の要望を即時に把握できる点についての期待を示しています。\n- 「AIによる業務効率の大幅向上とコスト効率化」: この意見グループは、従来の手作業による意見分析と比較して、AIによる自動化で分析プロセスが効率化され、作業時間の短縮や運用コストの効率化が実現される点に対する前向きな評価が中心です。\n\n## 出力例\n{\n    \"label\": \"AI技術の導入による意見分析の効率化への期待\",\n    \"description\": \"大量の意見やフィードバックから迅速に洞察を抽出できるため、企業や自治体が消費者や市民の声を的確に把握し、戦略的な意思決定やサービス改善が可能になります。また、従来の手法と比べて作業負荷が軽減され、業務効率の向上やコスト削減といった実際の便益が得られると期待されています。\"\n}\n","sampling_num":30,"workers":30,"source_code":"$24","model":"gpt-4o-mini"},"token_usage":16923},{"step":"hierarchical_overview","completed":"2026-01-31T09:21:34.950436","duration":2.56026,"params":{"prompt":"/system \n\nあなたはシンクタンクで働く専門のリサーチアシスタントです。\nチームは特定のテーマに関してパブリック・コンサルテーションを実施し、異なる選択肢の意見グループを分析し始めています。\nこれから意見グループのリストとその簡単な分析が提供されます。\nあなたの仕事は、調査結果の簡潔な要約を返すことです。要約は非常に簡潔に（最大で1段落、最大4文）まとめ、無意味な言葉を避けてください。\n出力は日本語で行ってください。\n","source_code":"$25","model":"gpt-4o-mini"},"token_usage":1808}],"total_token_usage":113677,"token_usage_input":101452,"token_usage_output":12225,"lock_until":"2026-01-31T09:26:34.957884","current_job":"hierarchical_aggregation","current_job_started":"2026-01-31T09:21:34.957870","estimated_cost":0.022552799999999998,"current_job_progress":null,"current_jop_tasks":null},"comment_num":508,"visibility":"public"}}],"$L26","$L27","$L28","$L29"]}],"$L2a"]
2b:I[49985,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Analysis"]
2c:I[68443,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Separator"]
2e:I[27787,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Footer"]
26:["$","$L2b",null,{"result":"$8:1:props:children:1:props:result"}]
27:["$","$L14",null,{"w":"fit-content","mx":"auto","children":["$","$L6",null,{"href":"/","children":["$","$L7",null,{"variant":"outline","size":"md","children":[["$","svg",null,{"ref":"$undefined","xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-chevron-left","children":[["$","path","1wnfg3",{"d":"m15 18-6-6 6-6"}],"$undefined"]}],"一覧へ戻る"]}]}]}]
28:["$","$L2c",null,{"my":12,"maxW":"750px","mx":"auto"}]
29:["$","$L14",null,{"maxW":"750px","mx":"auto","mb":24,"children":"$L2d"}]
2a:["$","$L2e",null,{"meta":{"reporter":"「#みんなでつくる舞鶴2040」プロジェクト","message":"この取組では、みなさま一人ひとりの「2040年の舞鶴ってこうなってほしいな」「こんなことやってみたい！」といった想いを募集します。","webLink":"/","privacyLink":"/","termsLink":null,"brandColor":"#e7adb7","isDefault":false}}]
2f:I[24982,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"ReporterContent"]
2d:["$","$L2f",null,{"meta":"$2a:props:meta","children":"$L30"}]
31:I[21863,["150","static/chunks/59650de3-92bcb3811df53909.js","771","static/chunks/771-6df7383af9cb22cb.js","302","static/chunks/302-413e4f1ef39e273a.js","732","static/chunks/732-1930f1f7bd6746a5.js","619","static/chunks/619-f072ac750404f9da.js","909","static/chunks/909-5e57b886fe664200.js","13","static/chunks/13-f12e2db1f59fda05.js","785","static/chunks/785-90bb983d5bf6562b.js","767","static/chunks/767-6b739569ff79f765.js","609","static/chunks/609-e6afbba7cf646b18.js","182","static/chunks/app/%5Bslug%5D/page-2d806954bbec0f0a.js"],"Image"]
30:["$","$L31",null,{"src":"/kouchouAI-reports/meta/reporter.png","alt":"「#みんなでつくる舞鶴2040」プロジェクト","maxW":"150px"}]
c:{"metadata":[["$","title","0",{"children":"④市民として、2040年に向けてできることとは【1/31 15:00 更新】 - 「#みんなでつくる舞鶴2040」プロジェクト"}],["$","meta","1",{"name":"description","content":"地域社会の活性化には市民参加が不可欠であり、交流やボランティア活動を通じて信頼関係を築くことが求められています。また、健康維持や子どもたちの成長を促すための教育や実体験の重要性が強調され、世代を超えたつながりや地域環境の保護も重要なテーマです。舞鶴市では地域イベントの拡大や子育て支援が求められ、住民の協力による持続可能な発展が期待されています。"}],["$","meta","2",{"property":"og:title","content":"④市民として、2040年に向けてできることとは【1/31 15:00 更新】 - 「#みんなでつくる舞鶴2040」プロジェクト"}],["$","meta","3",{"property":"og:description","content":"地域社会の活性化には市民参加が不可欠であり、交流やボランティア活動を通じて信頼関係を築くことが求められています。また、健康維持や子どもたちの成長を促すための教育や実体験の重要性が強調され、世代を超えたつながりや地域環境の保護も重要なテーマです。舞鶴市では地域イベントの拡大や子育て支援が求められ、住民の協力による持続可能な発展が期待されています。"}],["$","meta","4",{"property":"og:image","content":"http://localhost:3000/2455a1d9-5bf1-495f-a02d-67ca8ac37f0f/opengraph-image.png"}],["$","meta","5",{"name":"twitter:card","content":"summary_large_image"}],["$","meta","6",{"name":"twitter:title","content":"④市民として、2040年に向けてできることとは【1/31 15:00 更新】 - 「#みんなでつくる舞鶴2040」プロジェクト"}],["$","meta","7",{"name":"twitter:description","content":"地域社会の活性化には市民参加が不可欠であり、交流やボランティア活動を通じて信頼関係を築くことが求められています。また、健康維持や子どもたちの成長を促すための教育や実体験の重要性が強調され、世代を超えたつながりや地域環境の保護も重要なテーマです。舞鶴市では地域イベントの拡大や子育て支援が求められ、住民の協力による持続可能な発展が期待されています。"}],["$","meta","8",{"name":"twitter:image","content":"http://localhost:3000/2455a1d9-5bf1-495f-a02d-67ca8ac37f0f/opengraph-image.png"}]],"error":null,"digest":"$undefined"}
11:"$c:metadata"
